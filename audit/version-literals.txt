# Version literals in reader-facing text, one line each (wolf-book#7,
# part 2):
#
#   <path> <literal> <count> audited-at-wolf=<version>
#   <path> <literal> <count> audited-at-lupin=<version>
#   <path> <literal> <count> not-a-toolchain-version
#
# WHY THIS FILE EXISTS. Five pin bumps running healed the same three
# sites by hand and called them, in the pin notes, "residue the runner
# cannot see": §1.2's two Windows archive names, §1.2's
# `+dev.unknown` sentence, and the stamp paragraphs that reverse
# whenever the pinned compiler changes kind. Every other claim this
# book makes is executed — a program by the samples runner, a
# transcript by the console replay, a count by arithmetic on the file
# it counts. These are literals in prose with no run behind them, so
# there is nothing to compare them against and nothing did.
#
# `cargo xtask verify-docs` fails when a literal is missing from this
# list, when its count drifts, when this list names one a page no
# longer carries, or when the named pin has moved past an entry's
# audited-at. So a pin bump forces these sentences to be re-read, entry
# by entry, instead of remembered.
#
# TWO CLOCKS, because two projects release on their own schedules and
# the book pins both. `audited-at-wolf=` is re-read when
# `[wolf].impl_version` moves; `audited-at-lupin=` when the version
# inside `[lupin].version` does. Keying everything to one clock would
# mean an interpreter-only bump re-read nothing, which is the defect
# wolf-web#8 fixed on the site's own version of this check.
#
# ONE ENTRY MAY NAME BOTH CLOCKS, and bs38 is where that was bought.
# The check keys on (page, literal), so two sentences on one page that
# say the same number are ONE entry — and §1.2 has exactly that pair:
# `0.1.31` is the interpreter the COMPILER names (a wolf-side fact, out
# of `crates/wolf_driver/PAIRING`) and, since this bump, also the
# interpreter THIS BOOK PINS (a lupin-side fact). With one clock per
# entry the pair could only ride one of them, and whichever was chosen,
# the other project's bump would leave both sentences unread — which is
# the defect this file exists to stop, reintroduced one level down. So
# a fourth and fifth field are allowed, one per clock, and a pin that
# moves past EITHER fails. `not-a-toolchain-version` still stands alone.
#
# THE THIRD SPELLING is for the version-shaped literals that are not
# claims about wolf or lupin at all: a dependency's version in chapter
# 23, a published record's key in chapter 25. They still get an entry,
# so a NEW literal anywhere fails until somebody classifies it — they
# just do not ride a toolchain clock, because re-reading `rows 1.4.0`
# at every bump is the noise that gets a gate rubber-stamped.
#
# What the check reads: reader-facing text with the ledger comments
# stripped, `console` / `diagnostic` / program fences dropped (the
# replay and the samples runner own those byte for byte), and `text`
# fences KEPT — which is the point, because the two Windows archive
# names live in one and nothing executes them.

# --- the compiler's clock ---
book/ch01.md 0.2.10+dev.unknown 1 audited-at-wolf=0.2.10+dev.3d0a6e1
book/ch01.md 0.2.10+dev.3d0a6e1 1 audited-at-wolf=0.2.10+dev.3d0a6e1
book/ch01.md 0.2.10 3 audited-at-wolf=0.2.10+dev.3d0a6e1
book/ch22.md 0.2.10 1 audited-at-wolf=0.2.10+dev.3d0a6e1
book/ch33.md 0.2.6 1 audited-at-wolf=0.2.10+dev.3d0a6e1
book/ch33.md 0.2.8 1 audited-at-wolf=0.2.10+dev.3d0a6e1

# --- both clocks ---
# `0.1.31` is ONE literal on each of these two pages at bs40 (it was two
# on §1.2 at bs38, when the compiler's interpreter and the book's were
# the same number) and it is still two claims. Claim one: the
# interpreter the COMPILER names, out of `crates/wolf_driver/PAIRING` —
# a wolf-side file that moves when the compiler pin moves. Claim two:
# the sentence around it says that number is "one release before the
# interpreter beside it", and THAT is a lupin-side fact — it stops being
# true the day `[lupin]` moves to 0.1.33 while the compiler still names
# 0.1.31. So the entry rides both clocks, as it did at bs38, for the
# mirror-image reason: then the two numbers were equal and both
# sentences said so; now they differ by one and both sentences say
# that. Either pin moving can falsify the sentence, so either pin
# moving has to re-read it.
book/ch01.md 0.1.31 1 audited-at-wolf=0.2.10+dev.3d0a6e1 audited-at-lupin=0.1.32
book/back/colophon.md 0.1.31 1 audited-at-wolf=0.2.10+dev.3d0a6e1 audited-at-lupin=0.1.32

# --- version-shaped, and about neither tool ---
book/ch23.md 1.4.0 1 not-a-toolchain-version
book/ch23.md v1.4.0 2 not-a-toolchain-version
book/ch23.md v2.1.0 1 not-a-toolchain-version
book/ch25.md 0.1.0 3 not-a-toolchain-version
book/back/solutions.md v1.4.0 1 not-a-toolchain-version
