# ch06 — tells audit (bs14, lane A)

**Counts by severity:** charter 0 · tell 9 · noted 11 — 20 rows.
**Em-dashes (prose only, fences and ledger stripped):** 36 in 2780 prose
words = **12.9 per 1000** — mid-lane; the recurring shape is the
dash-pair insertion inside an already full sentence.
**Negation family:** 9 instances (lines 67–69, 129–130, 214–215,
281, 285–286, 334–335, 441–443, 515–516, 719–721).
**Banned vocabulary:** none (verified by eye and grep; zero
"simply/just/easy/of course", no marketing adjectives, no copula
avoidance).
**Verdict:** strong material at near-register — the hardening refactor
is the book's pedagogy at its best — but the chapter shares whole
formula-sentences with ch04 and ch08 ("Credit where it is due", "The
shape to keep", "Neither is free", the 3 a.m. failure), which reads as
one template writing several chapters.

## Findings

### Tell

- [67-69] «the set of tags this function may fail with, written between
  braces, and complete. Not "some errors" — these errors.» — negation
  punch-fragment on an em-dash; severity: tell; "…written between
  braces, and complete: these errors, no others" — or end at
  "complete."
- [129-130] «Neither is free, and the one that surprises you at 3am is
  not the one with the list.» — the "Neither X is free" formula, third
  instance in the lane (ch01 line 210, ch02 line 262), plus the
  3 a.m.-failure trope ch08 line 1015 also uses; severity: tell; keep
  the content, vary the sentence: "Both cost something; the surprise in
  production belongs to the one without the list."
- [212-215] «a handler is not a statement bolted onto the side, it is
  the other branch of a value» — negation family ("not X, it is Y");
  severity: tell; "a handler is the other branch of a value."
- [274-278] «which is the whole reason to put detail in a tag rather
  than in a printed string: the fact travels to the code that owns the
  policy, and the policy decides whether to print it, point at it, or
  count it.» — self-ranking ("the whole reason") + anadiplosis
  (policy/policy) + rule-of-three; severity: tell; "Detail belongs in
  the tag because the fact travels to the code that owns the policy."
- [280-289] the Coming-from-Go box — «the difference is not brevity» +
  «an unhandled failure is not a style violation, it is a type error» +
  «this book will not pretend that is worthless» + «The propagation is
  still written down. It is one glyph long.» — two negation pivots, a
  self-certifying-candor clause, and a staccato punch pair in one box;
  severity: tell; honesty: the Go accounting itself is fair and TONE
  asks for exactly this comparison — the finding is the rhetoric
  density, not the content; keep the type-error pivot, flatten the
  rest.
- [390] «The shape to keep is the pairing.» — the "The shape to keep"
  section-moral formula, verbatim ch08 line 819's «The shape to keep is
  that…»; severity: tell; SYSTEMIC candidate — a template sentence, not
  a voice; "Acquire, then `errdefer` the release, in adjacent lines."
- [395] «**Credit where it is due:** `errdefer` is Zig's, name and all»
  — the "Credit where it is owed/due" paragraph-opener formula, shared
  with ch08 line 1031; severity: tell; the crediting itself is the
  charter's comparison rule working — vary the opener ("`errdefer` is
  Zig's, name and all") and the credit still lands.
- [515-519] «Which is where we started: 440, confidently wrong, and now
  wrong on purpose in a place you can find. That is not nothing — the
  decision has moved from `to_int`'s default into the caller's loop,
  where the person who knows what a receipt is can see it — but `else
  0` is still the posture that turns "your input is broken" into "your
  total is smaller".» — litotes ("That is not nothing") + dash-pair
  insertion + quoted-antithesis flourish stacked in one paragraph;
  severity: tell; "The decision has moved to the caller's loop, where
  it is visible. It is still the wrong decision."
- [717-721] «nothing else in this program changes: not the signature,
  not the types, and not one line of `top` or `main`.» — negation
  anaphora triplet (lane pattern N1); severity: tell; "nothing else in
  this program changes — signature, types, `top`, and `main` all stand."

### Noted

- [35-37] «it has no bug in the sense of a crash; it has a bug in the
  sense that it lies to its user, quietly, for as long as the input
  stays malformed.» — balanced-antithesis mirror; severity: noted; the
  contrast is the section's thesis; counted for rhythm density.
- [39-40] «failure is a value with a type, the type is written in the
  signature, and the caller cannot fail to notice it» — triad +
  anadiplosis (type/type); severity: noted; it is the chapter's actual
  claim, stated once; density only.
- [71-72] «no wrapping ceremony stands between you and it» — the
  "ceremony" motif (ch04 line 42 "Nothing in those three signatures is
  ceremony"); severity: noted; motif-count entry.
- [110-112] «The signature is now a complete account of what a call can
  do — one `int` and two named ways of not producing one — and it fits
  on a line.» — dash-pair insertion + punch tail; severity: noted;
  parenthesize or split.
- [116-118] «The price is symmetric and immediate: no caller of an open
  row can ever handle it exhaustively by name» — the price/cost ledger
  motif (lane-wide); severity: noted; motif-count entry.
- [169-172] «exactly three things it can want: hand the failure to
  *its* caller, substitute a value, or handle it. Wolf spells them with
  one character, one keyword, and one keyword with a binding.» —
  double rule-of-three; severity: noted; defensible — the three forms
  are the section's actual inventory; density only.
- [176-177] «the same tag, unwrapped and unwrapped again by nobody» —
  ornamental paradox; severity: noted; "the same tag, with no wrapping
  to undo."
- [216-218] «`?` is what you write when the honest answer is "not my
  decision"» — honest-motif (lane-wide tic; ch05 rows it at ×4);
  severity: noted; motif-count entry; "…when the answer is 'not my
  decision'."
- [317-322] «The tag on stderr and exit 1 — the fourth verdict alongside
  chapter 1's three, and the barest of them: a tag, a stream, and a
  number, with no clause tag and no span, because nothing here is a
  fault.» then «A failure that reaches `main` is a program that
  declined to decide, and the deciding is what §6.4 is about.» —
  dash-hinge + triad, then anadiplosis (decide/deciding) + cross-ref
  appendage; severity: noted; split the first at the dash; "(§6.4 is
  the deciding.)"
- [578-581] «`?` says the caller decides. A handler says I decide, here,
  visibly. `else 0` says nobody decides — and a default is a decision,
  made in the one place that cannot know whether zero is safe.» —
  anaphoric triad + dash + closing maxim; severity: noted; the triad is
  the section's real taxonomy and the maxim is earned — counted toward
  the lane's closer-cadence density, no rewrite urged.
- [735-741] «values that hand themselves over, expressions that yield,
  arithmetic that refuses to lie, functions whose signatures are
  checked, containers, type parameters, and failures that travel as
  values with their names on» — anthropomorphized inventory parade
  closing Part 1; severity: noted; the recap is legitimate; two of the
  seven flourishes ("refuses to lie", "hand themselves over") carry the
  part's actual theses — trim the rest to plain nouns.

## Defended, not rowed (false-positive honesty)

- [3-5] the Mahler epigraph («ersterbend — dying away») — TONE §4.1
  register: one epigraph, composer named, light structural analogy
  (the failure announced long before it lands = the row in the
  signature), no technical load; the em-dash is a gloss on a foreign
  term, not a hinge.
- [33-35] «The receipt is wrong and it is confident. … Nobody did.» —
  the section's sass allocation, aimed at the program, dry and true;
  within budget on its page.
- [174-181] «Rows compose by union: everything the callee can fail with
  joins everything the caller can» — TONE §3.2's calibration passage
  lineage ("whose row … flows into ours by union"); sanctioned seed.
- [441-443] «a malformed row is not a defect in the program, it is data
  the program should have an opinion about» — negation in form, but the
  correction (defect vs data) is the section's load-bearing
  distinction; counted in the density header only.
- [721-722] «Hold us to it. If parallelizing this program turns out to
  need a rewrite, the claim was false and the book will say so on that
  page.» — a falsifiable cross-reference to a named, contractually
  bound chapter (13), exactly the shape TONE's tense discipline
  permits and budgets; the ledger records the obligation.
- [592-594] «Before wolf had a compiler it had a syntax argument» —
  past tense as real history, permitted by §1.
