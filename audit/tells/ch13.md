# bs14 tells audit — ch13 (Dividing one job)

**Counts:** charter 0 · tell 6 · noted 5
**Em-dashes:** 20 in 1665 prose words = 12.0/1000w. 5 of the 20 are the
exercise-header template; 15 in running prose.
**Negation family:** 9 hard instances — the "not X. It is Y" reveal closes
nearly every section of this chapter; "rather than" ×3.
**Banned vocabulary:** none.
**Verdict:** vocabulary clean, charter held, but per word this is the
densest antithesis chapter so far — the negation reveal and a "N tools, N
codes, one rule" numeric-triple cadence are both chapter-level tics.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (R1–R3, R5, R8 applied; R9 scan found
one stale fact, below); line references are to the bs14 text.

**Em-dashes:** 0 running in 1624 prose words = 0.0/1000w (was 15
running in 1665 = 9.0/1000w; template 5 → 0 by R1). No site met the
all-four-substitutions-misread bar; the budget is a ceiling, not a
quota.
**Negation family:** 9 hard → 4; the reveal mold 4 → 1 (the surviving
reveal is [334-338]'s second, the chapter's thesis, per this catalog's
own suggestion). "rather than" ×3 → ×1 (R5 ration).
**Numeric-triple cadence:** ×4 → ×1 ([314] survives in the exercise
stem, where the sameness is the question's setup).
**Tells:** 6 → 0. All eleven rows dispositioned below.
**Word count:** 1665 → 1624.
**Reality-check (R9):** [101]'s «`347..352` is the same five bytes the
compiler underlines» went stale when the is27 re-record moved lupin's
file-door lines to line:col — no `347..352` appears in the transcript
at the current pin (addcd7f / 0.1.16). Rewritten as the checkable
present-tense fact: both tools point at the same five characters,
`total`. No other schedule/deferral prose in the reader text (§13.1's
hold is ledger-side only).

- [101, 270, 279, 314] «Two tools, one code, one span» / «Same two tools,
  same agreement, same clause vocabulary» / «Two error codes, two
  boundaries, one rule» / «Same tasks, same channel, same schedules.» —
  one finding: the numeric-triple summary cadence used four times as
  section punctuation; rule-of-three as default rhythm; severity: tell;
  suggest keeping one and flattening the rest ("Both tools reject it with
  the same code at the same span").
  **bs16: fixed-by-R8 (three of four)** — [101] flattened to the
  suggested shape plus the reality-checked five-characters fact; [270]
  → «Both tools refuse the declaration in the same clause vocabulary»;
  [279] → «E1101 and E1102 enforce one rule at two boundaries…»; [314]
  kept (the stem's setup, content not cadence).
- [122-124] «Wolf is not detecting your race. It is declining to compile
  the shape a race is made of» — negation family, the chapter's central
  instance; severity: tell; suggest "Wolf declines to compile the shape a
  race is made of, and the shape has one instance in it."
  **bs16: fixed-by-R3** — suggested sentence adopted verbatim (the
  ledger's quotes of the second clause still match).
- [126-128] «because it is the pitch. Add the second reader and you have
  written the classic lost-update bug — two tasks, one accumulator, an
  answer that depends on timing.» — em-dash plus rule-of-three appositive;
  also "worth stating plainly" earlier in the line is a recurring lane tic
  (see LANE-B.md); severity: noted; suggest a colon before the list.
  **bs16: fixed-by-R5 + R2** — «is worth stating plainly, because it
  is the pitch» → «is the pitch»; colon before the list as suggested.
- [137-138] «would print `0` — not a race at all, a silently wrong
  answer» — negation with em-dash splice; context partly defends (the
  correction race-vs-wrong-answer is real); severity: noted.
  **bs16: fixed-by-R2** — colon for the dash; the corrective stays
  (R3's content test).
- [199-201] «cannot be observed — which is a property of the operation
  you combined them with, not of the language» — em-dash splice plus
  "X, not Y"; severity: noted; a comma serves.
  **bs16: fixed-by-R2** — comma; the "of the operation, not of the
  language" contrast stays (content).
- [231-232] «That is not a limitation to work around.» — negation family
  (counted for density); does corrective work; severity: noted.
  **bs16: kept-with-defense** — corrective the reader plausibly
  arrives with; the neighboring «rather than one» goes to «not one»
  under the R5 ration.
- [237-239] «The rule the capture check enforces is not really about
  closures. It is about what may exist in two places at once» — negation
  family, textbook "not X. It is Y"; severity: tell; suggest "The capture
  check enforces a rule about what may exist in two places at once;
  closures are one boundary it crosses."
  **bs16: fixed-by-R3** — suggestion adopted, merged with the
  channel clause: «…a closure is one boundary that rule crosses, and a
  channel is the other place a value can go».
- [288-289] «a divided job that does not reproduce its sequential answer
  is not faster — it is wrong» — negation family with em-dash; the
  sentence earns some of its shape (the exercise's whole point), but it is
  the fourth reveal of the chapter; severity: tell; suggest "a divided job
  that does not reproduce its sequential answer is wrong, not fast."
  **bs16: fixed-by-R3** — suggested sentence adopted verbatim.
- [320-321] «What made that uneventful was not care; it was that the two
  shapes available to write it in are both shapes the machine can check.»
  — negation family ("not X; it was Y"); severity: tell; suggest "The two
  shapes available to write it in are both shapes the machine can check,
  which is what made it uneventful."
  **bs16: fixed-by-R3** — suggested sentence adopted («…which is what
  made that uneventful»).
- [334-338] «wolf's claim here is not that it caught something Rust
  misses. The claim is narrower … The accumulator in the closure is not a
  bug this book teaches you to avoid. It is a program that does not
  build» — two negation reveals stacked in the closing paragraph;
  severity: tell; suggest keeping the second (it is the chapter's thesis)
  and flattening the first: "wolf's claim is narrower than Rust's and it
  is about the diagnostic."
  **bs16: fixed-by-R3** — first reveal flattened per suggestion; the
  second kept as the chapter's one reveal survivor (its thesis).
- [345] «POSIX grep happily matches it everywhere» — anthropomorphic
  adverb; within the sass budget (page's one wry clause, lands on the
  machine); severity: noted; keep.
  **bs16: kept-with-defense** — TONE §2 budget; unchanged.

## bs16 — register fixes with no bs14 row

- **R1** — all five exercise-header separators to a period.
- **R2** — unrowed hinge dashes: the opener's «in flight — one
  reader…» → colon; «two enforcement moments — the compiler…» →
  colon; «cannot race with anything — there is nothing…» → colon;
  «one owner — `main`, after the join — does the adding» →
  parentheses; «needs a *set* — two operands» → colon; 13-3's «what
  they print — the codes…» → colon; «find out later — Go's race
  detector…» → colon.
- Zero edits inside code, console, REPL, or diagnostic blocks; the
  ledger and the §13.1 HOLD comment untouched; no fence changes.
