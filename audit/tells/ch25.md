# ch25 — tells audit (bs14, lane C)

**Counts:** charter 0 · tell 0 · noted 1
**Em-dashes (prose only):** 0 in 140 prose words = 0.0/1000. (The file is
a held chapter: two HTML-comment ledger blocks and fifteen lines of
reader-facing prose, L80–94. Only the prose is audited.)
**Negation-family count:** 0 (the file's negations — "is not a command",
"resolves to nothing", "changes no compilation", "the pages are not
written" — are scope statements of fact, not the contrast family).
**Banned vocabulary:** none.
**Verdict:** Fifteen lines of deadpan scope statement at the reference
register; nothing separates it from the charter.

## Findings

- [82–87] «an edition key that changes how a package is compiled, a
  standard library on the page to depend on, and a publish client that
  ships `owner/pkg` with its capabilities declared» (and the mirrored
  triple at L86–88, «`wolf publish` is not a command here, `use std.io`
  resolves to nothing, and a manifest's edition key changes no
  compilation») — two three-item parallel lists back to back; both
  enumerate the chapter's actual three sections, so content defends them;
  the mirroring is the only polish; severity: noted; suggestion: none
  required.

## bs16 — notice only

Held chapter; per the bs16 contract stubs are not rewritten. The bs14
catalog stands as-is: 1 noted row (mirrored triples, defended), zero tells, zero charter rows — the
held-chapter boilerplate is the scope register working (LANE-C's own
verdict). No edits to book/ch25.md; densities unchanged.

## bs20 birth audit — §25.3 + the re-cut front matter

Written register-native at bs20 (the chapter's first reader-facing
section; the bs14/bs16 rows above audited the retired stub, whose
mirrored-triple finding retires with it — the triple lost its third
leg when §25.3 shipped). Measured at birth, REGISTER.md's metric
(running-prose em-dashes per 1000 prose words; fences and ledger
comments stripped; bullet markers and heading dashes are layout):

**Em-dashes:** 0 running in 778 prose words = 0.0/1000w (budget
≤5/1000w; no site met the all-four-substitutions-misread bar — the
two candidates both read as colons and became colons). The em-dashes
on the page are tool output, byte-exact (`record written to … —
submit it`, `immutable (append-only, X7)`).
**Dash-pairs:** 0.
**Negation family:** 1 reveal survivor, chosen because it is the
section's actual teaching («the verb does not upload anything. It
writes down what the package is»); the rest are comma-form semantic
contrasts («not a promise in release notes, an arithmetic fact»;
«is not the package, whatever it calls itself») and flat scope
statements (the front matter's reserved sections, the closing client
sentence).
**"rather than":** 0. **"worth V-ing":** 0. **Banned vocabulary:**
none. **Bold:** none in running prose (the covenant sentence stays
in ch24; §25.3 refers to it without re-bolding).
**Terms:** *transparency log* italicized once at definition; one
emphasis italic («what `den/logsearch@0.1.0` *is* stopped being its
author's to change»), the corpus-normal load.
**R9 scan:** clean — the absent registry client is one scope
sentence, present tense, no schedule; the consumer-side env-var log
check stays in the ledger, not the prose; both transcripts replay
from `samples/pkg/record` at the pins.
**Sass ledger:** «whatever it calls itself» (the record read),
«whether the code is any good» (the covenant close) — both land on
the design or the world, none on the reader; under one per page.
**Decision ids:** X7 appears in tool output only (the immutability
refusal, quoted byte-for-byte — the ch23-precedented decision-id
class, not the forbidden campaign-id class).
**Verdict:** at register from birth; nothing here needs a fix pass.
