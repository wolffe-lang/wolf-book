# back/glossary — tells audit (bs14, lane C)

**Counts:** charter 1 · tell 2 · noted 4
**Em-dashes (prose only):** 31 in 790 prose words = 39.2/1000 raw — the
lane's highest number, and every one of the 31 is the entry template
(`**term** (§) — definition`). Running-prose dashes: 0. The density is a
single template decision, not thirty-one findings — flagged SYSTEMIC on
the lane scoreboard.
**Negation-family count:** 5 (L30–31, L54–55, L63–64, L96–97, plus the
softer L9–10 antithesis).
**Banned vocabulary:** none.
**Verdict:** Structurally deadpan, but several definitions import the
chapters' wry antitheses into reference material, where TONE §2 says the
register is deadpan and §3.8 says flat is the voice.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (ratified 2026-08-28, three amendments).

**Em-dashes:** 0 in 744 prose words (was 31, all template). R1's one
punctuation decision applied at the template: the entry separator
`**term** (§) —` becomes a colon (the register's own call for the
glossary — the right side is a definition), all 31 entries in one
edit. Running-prose dashes were already 0.
**Charter:** the package entry's ch23-epigram reprise replaced with
the row's deadpan suggestion — this is the R4 cross-file echo settled
WITH ch23 in the same push: ch23 keeps the (shortened) epigram as the
survivor; the glossary entry now reads "a directory outside your
project, with its own manifest; contrast *module*."
**Negation family:** 5 → 2 kept per defense ([9–10] border, [30–31]
stale-handle — both the guarantee itself); provenance to the positive
definition; task's no-triple to one deadpan contrast ("distinct from
an OS thread").
**Reality check (R9):** no schedule prose; section numbers verified
against their chapters' unchanged headings. Word count: 790 → 744.

## Findings

- [54–55] «**package** (23.0) — a directory somebody else wrote. The
  difference from a module is not technical; it is that you did not read
  it.» — the ch23 L3–5 epigram reprised inside a glossary entry:
  negation family, and sass in reference material (TONE §2: appendices
  are deadpan); severity: charter; suggestion: "a directory outside your
  project, with its own manifest; contrast *module*."
  **bs16: fixed (charter + R4)** — suggestion adopted verbatim; the
  echo's surviving instance is ch23's opening (rewritten there under
  its own row, same push).
- [63–64] «**provenance** (9.3) — not where a pointer points, but which
  lineage it inherits its permission from.» — "not X, but Y" as the
  entire definition; the flat positive exists; severity: tell;
  suggestion: "the lineage a pointer inherits its permission from,
  distinct from its address."
  **bs16: fixed-by-R3** — suggestion adopted verbatim.
- [96–97] «Not a thread, not a coroutine, not a promise.» (**task**) —
  no-anaphora rule-of-three fragment in a definition; severity: tell;
  suggestion: "a unit of concurrent work, owned by a scope" already
  defines it — cut the fragment or keep one contrast.
  **bs16: fixed-by-R3** — one deadpan contrast kept: "distinct from
  an OS thread."
- [whole file] the `**term** (§) — definition` template dash, ×31 —
  template-baked, one bs15 decision (keep, or swap the dash for a colon
  book-wide) rather than per-entry findings; severity: noted;
  suggestion: if the book keeps its dash budget, a colon here retires 31
  dashes in one edit.
  **bs16: fixed-by-R1** — the colon, all 31 entries, one decision.
- [9–10] «The border is what the compiler checks; the interior is
  unchecked because it does not need checking.» — polished antithesis in
  a definition; severity: noted; suggestion: none required.
  **bs16: kept-with-defense** — unchanged.
- [30–31] «A stale handle is a fault, never a dangling read.» — "X,
  never Y"; concise and the contrast is the guarantee itself; severity:
  noted; suggestion: none.
  **bs16: kept-with-defense** — unchanged.
- [19–20] «One tier, one language, no macro expander.» (**comptime**) —
  triadic fragment; terse enough to pass, recorded for rhythm; severity:
  noted; suggestion: none.
  **bs16: kept-with-defense** — unchanged.
