# ch01 — tells audit (bs14, lane A)

Counts: charter 0 · tell 6 · noted 9 — 15 findings.
Em-dash: 19 in 2112 prose words = 9.0 per 1000 (prose only; code fences
and ledger comments stripped). Lowest density in the lane so far, still
~3x a K&R page; the recurring shape is the "— §N.M does X" appendage.
Negation family: 4 (2 canonical "it is not X. It is Y." / "not X, it is
Y"; 2 half-forms).
Banned vocabulary: none (verified by eye; grep and read both clean).
Verdict: the chapter closest to the K&R register — its distance is
cadence, not vocabulary: negation parades, epigram closers on nearly
every paragraph, and cross-reference em-dashes doing a period's job.

## bs16 register rewrite — recomputed header (lane A)

Rewritten against REGISTER.md (R1, R2, R3, R5, R6, R8, plus the
connective ration). Line references below are to the bs14 text.

**Em-dashes:** 1984 prose words, 0 running (0.0/1000w; was 11 running
in 2113 = 5.2/1000w standardized — the bs14 header's 9.0 included the
8 template dashes R1 retires). Every running dash substituted cleanly
under R2's hierarchy (the epigraph's and the cold-open's to colons,
the §N.M appendages to parens or sentences per R6, «and then — only
then —» to the catalog's own wording); none met the survivor bar.
**Negation family:** the chapter keeps ONE reveal survivor by R3's
allowance — [131-132] «`lupin` is not the compiler with the optimizer
switched off. It is an independent implementation…», kept because the
compiler-lite misreading is the one a reader arrives with and the
contrast is §1.2's actual teaching. [207-210] survives positive-first
(«The difference is around the file, not in it»); the three anaphora
parades trimmed per their rows.
**Epigram closers:** 7+ → 3, one per section (§1.2 the silent-compiler
line, §1.4 the interrogate-the-language line, §1.5 «ended on
purpose»); the rest end on fact.
**R4:** ch01 is the ratified SURVIVOR for «Neither X is free» — the
Coming-from-Python box's «Neither answer is free.» stands unedited.
**Connective ration:** «rather than» ×4 → ×2 (kept: written-from-the-
spec, interrogate-the-language); «worth» markers ×2 deleted («worth
slowing down for» → imperative, «worth more than they look» →
sentence deleted), §1.1's title and the literal «worth nothing» stay.
**Reality-check:** no schedule prose found (the bs14 verdict holds:
0 charter). §1.5's line:col claim and the version transcripts already
match the bs17 pins; ledger rows untouched.
**Word count:** 2113 → 1984. Zero edits inside fences; template 8 → 0.

## Findings

- [47-48] «Eighteen lines, and nothing in them is a preview of syntax
  you meet later — this is the language.» — reflexive em-dash (a colon
  serves) plus negation-to-affirmation flip; severity: noted; "Eighteen
  lines, and nothing in them is a preview of syntax you meet later: this
  is the language."
  **bs16: fixed-by-R2** — suggestion adopted (colon).
- [76-78] «No imports, no enclosing class, no guard against being
  imported: there is nothing above `fn main` because there is nothing a
  wolf file needs above `fn main`.» — "no X, no Y, no Z" anaphora parade
  (1 of 3 in this chapter) with an echoed-clause closer; severity: tell;
  "Nothing sits above `fn main` because a wolf file needs nothing there."
  **bs16: fixed-by-R3** — positive claim leads; the parade's three items
  survive as a trailing enumeration («: no imports, no enclosing class,
  no guard against being imported») because they are the migrating
  reader's content, not rhythm.
- [89-92] «there is no separate package manager, no build-system layer,
  no second executable to keep in step with the first» — anaphora parade
  (2 of 3); severity: tell; "there is no separate package manager and no
  build layer."
  **bs16: fixed-by-R3** — suggestion adopted.
- [100-102] «It has no runtime to install beside it, it starts in the
  time the kernel takes to map it, and you can hand it to somebody who
  has never heard of wolf.» — rule-of-three rhythm on a comma splice;
  severity: noted (the three facts are real; the splice and drumbeat are
  the defect); "It needs no runtime, starts as fast as the kernel can
  map it, and runs for somebody who has never heard of wolf."
  **bs16: fixed** — suggestion adopted.
- [102-104] «The compiler said nothing on its way there, which is the
  only thing a compiler should say when it agrees with you.» — wry maxim
  closer; severity: noted (good sentence in isolation; logged as part of
  the chapter-wide epigram-closer cadence, below); keep this one, cut
  siblings.
  **bs16: kept-with-defense** — §1.2's one closer, per the row.
- [131-132] «`lupin` is not the compiler with the optimizer switched
  off. It is an independent implementation of the wolf specification» —
  negation family ("It is not X. It is Y."); severity: tell (TONE §3.7
  models the device once; here it opens the section); "Lead with what
  lupin is — an independent implementation written from the spec — and
  let the not-the-compiler contrast follow if needed."
  **bs16: kept-with-defense (R3 survivor)** — the chapter's one allowed
  reveal: the compiler-lite misreading is real and the contrast is the
  section's teaching. The paragraph's closing epigram recast instead
  (see [58-59…] row).
- [154-155] «Each names the other, and that is not decoration.» — bare
  negation flourish; severity: noted; "Each names the other for a
  reason."
  **bs16: fixed-by-R3** — suggestion adopted.
- [195-197] «There is no project to create first, no manifest to write,
  no directory layout to honor, no virtual environment to activate, no
  lockfile to drift out of date.» — anaphora parade, five items (3 of
  3); severity: tell; "There is no project to create, no manifest, no
  lockfile."
  **bs16: fixed-by-R3** — trimmed to three items («no project to create
  first, no manifest, no lockfile to drift out of date»).
- [207-210] «the difference is not in the file, it is around it» —
  negation family ("not X, it is Y"); severity: noted (compact and
  doing real work; counts toward density); "the file is the same; what
  surrounds it differs."
  **bs16: fixed-by-R3 (comma form)** — positive first: «The difference
  is around the file, not in it.» The dash pair bracketing the Python
  fence goes with it (colon in, plain sentence out).
- [279-280] «When a later chapter claims wolf does something, you can
  ask.» — unnamed forward reference; severity: noted, defended: no
  feature is promised (tense discipline targets deferral, and this is a
  present-tense claim about spec authority); acceptable as is.
  **bs16: kept-with-defense.**
- [310-312] «and then — only then — run» — dramatic em-dash
  interruption pair; severity: noted; "and only then run."
  **bs16: fixed-by-R2** — suggestion adopted.
- [317] «The phases matter because they divide the ways a program can
  end.» — editorialized significance ("matter" announces importance
  instead of showing it); severity: noted; "The phases divide the ways
  a program can end."
  **bs16: fixed-by-R5** — suggestion adopted.
- [354-355] «Two implementations, one rule, one error code, two
  voices:» — numeric parallel parade (AI cadence); severity: noted
  (summarizes a fact the page just demonstrated; the drumroll is the
  only excess); "One rule, two voices:"
  **bs16: fixed-by-R11** — suggestion adopted.
- [58-59, 102-104, 136-137, 162-163, 274-276, 295-296, 409-411]
  «it gets more honest in every chapter.» / «A language with one
  implementation cannot tell the difference between what it specified
  and what it happens to do.» / «Output that differs from a book's
  printed output is a version question before it is a bug.» / «a way to
  interrogate the language rather than a calculator that happens to
  speak it.» / «Chapter 2 is where that message stops being a
  surprise.» / «a program which ends this way ended on purpose.» —
  epigram/punchline closer on nearly every paragraph; TONE's own
  calibration ends passages this way once, the chapter does it 7+
  times; severity: tell (SYSTEMIC — cadence, not any single sentence);
  let most paragraphs end on fact, keep one closer per section.
  **bs16: fixed-by-R8** — kept one per section: [102-104] (§1.2),
  [274-276] (§1.4, the section's thesis), [409-411] (§1.5). Recast:
  [58-59] (the honest-motif promise ends at «running example»),
  [136-137] (folded to a plain consequence), [162-163] (imperative:
  «check the versions before calling it a bug»), [295-296] («Chapter 2
  explains that message»), plus the unrowed «most useful four
  characters» ranking («Part 2 leans on it»).
- [57, 72, 143, 211-212, 229-230] «§1.5 shows what the first lie does»
  / «— §1.3 makes that concrete» / «— §1.5 begins the habit» / «§1.5 is
  where you watch what that compile buys» / «the exercises below are
  where you write one» — the "— §N.M does X" cross-reference appendage,
  five times in one chapter; severity: tell (SYSTEMIC — a template
  shape; most cross-refs read fine as parentheses or their own plain
  sentence); "(§1.3 makes that concrete.)"
  **bs16: fixed-by-R6** — [72] to parens; [143] to its own sentence with
  the R5 marker removed («Slow down at the pages where they speak in
  turn; §1.5 begins the habit»); [57], [211-212], [229-230] were already
  woven grammar (no dash) and stay.
