# ch03 — tells audit (bs14, lane A)

**Counts by severity:** charter 0 · tell 7 · noted 12 — 19 rows.
**Em-dashes (prose only, fences and ledger stripped):** 27 in 2150 prose
words = **12.6 per 1000** — mid-lane; the worst offenders are two
dash-pair insertions in one §3.1 paragraph.
**Negation family:** 8 instances (lines 50–51, 85–86, 151–153, 199–200,
393–394, 470–471, 557–558, 605–606), of which 393–394 is TONE §3.4's own
calibration seed — sanctioned.
**Banned vocabulary:** none (verified by eye and grep; zero
"simply/just/easy/of course", no marketing adjectives, no copula
avoidance).
**Verdict:** the chapter is close to register sentence-by-sentence — the
trap walkthrough is the charter's own seed — but it leans on inherited
formulas ("N X, one Y", "worth taking literally", the negative-space
opener) and repeats ch02's measurement-credo sentence nearly verbatim,
which reads as a template, not a voice.

## bs16 register rewrite — recomputed header (lane A)

Rewritten against REGISTER.md (R1, R2, R3, R4, R5, R7, R10, plus the
connective ration). Line references below are to the bs14 text.

**Em-dashes:** 1 in 2055 prose words = 0.5/1000w running (was 20
running in 2150 = 9.3/1000w standardized; bs14's 12.6 included the 7
template dashes R1 retires). The one survivor is the §3.3
calibration-seed dash («— in release builds too», TONE §3.4 verbatim;
scope-guard defended).
**R4 (the credo's first true test, second half):** ch03's
measurement-credo echo is REWRITTEN in local words — the bill box now
reads «the answer to "what does the checking cost" is a measurement:
chapter 21 runs the comparison against C on a dated machine…» (ch02
keeps the ratified survivor). ch03's negative-space opener («Look at
what `kind` does not contain: a `return`.») is the ratified SURVIVOR
for that template and STANDS UNEDITED (the row's local suggestion
yields to the ratified list; ch08/ch10's echoes are the rewrite
sites).
**R10:** the unblessed bold «**assignment hands the value over.**»
demoted to plain prose per the amendment, with its self-ranking lead
(«the sentence of this section») cut; «what does X3 cost» restated as
content («what does the checking cost») per
decision-numbers-are-apparatus; the italic stress on «*a measurement*»
goes plain. «CI» stays (ruled vocabulary).
**Negation family:** 8 → the defended set (the §3.4
counterexample contrast, the poisoned/empty correction in comma form,
the TONE-seed triple); [85-86] and [470-472] positive-first; no
reveal-mold survivor needed (0 of 1 used).
**Connective ration:** «rather than» ×4 → ×2 (kept: the hardware-
felt-like sass line, the refuses-rather-than-guessing division of
labor); «worth taking literally» → imperative, «worth meeting on
purpose» deleted.
**Reality-check:** no schedule prose (bs14: 0 charter, confirmed at
the current pins); ledger rows untouched.
**Word count:** 2150 → 2055. Zero edits inside fences; template 7 → 0.

## Findings

### Tell

- [50-51] «Look at what `kind` does not contain: a `return`.» —
  negative-space opener, the same formula as ch08's «notice what is not
  in it» (line 115) — a lane-wide template; severity: tell; "The whole
  body of `kind` is one `match`, so there is no `return`."
  **bs16: kept (R4 survivor)** — ch03 is the ratified home of the
  negative-space opener; the survivor stands and ch08/ch10 rewrite
  their echoes.
- [85-86] «It is not a stylistic preference; it is the narrower claim,
  and the compiler holds you to it:» — negation family ("It is not X;
  it is Y"); severity: tell; "`let` is the narrower claim, and the
  compiler holds you to it:"
  **bs16: fixed-by-R3** — suggestion adopted («Reach for `let` first:
  it is the narrower claim…»).
- [140-148] «`push` puts an element on the end of the list — chapter 5
  is where lists get their chapter — and it changes the list …» plus
  «Why the language insists — what it buys, and the whole system the
  marker belongs to — is chapter 7's story» — two dash-pair insertions
  in one paragraph, each smuggling a cross-reference or a ledger clause
  into an already long sentence; severity: tell; parenthesize the
  chapter-5 aside; "Chapter 7 explains why the language insists."
  **bs16: fixed-by-R6 + R7** — the chapter-5 aside to parens; the
  why-it-insists pair recast as a plain sentence («Chapter 7 tells why
  the language insists, and the whole system the marker belongs to»).
- [150-153] «The fifth line is the sentence of this section:
  **assignment hands the value over.** … Not a second list with the same
  contents, and not a view onto somebody else's list — the list, the one
  `names` was holding, handed across.» — self-ranking ("the sentence of
  this section") + bold rule-sentence in running prose + negation
  anaphora resolved on an em-dash; severity: tell; "Assignment hands
  the value over: `roster` now holds the list `names` was holding."
  **bs16: fixed-by-R10 + R3** — bold demoted to plain prose (the
  amendment), «the sentence of this section» cut, the anaphora folded
  into one comma-form contrast.
- [219-225] «There is a much longer one — why the rule exists, what it
  buys, why a language would choose it over a garbage collector, and
  what the `copy` in the compiler's suggestion costs — and it is chapter
  7's, which opens on this diagnostic and does not stop for a while.» —
  dash-pair enclosing a four-item list (the notation.md template shape)
  + "what it buys" ledger motif + trailing wry clause; severity: tell;
  "Chapter 7 tells the longer story and opens on this diagnostic."
  **bs16: fixed-by-R7** — the four-item list moves behind a colon as
  the sentence's payload; the dash pair goes; the wry closer stays as
  §3.1's one closer.
- [466-469] «this book does not make measured claims without the
  measurement: chapter 21 runs the comparison against C, prints the
  numbers CI produced on a dated machine, and names the cases where the
  check is still visible.» — the measurement-credo sentence, nearly
  verbatim ch02 lines 491–494 ("this book does not make measured claims
  without the measurement: chapter 21 runs the comparison, prints the
  numbers CI produced, and names the cases where C still wins") — a
  copied template including its rule-of-three; severity: tell; SYSTEMIC
  candidate — state it once book-wide (the front matter already does)
  and let each chapter say only "chapter 21 measures this."
  **bs16: fixed-by-R4** — the echo rewritten in local words (the
  register's own worked example); ch02 keeps the ratified survivor.
- [470-472] «the alternative is not "free arithmetic", it is arithmetic
  whose failures are undefined» — negation family ("not X, it is Y");
  severity: tell; "the alternative is arithmetic whose failures are
  undefined."
  **bs16: fixed-by-R3** — suggestion adopted.

### Noted

- [3-5] «what a name holds, what a piece of code hands back, and what
  arithmetic does when the answer will not fit» — rule-of-three chapter
  inventory; severity: noted; defensible — the three items are the
  chapter's three sections in order; recorded for rhythm density.
  **bs16: kept-with-defense.**
- [81] «one name, one type, new contents» — triad rhythm; severity:
  noted; content is real; density only.
  **bs16: kept** (its lead-in dash to a colon under R2).
- [117] «Wolf lets you do either and asks you to say which.» — epigram
  closer; severity: noted; good sentence, counted toward the lane's
  closer-cadence density (see also 224–225, 292, 324, 458, 578).
  **bs16: kept-with-defense** — R8's budget holds chapter-wide (most
  sections end on fact; the maxims kept are these rowed keepers).
- [195-197, 281] «Two tools, one rule, the two enforcement moments
  chapter 1 set up» / «Three shapes, one rule.» — the "N X, one Y"
  numeric-parallel formula twice in one chapter (ch01 line 354 and ch08
  line 249 carry more); severity: noted; lane-wide pattern count.
  **bs16: kept** — both are plain count-led openers with no stage
  direction (R11's permitted shape).
- [199-200] «The second note is worth taking literally.» and [294] «one
  consequence worth meeting on purpose» — the "worth X-ing" meta-formula
  twice (ch08 rows the same shape); severity: noted; open with the
  content instead.
  **bs16: fixed-by-R5** — «worth taking literally» → the imperative
  «Take the second note literally:»; «worth meeting on purpose»
  deleted («The reading rule has one consequence:»).
- [199-200] «`names` is not poisoned, it is empty — hand it something
  and it is a working name again» — negation family; severity: noted;
  context defends it: it corrects the Rust reader's real expectation of
  poisoning; counted for density.
  **bs16: kept-with-defense** (comma form; the trailing dash to a
  semicolon under R2).
- [224-225] «You will write a great deal of wolf on that much.» —
  epigram closer ending the section; severity: noted; closer-cadence
  member.
  **bs16: kept-with-defense** — §3.1's one closer.
- [326] «Loops are the exception that proves the rule» — stock idiom
  used loosely (the loops are an exception; nothing is proved);
  severity: noted; "Loops are the exception, and there are three of
  them."
  **bs16: fixed** — suggestion adopted.
- [363-365] «When the count is known, `for i in 0..n` says so; when the
  exit condition is the point, `loop` plus `break v` says that
  instead.» — balanced-antithesis mirror; severity: noted; content-
  bearing advice; density only.
  **bs16: kept-with-defense.**
- [427-429] «which means `count` is still 0, which means the average of
  nothing is a trap rather than whatever the hardware felt like» —
  chained "which means" anadiplosis + wry clause; severity: noted; one
  "which means" survives; "whatever the hardware felt like" is the
  page's sass allocation and lands on the machine.
  **bs16: fixed** — anadiplosis broken («…never ran, so `count` is
  still 0 and…»); the sass clause stays; the trailing dash-pair aside
  to parens under R2.
- [456-458] «A hash function wants `wrapping`. A gauge wants
  `saturating`. A receipt total wants neither, which is why the trap
  above is the right answer.» — anaphoric staccato triad; severity:
  noted; the three cases are real and the advice is the section's
  point; density only.
  **bs16: kept-with-defense.**
- [460, 466] «The bill for checking.» box speaks of "X3" and "the
  numbers CI produced" — decision-numbers and CI argot in reader prose;
  severity: noted; TONE's letter bans sprint/pin vocabulary, not
  D/X-numbers, and notation.md introduces CI — the same boundary
  question ch07's X1 row files for the human.
  **bs16: fixed-by-R10** — the boundary question is now ruled:
  decision numbers are apparatus, so «what does X3 cost» becomes «what
  does the checking cost» (the trap line's own X3 is tool voice and
  stays); «CI» stays by the same ruling.

## Defended, not rowed (false-positive honesty)

- [393-401] «the program does not wrap, does not continue, and does not
  negotiate. That is a *trap*: the fault of a defined execution. … Most
  bugs should be so polite.» — TONE §3.4's calibration passage, lifted
  as seed exactly as §3 invites; the negation triple and both wry
  clauses are the charter's own.
- [322-324] «"Neither arm is more right" is the compiler declining to
  guess.» — quotes the diagnostic and builds on it, exactly the §1
  two-voices discipline; "declining to guess" echoes front-matter
  phrasing but is doing primary-source work here.
- [557-559] «The compiler did not say "this looks incomplete"; it handed
  you a value your program has no answer for» — negation in form, but
  the contrast (vague feeling vs concrete witness) is the technical
  point of exhaustiveness-by-counterexample; counted in the density
  header only.
- [575-578] «totality is a static property, the type checker owns it,
  and the interpreter refuses the program rather than guessing» — a
  triad, but a real division of labor stated once; no action.
