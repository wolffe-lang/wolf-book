# ch32 — tells catalog and register dispositions

Chapter 32 had no catalog from bs14 and no bs16 disposition file. It
was measured against the register on 2026-09-08 and needed no edit.

## Densities (standard metric)

| | measured |
|---|---|
| prose words | 1,336 |
| running em-dashes | 0 (0.0/1000) |
| «rather than» | 1 (the epigraph, «down rather than up», a semantic contrast) |
| «worth V-ing» | 0 in prose (three in the audit ledger, out of scope) |
| negation family | 2 |
| section-close maxims | 1 |

## Dispositions

**R3.** One reveal: §32.3 «Wolf did not write a better allocator. It
changed the question to one that has a two-line answer.» Kept as the
chapter's survivor; it is the measured comparison the section just
printed (65 lines against two). §32.3 «the machinery underneath a
program, not the program itself» is comma-form content contrast and
stays.

**R8.** One closer: «Seven unsafe blocks and one obligation, against a
brace you already had.» Kept; it restates the two measurements
(`audit-surface`'s seven blocks and one `#[trusted]` string against
chapter 28's `region words {`) and a reader can check both. The chapter
is the book's coda and TONE §3 models a closing cadence once.

**R4.** «Two honesties before the program» echoes TONE §3.3's «One
honesty up front», the calibration seed, and is not a cross-chapter
template. Kept.

**R10.** No non-blessed bold in running prose. D11 appears only inside
the quoted `wolf audit-surface` output, which is tool output and
untouchable.

**Untouched.** All code blocks, both console transcripts, the `wc`
block, the epigraph, and the audit ledger.

## Gates

`cargo xtask verify-docs` exit 0 (no edit was made; recorded for the
catalog's completeness).
