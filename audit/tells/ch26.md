# ch26 — tells audit (bs14, lane C)

**Counts:** charter 2 · tell 5 · noted 9
**Em-dashes (prose only):** 32 in 2211 prose words = 14.5 per 1000
**Negation-family count:** 3 clear (+1 borderline antithesis)
**Banned vocabulary:** none
**Verdict:** Close to the K&R register — the measurements are honest and the
economy is real; the residue is an em-dash reflex (14.5/1000), a habit of
closing sections on "not X, but Y" turns, four bold run-in leads in §26.5,
and two British spellings breaking the charter's American-spelling rule.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (ratified 2026-08-28, three amendments);
line references below are to the bs14 text.

**Em-dashes:** 0 running in 2172 prose words = 0.0/1000w (was ~26
running in 2239 = 11.6/1000w standardized; template 7 → 0 by R1).
Dash-pairs to parentheses (R7); hinges to colon/comma per R2's order;
no survivor met the all-four-misread bar.
**Charter:** "rumour" → "rumor", "favour" → "favor" (both rows), plus
unrowed "totalling" → "totaling" (same TONE §1 rule).
**R10:** the part-opening rule statement and all four §26.5
measurement/thesis bolds demoted to plain prose — the 2026-08-28
ruling postdates this catalog's tentative defense of [13–15], and
LANE-C's systemic row names the measurement-section bolding as the
one shape to unbold across ch26/ch28/ch32.
**Negation family:** 3 → content survivors in positive-first or comma
form ("Documentation says what can go wrong; a row is checked", "a
choice, not a limit", "in how, never in what" kept per defense).
**R5:** "worth sitting with" and "worth naming" markers deleted
(unrowed); §26.5's "worth having in the part's first project and worth
nothing afterward" KEPT — a literal cost/benefit claim, not the
significance-marker connective.
**Reality check (R9):** no schedule prose; `env_args()` claim is
present-tense fact at the pin; fences untouched, no fence-status
changes. Word count: 2239 → 2172.

## Findings

- [5-6] «a whole program with a name, a binary, and somebody's actual work
  to do» — rule-of-three cadence; severity: noted; the three items are
  genuine (name, binary, purpose), so context defends it.
  **bs16: kept-with-defense** — unchanged.
- [8-11] «You read the same tool twice — once in the manner Kernighan and
  Ritchie wrote it, once in wolf — and the rule for those two columns is
  stated here» — em-dash parenthetical pair; severity: noted; counts
  against the budget, parens or commas would serve.
  **bs16: fixed-by-R7** — parentheses.
- [13-15] «**Where wolf is shorter or safer, we show it and measure it.
  Where wolf is longer, clumsier, or gives something up, we say so on the
  page, in the same voice.**» — bold-for-emphasis in prose; severity:
  noted; a one-off part-opening rule statement, defensible as deliberate;
  the "longer, clumsier, or gives something up" triplet is a real
  enumeration.
  **bs16: fixed-by-R10** — bold demoted to plain prose (the ratified
  rule-sentence set is closed; the enumeration stays as written).
- [26] «rumour» — British spelling; severity: charter (TONE §1: American
  spelling); suggestion: "rumor".
  **bs16: fixed** — "rumor".
- [35-36] «Walking through text, you are either inside a word or between
  words» — "we" walks / "you" acts (the reader is walking the algorithm,
  not typing); severity: noted; suggestion: "Walking through text, the
  machine is either inside a word or between words."
  **bs16: fixed** — suggestion adopted; the second clause follows
  ("the number of times it crosses").
- [52-54] «Every difference between the two programs is therefore a
  difference in *how*, never in what» — negation-family-adjacent
  antithesis; severity: noted; borderline — the contrast is the section's
  real point; suggestion: "Every difference between the two programs is a
  difference of method."
  **bs16: kept-with-defense (italics dropped)** — the contrast is the
  section's real point; only the emphasis italics on "how" go.
- [202-203] «the sentinel reaches the exit status — the convention working
  exactly as designed» — reflexive em-dash where a comma serves; severity:
  noted.
  **bs16: fixed-by-R2** — comma.
- [279-281] «because the loop was never long — K&R's chapter-1 counter is
  one of the tightest pieces of teaching code anyone has written» —
  reflexive em-dash where a colon or period serves; severity: noted.
  **bs16: fixed-by-R2** — colon; the adjacent "worth sitting with"
  marker deleted (R5).
- [345-346] «A row is not documentation about what can go wrong. It is the
  thing the compiler checks.» — negation family ("not X. It is Y."), the
  construction called out by name in the sprint decree; severity: tell;
  suggestion: "Documentation says what can go wrong; a row is checked."
  **bs16: fixed-by-R3** — suggestion adopted verbatim.
- [481-483] «Blank lines and comment-only lines removed — because the C
  carries a twelve-line attribution header the wolf file has no reason to
  have — that is **76 lines of C against 52 lines of wolf**» — em-dash
  pair where parens serve, plus bold-for-emphasis on the number; severity:
  noted; the bolded measurement is arguably the page's headline claim.
  **bs16: fixed-by-R7 + R10** — dash pair to parentheses; the bold
  goes with the systemic unbold (the number is the loudest thing on
  the page already, as ch28's twin row says).
- [484-485] «a smaller one than the folklore about safe languages
  predicts» — vague-attribution-shaped; severity: noted; context defends
  it — "folklore" is deliberately naming the absence of a source.
  **bs16: kept-with-defense** — unchanged.
- [490, 496, 505-506, 518-519] «**The arithmetic helper the C needs, wolf
  does not.**» / «**The C's `main` is 38 lines of code; wolf's is 27.**» /
  «**Ritchie's program takes its file names from the command line. Ours
  holds them in a list.**» / «**Ritchie's program counts a file of any
  size. Ours reads the file into memory first.**» — bold-sprinkle: four
  bold sentence-leads in one section; severity: tell; they function as
  run-in headings, so one fix is to demote all four to plain first
  sentences (the paragraphs stand without the bold); possibly systemic if
  other measurement sections repeat the shape.
  **bs16: fixed-by-R10** — all four leads demoted to plain first
  sentences (the systemic measurement-section unbold; the paragraphs
  stand without the bold).
- [509-510] «those four lines are a choice this chapter made and not a limit
  it ran into» — negation family ("a choice … and not a limit");
  severity: tell; suggestion: "this chapter chose those four lines;
  `env_args()` removes them."
  **bs16: fixed-by-R3 + R7** — "a choice, not a limit" (comma form);
  the `env_args()` aside moves to parentheses.
- [533] «favour» — British spelling; severity: charter (TONE §1: American
  spelling); suggestion: "favor".
  **bs16: fixed** — "favor".
- [533-536] «neither of them is lopsided because wolf is a better language
  than C. They are lopsided because C's chapter 4 and chapter 6 spend
  their pages on stacks and on `malloc`» — negation family ("not because
  X … because Y"); severity: tell; suggestion: "they are lopsided because
  C's chapters 4 and 6 spend their pages on stacks and `malloc`; wolf
  spends those pages on nothing at all" (the denial clause can go — the
  reason carries the point alone).
  **bs16: fixed-by-R3** — the denial clause goes; the reason carries
  the point.
- [whole file] 32 prose em-dashes at 14.5/1000 words — density above a
  K&R budget, with the reflexive instances cited at 202, 281, 482;
  severity: tell; most pairs are deliberate parentheticals, so the fix is
  pruning the cited reflexive uses, not a sweep.
  **bs16: fixed-by-R2/R7** — every running dash substituted under the
  ordered hierarchy (parens for the deliberate pairs, colon/comma for
  the hinges); 0 remain.

## bs16 — register fixes with no bs14 row

- **R1** — all six exercise-stem separators to periods; 26-2's
  stem-body dash pair to parentheses.
- **R2/R7** — unrowed pairs and hinges: L19–21 folklore (parens), L30
  (parens), L43 (parens), L125–126 (parens, emphasis italics dropped),
  L134 (parens; "totalling" → "totaling"), L244 (parens), L446 (colon),
  L508–509 (parens), L523–524 (parens), L530–531 (parens).
- **R5** — L277 «worth sitting with», L435 «worth naming» deleted.
- Zero edits inside code, console, or diagnostic blocks; the audit
  ledger comment is untouched.
