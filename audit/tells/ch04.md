# ch04 — tells audit (bs14, lane A)

**Counts by severity:** charter 0 · tell 5 · noted 11 — 16 rows.
**Em-dashes (prose only, fences and ledger stripped):** 22 in 1594 prose
words = **13.8 per 1000** — mid-lane; mostly single hinge-dashes where a
colon or period serves.
**Negation family:** 8 instances (lines 106–107, 227–228, 255–256, 282,
420–421, 427–428, 430–432, and the "behave nothing like them" half-form
at 135–136).
**Banned vocabulary:** none (verified by eye and grep; zero
"simply/just/easy/of course", no marketing adjectives, no copula
avoidance, no curly quotes).
**Verdict:** the shortest and cleanest chapter in the lane so far —
mostly at register, its distance being inherited house formulas ("worth
stating", "…'s business", the bolded rule-sentence) rather than any
local excess.

## Findings

### Tell

- [106-107] «the signature is not documentation that hopes to stay true.
  It is documentation the compiler enforces, at every call:» — negation
  family ("is not X. It is Y") with personification ("hopes to stay
  true"); severity: tell; "the signature is documentation the compiler
  enforces, at every call:"
- [130-133] «That is the whole argument for mandatory signatures in one
  picture — the error names a *disagreement between two places*, which
  is possible only because both places said something.» — self-ranking
  significance ("the whole argument … in one picture") + reflexive
  em-dash + italic stress; severity: tell; "The error names a
  disagreement between two places, which is possible only because both
  places said something."
- [382-384] «The rule is one sentence: **A parameter borrows; a return
  moves.**» — the "the rule is one sentence" signpost + bolded
  rule-sentence in running prose — the exact shape ch07 line 374 and
  ch09 line 109 carry (lane-wide template); severity: tell; if bolded
  rule-sentences are a deliberate book convention, bs15 should ratify
  it in TONE.md once; the signpost clause cuts either way.
- [420-428] «passes the machine words that locate the list, not the
  elements — the cost is the same whether the list holds two rows or two
  million … the thing the compiler is actually tracking is not data
  movement, it is *who is allowed to read this next*.» — two negation
  pivots in one callout box, the second italic-stressed, plus a
  hinge-dash; severity: tell; keep the second pivot (it is the box's
  payload), flatten the first: "passes the machine words that locate
  the list; the cost is the same at two rows or two million."
- [430-432] «which is not a coincidence, and not the whole language.» —
  double-negation flourish; severity: tell; "Every sample in Part 1
  only reads, by design; mutation through a parameter is §7.4's."

### Noted

- [5-6] «Code that appears twice is a function that has not been named
  yet.» — epigram opener-closer; severity: noted; good sentence,
  counted toward the lane's closer-cadence density.
- [42-43] «every word in them is load-bearing» — the "load-bearing"
  motif (ch02 line 231 "the load-bearing word"; recurs lane-wide);
  severity: noted; motif-count entry.
- [76-81] «`width` is an `int` because `0` is, `row` is a `str` because
  `lines()` yields those, `i` is an `int` because …» then «inside a body
  it does everything, and at a signature it does nothing.» — anaphoric
  triad + balanced-antithesis mirror; severity: noted; both carry real
  content (the triad is the evidence, the mirror is the rule); recorded
  for rhythm density.
- [86-87] «an error rather than an invitation to guess» — "rather than"
  contrast frame + the declines-to-guess motif (front matter and ch03
  carry siblings); severity: noted; motif-count entry.
- [183-185] «with no apparatus at all — a function value is an ordinary
  value, so it goes in a `let`, into a call, and out of one» —
  hinge-dash + rule-of-three; severity: noted; the triad enumerates
  real positions; colon for the dash.
- [211] «Two limits are worth stating while the form is in front of
  you.» — the "worth X-ing" meta-formula (ch03 ×2, ch08 ×2 — lane-wide);
  severity: noted; "Two limits, while the form is in front of you:"
- [218-219, 313] «which is chapter 10's business» / «which is exercise
  4-6's business» — the "…'s business" cross-reference formula (ch02
  line 47 makes three lane-wide); severity: noted; formula-count entry.
- [253-254] «The order of the output is the point» — the "is the point"
  formula (ch02 line 291, how-to-read line 54); severity: noted;
  formula-count entry.
- [278-280] «teardown has to unwind what setup wound … Write
  acquisitions in order, and the releases arrange themselves.» —
  chiasmus + epigram closer; severity: noted; stylish and earned once —
  counted toward closer-cadence density.
- [282-283] «The stack is built at runtime, not at compile time, and
  that is the part worth a program of its own:» — X-not-Y + "worth"
  formula in one sentence; severity: noted; "That the stack is built at
  runtime deserves a program of its own:" — and the contrast is the
  technical point, so it survives.
- [389-391] «reading somebody else's value for the length of a call is
  the overwhelmingly common case, and wolf spends no syntax on the
  common case.» — anadiplosis (common case / common case) + the
  spends/buys ledger motif; severity: noted; "…is the common case, and
  wolf spends no syntax on it."

## Defended, not rowed (false-positive honesty)

- [315-318] «The samples above print instead, because they run under the
  reference interpreter, which has no filesystem by design; chapter 26
  opens real files under the compiler, and the shape there is the shape
  here.» — reads adjacent to deferral prose but is not: "no filesystem
  by design" is scope with a decision behind it, stated in present
  tense, and the cross-reference is to a bound chapter. Survives the
  landing test.
- [135-143] the Coming-from-Python box — two contrasts, both the
  technical point (hint vs checked signature; per-function vs
  per-expression burden); the register TONE's comparison rule asks for.
- [380-381] «There are none, and one list was read by two functions and
  then read again by `main`.» — the demonstration-first move at its
  best; cited as calibration-quality, no action.
- [319-320] «`errdefer`, which waits for chapter 6 because it needs an
  error to defer on» — book-internal forward reference to a bound
  chapter, and the page's one wry clause; within budget.
