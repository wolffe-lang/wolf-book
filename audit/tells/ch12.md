# bs14 tells audit — ch12 (Channels and select)

**Counts:** charter 0 · tell 4 · noted 10
**Em-dashes:** 35 in 2857 prose words = 12.3/1000w. 9 of the 35 are the
`**Exercise N-M** *(kind · tool)* —` header template (systemic, see
LANE-B.md); ~26 in running prose, roughly triple a K&R page.
**Negation family:** 8 hard instances ("not X; it is Y" / "X, not Y");
plus "rather than" ×10 as an adjacent reflex.
**Banned vocabulary:** none.
**Verdict:** the charter holds and the vocabulary is clean; the chapter's
distance from K&R is rhythm — the "not X; it is Y" antithesis as a default
move and an em-dash budget spent about double.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (rules R1–R3, R5–R8, R10 checked; R9
scan clean — no deferral/schedule prose in the reader text); line
references below are to the bs14 text.

**Em-dashes:** 0 running in 2717 prose words = 0.0/1000w (was 26
running in 2857 = 9.1/1000w standardized; template 9 → 0 by R1). Every
running dash substituted cleanly under R2's hierarchy; no site met the
all-four-substitutions-misread bar. The budget (≤5/1000w) is a ceiling,
not a quota.
**Negation family:** 8 hard → 5; survivors are the catalog-defended
content contrasts ([65], [163], [193-194], [491-492], [580-582]).
The reveal mold 3 → 0.
**"rather than":** ×10 → ×2 ([46], [166]), inside the R5 ration.
**Tells:** 4 → 0. All fourteen rows dispositioned below; noted rows
with a defense are kept.
**Word count:** 2857 → 2717.

- [3-5] «That was deliberate — a channel is the obvious thing and the
  obvious thing is usually right — but the shelf is about to need the
  parts nobody guesses» — em-dash pair as parenthetical in the opening
  sentence; severity: noted; suggest a colon and a period: "That was
  deliberate: the obvious thing is usually right. But the shelf…"
  **bs16: fixed-by-R2** — suggestion adopted, keeping the channel
  clause: «deliberate: a channel is the obvious thing, and the obvious
  thing is usually right. But the shelf…».
- [65] «Closing is not a fault and never becomes one.» — negation family
  (counted for density); context defends: it states the rule the section
  proves; severity: noted; keep.
  **bs16: kept-with-defense** — states the rule the section proves;
  unchanged.
- [108-110] «with no fences, no volatile, and nothing to remember» —
  rule-of-three cadence; the third item is flourish, the first two are
  real; severity: noted; suggest "with no fences and no volatile".
  **bs16: fixed-by-R3** — suggested double adopted; the same
  sentence's dash pair goes to parentheses under R7.
- [163] «No traversal, no serialization, no copy.» — rule-of-three;
  context partly defends (three real avoided costs); severity: noted;
  acceptable as is if the chapter's other triples thin out.
  **bs16: kept-with-defense** — the chapter's other triples thinned
  ([108-110] to a double, [507] cut), so the condition is met;
  unchanged.
- [193-194] «has made an ownership mistake, not a concurrency mistake» —
  "X, not Y" corrective; defensible, the distinction is the paragraph's
  point; severity: noted; keep.
  **bs16: kept-with-defense** — R3's semantic-contrast test passes;
  unchanged.
- [245-246] «A `timeout` arm is therefore not a delay. It is the arm that
  wins when no other arm can» — negation family, cluster member;
  severity: tell; suggest "A `timeout` arm wins when no other arm can; it
  neither bounds the body's runtime nor fires when work was available."
  **bs16: fixed-by-R3** — suggestion adopted, the two-mistakes clause
  kept as a trailing relative.
- [272-275] «it is not the wall clock, not the arm's position in the
  source, and not whichever channel happened to be touched first. It is a
  recorded decision» — negation triple plus the "It is" reveal: negation
  family and rule-of-three in one sentence; severity: tell; suggest "The
  choice comes from the scheduler's seed, not the clock or the source
  order, and a recorded decision can be replayed."
  **bs16: fixed-by-R3** — the triple folded into the positive claim
  («drawn from the *scheduler's seed*, not from the wall clock, the
  arm's position in the source, or whichever channel happened to be
  touched first: a recorded decision, and a recorded decision can be
  replayed»); the reveal goes.
- [312-313] «And the verdict — `SCHEDULE-DEPENDENT` — is a finding rather
  than a failure» — interruptive em-dash appositive where commas serve;
  severity: noted; suggest "And the `SCHEDULE-DEPENDENT` verdict is a
  finding, not a failure".
  **bs16: fixed-by-R2** — suggestion adopted in comma form.
- [328-329] «is not a task — it is an entry in the runtime's wait set» —
  negation family with em-dash splice; severity: tell; suggest "An arm
  waiting on a channel, a timer, or an I/O completion costs an entry in
  the runtime's wait set, not a task."
  **bs16: fixed-by-R3** — suggestion adopted («costs an entry in the
  runtime's wait set for the duration of the `select`, not a task»).
- [491-492] «the classic lock-order deadlock is not detected, it is
  absent, and it is absent for the same reason a sorted list has no
  inversions» — negation family (counted); context defends: the
  detected/absent distinction is the section's sharpest true claim;
  severity: noted; keep.
  **bs16: kept-with-defense** — R3's semantic-contrast test passes;
  the chapter's one reveal-shaped survivor, chosen because the
  contrast is the section's teaching; unchanged.
- [507] «Three decisions, two schedules, one outcome.» — rule-of-three
  cadence as a closing flourish; the numbers are real (they appear in the
  transcript above), which defends it halfway; severity: noted.
  **bs16: fixed-by-R8** — the cadence closer cut; the paragraph now
  ends on the checked claim («…not argued on the page»).
- [547-548] «The bug is not caught. It is unsayable, which is a stronger
  guarantee than any detector» — negation family, cluster member;
  severity: tell; suggest "The bug is unsayable, a stronger guarantee
  than any detector."
  **bs16: fixed-by-R3** — suggestion adopted; the tail's "rather than
  a runtime check" goes to "not a runtime check" under the R5 ration.
- [551-552] «Deadlock through a channel remains perfectly constructible —
  exercise 12-2 is four lines of it — and that asymmetry is deliberate.»
  — em-dash pair where parens serve, plus "perfectly" as intensifier;
  severity: noted; suggest "(exercise 12-2 is four lines of it)".
  **bs16: fixed-by-R2** — parentheses adopted and "perfectly" dropped.
- [580-582] «That is concurrency: several jobs in flight, structured so
  that none of them outlives its brace. It is not the same subject as
  taking one job and dividing it» — negation family at the chapter seam;
  defensible: the not-the-same-subject distinction is the seam's job and
  TONE §3.7 blesses the shape; severity: noted; keep.
  **bs16: kept-with-defense** — TONE §3.7's seam shape; only the
  clause's "rather than about a scope" goes to "not about a scope"
  (R5 ration) and the paragraph's "Worth noticing" opener is R5's
  (see below).

## bs16 — register fixes with no bs14 row

- **R1** — all nine exercise-header separators: «*(kind · tool)* —» →
  «*(kind · tool)*.»
- **R2** — unrowed hinge/pair dashes: §12.1's «is unbounded — a
  queue…» → colon (with the fronted negation «what neither is, is
  unbounded» → «neither is unbounded» under R3); «nobody will send —
  which is §10.2's trap» → semicolon + own sentence (R6); «returns
  `Closed` — an ordinary error value» → comma; the rendezvous edge
  dash pair → parentheses (R7); «Frozen data qualifies — the cold
  open» → colon; the region-changes-hands list pair → parentheses
  (R7); «That is `[mem.tier0.move.2]` —» → colon; «a design technique
  rather than a mechanism — moving…» → «makes region transfer a
  design technique: moving…»; «the one waiting — which is exercise
  12-2's…» → comma; the Go-throughput dash → colon; the
  canonical-order dash → colon; «in two voices — the compiler…» →
  colon; 12-8/12-9 stem dashes → semicolon/colon.
- **R5** — four markers deleted: «the reason is worth stating» (close
  values), «That is the sentence to hold on to» (seeded replay, folds
  into the claim), «worth being precise» → «a bounded one», «the
  reflex is worth interrupting once» (cut), «Worth noticing what none
  of those did» (cut; the following sentence carries). The «rather
  than» connective rationed ×10 → ×2.
- Zero edits inside code, console, REPL, or diagnostic blocks; the
  audit ledger comment is untouched. R9 scan: no deferral/schedule
  vocabulary in the reader text; the fence set is untouched (no fence
  authority exercised in this chapter).
