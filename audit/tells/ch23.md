# ch23 — tells audit (bs14, lane C)

**Counts:** charter 0 · tell 6 · noted 5
**Em-dashes (prose only):** 19 in 1361 prose words = 14.0/1000. Four are
exercise-stem template dashes (`**Exercise 23-N** *(...)* —`) and one is a
heading title dash; ~14 are running-prose dashes. Over the reflexive line:
several serve where a colon or comma would (rows below).
**Negation-family count:** 7 (L4, L58, L84, L111–112, L189 ×2, L237–239).
**Banned vocabulary:** none.
**Verdict:** vocabulary is clean and the sentences are short, but the
chapter's default rhetorical move is antithesis — "not X — it is Y" and
"X rather than Y" recur enough to become the page's rhythm, which with the
em-dash density is what separates it from the K&R register.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (ratified 2026-08-28, three amendments);
line references below are to the bs14 text.

**Em-dashes:** 0 running in 1275 prose words = 0.0/1000w (was ~14
running in 1361 = 10.3/1000w in the standardized metric; template 5 →
1). R1 retires the four exercise-stem dashes to periods; the §23.3
heading title dash stays — the heading template is not in R1's
enumerated set and its anchor is load-bearing; flagged for the
orchestrator as a cross-lane consistency call. No running dash met the
all-four-substitutions-misread bar; the R2 budget is a ceiling, not a
quota, and each substitution was tried in order.
**Negation family:** 7 → content survivors only: L84 in comma form
("a subscription, not a dependency"), the L189 ledger claim opened as
its own sentence ("The ledger is not an input to resolution: …" — the
section's teaching, the chapter's one reveal-shaped survivor), L237–239
force-push kept per catalog defense, and "one digest over the whole
tree, not one per file" (semantic contrast, comma form).
**"rather than":** 5 → 2 (L70 manifest-edit and L211 real-difference
survive; R5 amendment cap respected).
**Reality check (R9):** §23.1's "you can point at today" retired to a
scope statement; probed at pin addcd7f — `wolf add left-pad` still
refuses registry deps (exit 2), re-verify line added to the #157 ledger
row. Fences untouched; no fence-status change in this chapter.
**Word count:** 1361 → 1275.

## Findings

- [3–5] «A module is a directory in your project. A *package* is a
  directory somebody else wrote, and the difference is not technical — it
  is that you did not read it.» — negation family + em-dash where a colon
  serves; the epigram earns its content but opens a chapter-long pattern;
  severity: tell; suggestion: "…somebody else wrote. The difference is
  that you did not read it."
  **bs16: fixed-by-R3 + R2** — suggestion adopted verbatim; the R4
  cross-file echo is settled ch23-side (this instance survives as the
  chapter's opening claim; the glossary's reprise goes deadpan in the
  same push).
- [58–59] «There is no code. Not a script, not an expression, not a
  string that gets interpolated» — negation anaphora in rule-of-three
  rhythm; severity: tell; suggestion: "There is no code: the values are
  strings, integers, bare words, lists, and maps, and that is the entire
  grammar."
  **bs16: fixed-by-R3** — suggestion adopted verbatim.
- [61–63] «**reading a manifest tells you everything a package does to
  the machine that builds it, because a manifest cannot ask for anything
  to happen.**» — bold-for-emphasis on a full sentence in running prose
  (bold-sprinkle); recurs at ch24 L10 — lane-level pattern; severity:
  tell; suggestion: drop the bold; the sentence carries itself.
  **bs16: fixed-by-R10** — bold demoted to plain prose (the ratified
  amendment: never italics); the sentence carries itself.
- [66–71] «owned rather than claimed … a manifest edit rather than a
  search-and-replace» — first two of the chapter's five "X rather than Y"
  contrasts (also L212, L241, L245); as a default frame it is the
  negation family in formal wear; severity: tell; suggestion: vary the
  frame — state the positive fact and let one contrast stand per section.
  **bs16: fixed-by-R5 (amendment)** — "rather than" 5 → 2: L66
  becomes "so that every name has an owner", L241 and L245 lose the
  frame (rows below); the manifest-edit contrast (L70) and §23.3's
  real-difference contrast (L211) survive under the ≤2 cap.
- [84] «a moving target is not a dependency, it is a subscription.» —
  negation family; the aphorism is true and within the sass budget, so
  recorded for density; severity: noted; suggestion: keep, or "a moving
  target is a subscription, not a dependency" if the family thins.
  **bs16: fixed-by-R3** — the family thinned chapter-wide, so the
  comma form is adopted.
- [110–112] «they are printed here because they are not an add-on
  report — they are what the resolver has to know anyway.» — negation
  family + em-dash where a comma serves; severity: tell; suggestion:
  "they are printed here because the resolver has to know them anyway."
  **bs16: fixed-by-R3 + R2** — suggestion adopted (semicolon joins the
  two clauses; the dash and the not-an-add-on frame go).
- [127–128] «Compare with the ecosystems most of us came from, where
  adding a dependency prints a progress bar.» — sass; lands on the
  machine, one per page, within budget — recorded to keep the page's
  count honest; severity: noted; suggestion: none.
  **bs16: kept-with-defense** — within TONE §2's budget; unchanged.
- [180–184] «One line per resolved dependency — the name, the version, a
  content address, and the capability set — under a one-line header … a
  local tree you own and can edit — pinning a hash to a directory you are
  editing would make every save look like an attack.» — three em-dashes
  in one paragraph, the third where a colon serves; severity: tell;
  suggestion: parenthesize the field list; colon before "pinning a hash".
  **bs16: fixed-by-R7 + R2** — field list to parentheses; colon before
  "pinning a hash"; the lockfiles double-duty dash-pair in the same
  section also moves to parentheses (R7).
- [189] «Notice what the ledger is *not*. It is not an input to
  resolution.» — negation-led definition, second use of the
  "notice what is not there" device (first at L56); the device is the
  chapter's actual thesis (a format defined by what it cannot do), so
  context defends it; severity: noted; suggestion: keep one instance;
  open the second positively ("The ledger has exactly one job…", which
  L196 already says).
  **bs16: fixed per suggestion** — the L56 instance keeps the device
  (the chapter's thesis); L189 opens on the claim itself ("The ledger
  is not an input to resolution: …"): the stage direction goes, the
  content contrast stays as the chapter's one reveal-shaped survivor
  (R3).
- [237–239] «An author who force-pushes `v1.4.0` has not changed your
  dependency; they have broken your build, loudly, at the first command
  that touches it.» — negation antithesis; earned, single, checkable;
  severity: noted; suggestion: none.
  **bs16: kept-with-defense** — unchanged.
- [241] «Two details of the hash are decisions rather than accidents.» —
  fourth "rather than" of the chapter (see L66–71 row); severity: noted;
  suggestion: "Two details of the hash are deliberate."
  **bs16: fixed-by-R5 (amendment)** — suggestion adopted; the whole-
  tree/per-file sentence in the same paragraph goes to "X, not Y"
  comma form and its dash-pair to parentheses (R7).
