# ch10 — tells audit (bs14, lane A)

**Counts by severity:** charter 0 · tell 11 · noted 13 — 24 rows.
**Em-dashes (prose only, fences and ledger stripped):** 42 in 2685 prose
words = **15.6 per 1000** — tied with ch05 for the lane's highest
density; several dash-pair insertions carry cross-references or lists.
**Negation family:** 14 instances (lines 61–64, 146–147, 165–166,
263–264, 265–266, 271, 333, 339, 362–364, 405–406, 416, 435, 456, and
the "not the scheduler's business" mirror at 116) — second only to ch09
in the lane.
**Banned vocabulary:** none (verified by eye and grep; zero
"simply/just/easy/of course", no marketing adjectives, no copula
avoidance).
**Verdict:** the argument is K&R-grade — one transcript carries the
whole case against detached spawn — but the prose performs it: the
"worth X-ing" formula five times, the negative-space opener a third
lane appearance, and closer negation pivots on nearly every section.

## Findings

### Tell

- [61-66] «Count what is not in that program. No thread handles, no
  `join` call, no mutex around the store, no `Send` or `Sync` bound on
  `Doc`, no library imported, and no way for either reader to still be
  running when `print` executes.» — negative-space opener (the ch03
  «Look at what `kind` does not contain» / ch08 «notice what is not in
  it» template, third lane appearance) launching a six-beat "no X"
  anaphora parade, the lane's longest; severity: tell; keep two beats:
  "There are no thread handles and no `join` call — and no way for
  either reader to still be running when `print` executes."
- [68, 82, 153, 331, 414] «Two questions are worth separating» / «the
  two facts worth memorizing» / «It is worth knowing now» / «are worth
  stating plainly» / «is worth naming here» — the "worth X-ing"
  meta-formula five times in one chapter (ch03 ×2, ch04 ×2, ch08 ×2
  elsewhere); severity: tell; SYSTEMIC — open with the content; the
  reader will price it.
- [146-149] «Wolf has no detached spawn — no `go`, no `Thread::new`, no
  `submit` — so "who owns this task" is never a question you can
  decline to answer. You answer it by naming a scope» — dash-pair
  enclosing a "no X" triplet (two lane templates at once) + anadiplosis
  (answer/answer); severity: tell; "Wolf has no detached spawn, so
  every task has an owner: you name the scope."
- [168] «So the shape to build with is the one the cold open uses.» —
  the "the shape to keep/build" section-moral formula (ch06 line 390,
  ch08 line 819 — third lane appearance); severity: tell; SYSTEMIC;
  "Build what the cold open builds:"
- [253-267] the Coming-from-Go box — «Go's own documentation is the
  honest source» + «The leak is not detected by a tool; it has no
  spelling.» + «is not a state this language has» + «a defined outcome,
  with a roster, at exit 3» — honest-motif, two negation pivots, and a
  triad closer in one box; severity: tell; the accounting is fair and
  credited (issue number and all) — the finding is rhetoric density;
  keep "it has no spelling" (it is the box's real claim), flatten the
  rest.
- [269-274] «The honest half of that trade is on the page too. Wolf did
  not make the mistake impossible — … What changed is the *class* of
  the bug. A silent leak grows until a process dies of memory a week
  later; a deadlock is a fault, at a line, with the list of who was
  waiting, and the same list every time you run it.» — honest-motif +
  negation + balanced mirror + triad stacked as a section close;
  severity: tell; the content survives as: "Wolf did not make the
  mistake impossible; it changed the class of the bug. The hang is now
  a fault, at a line, with a roster."
- [325-327] «The crossing point is the brace. That is the sentence to
  keep, because it is what makes concurrent failure ordinary:» —
  self-ranking signpost ("the sentence to keep" — ch08's self-grading
  family, lane systemic); severity: tell; "The crossing point is the
  brace." stands alone; the reader will keep it.
- [362-365] «The interesting question is not whether the blocked reader
  dies — the scope cannot join until it does — but whether it gets to
  clean up on the way out:» — "not whether X … but whether Y" negation
  frame + dash-pair insertion; severity: tell; "The blocked reader will
  die — the scope cannot join until it does. The question is whether it
  gets to clean up on the way out:"
- [396-402] «it is delivered at a blocking point, it arrives at the
  blocked task as an ordinary error value, and the task returns through
  its own frames — which runs its own `defer`s, in chapter 4's order,
  for chapter 4's reasons. There is no unwinding mechanism anywhere in
  this, because there is no unwinding anywhere in wolf» — triad +
  anaphora + the echoed-clause negation shape (ch01 lines 76–78:
  "there is nothing above `fn main` because there is nothing a wolf
  file needs above `fn main`" — same mold); severity: tell; "There is
  no unwinding mechanism because wolf has none: a cancelled `recv`
  hands back an error and the return runs the `defer`s."
- [404-412] «This is a deliberate trade — the alternative is
  asynchronous interruption, which is how other runtimes acquired the
  rule that you may not allocate in a signal handler — and the cost is
  stated rather than hidden: cancellation latency is bounded by the
  distance to your next blocking point» — dash-pair insertion +
  trade/cost ledger motif + self-certifying candor ("stated rather than
  hidden"); severity: tell; the signal-handler aside earns its place;
  "the cost is stated rather than hidden" is the book notarizing itself
  again — delete the clause, keep the cost.
- [435-437, 452-457] «A task is not a coroutine and not a promise. It
  is work the runtime schedules onto OS threads» and «nothing in a
  signature anywhere in this chapter said "concurrent", no caller had
  to be rewritten …, and the reader in the cold open was an ordinary
  closure over ordinary values. What a function needs in order to spawn
  is not a keyword. It is a scope» — the closing section opens and
  closes on "It is not X. It is Y." pivots with a no-anaphora triad
  between; severity: tell; open affirmatively ("A task is work the
  runtime schedules onto OS threads") and keep one pivot at most.

### Noted

- [8-11] «Chapter 8 closed with two sentences it said were the whole of
  wolf's concurrency safety story … Here is the second of them,
  cashed.» — self-referential ranking + ledger motif ("cashed");
  severity: noted; the callback itself is the book's structure working;
  "cashed" is the motif-count entry.
- [68-74] «*What does this program mean when more than one thing is
  happening?* … *What does this program cost?*» — two italicized
  organizing questions where TONE §3.6 models one; severity: noted;
  context defends: the material genuinely poses both and the part
  answers them with different tools; recorded against the
  one-per-chapter budget.
- [72-73] «`lupin` owns the scheduler, and — as §12.2 shows — owns a
  seed you can hand it» — dash-pair cross-reference insertion +
  anadiplosis (owns/owns); severity: noted; parenthesize.
- [110, 116] «inside the braces, order is the scheduler's business» /
  «after the brace, order is not the scheduler's business at all» —
  mirror antithesis on the "…'s business" formula; severity: noted;
  defensible — the mirror is the model's actual two facts; counted for
  formula density.
- [145] «That is an ordinary name-resolution error, and its
  ordinariness is the point.» — "is the point" formula + anadiplosis;
  severity: noted; formula-count entry.
- [159] «Wolf's rule is decision D14, and it is short.» — D-number in
  reader prose; severity: noted; same boundary question ch03/ch07 file
  for the human (TONE bans sprint/pin apparatus, not D-numbers).
- [163-166] «it is moved, it is frozen, or it is wrapped in a
  synchronizing type. Two tasks and one plain mutable local is not on
  the list.» — rule-of-three + punch closer; severity: noted;
  defensible — the three verbs are D14's actual law; the punch line is
  counted toward closer-cadence density.
- [171-173] «Chapter 12 gives that sentence its vocabulary — capacity,
  close, and what a channel is allowed to carry — and §12.4 gives the
  synchronizing type its keyword.» — dash-pair enclosing a triad
  (template shape); severity: noted; parenthesize.
- [232] «every phrase in it is load-bearing» — the "load-bearing" motif
  (ch02, ch04 — third lane appearance); severity: noted; motif-count
  entry.
- [237-239] «being closed is what makes the condition decidable. The
  runtime knows where every task is parked because it parked them.» —
  epigram closer with anadiplosis; severity: noted; earned once;
  closer-cadence member.
- [242-243] «its absence is part of the proof rather than decoration» —
  "rather than" frame + the "not decoration" motif (ch01 line 154);
  severity: noted; motif-count entry.
- [251] «Now the comparison the section is named for.» — meta-narration
  signpost paragraph; severity: noted; cut — the box announces itself.
- [339-349] the second Go box — «the contrast here is not about style»
  opener + «a `log.Println` and a hope» + «All three work; all three
  are the program carrying its own error plumbing.» + «Wolf's version
  is the brace you already wrote.» — negation opener and anaphora, but
  the box is fair, credited (`errgroup`), and its punch closer is the
  section's true summary; severity: noted; counted toward box-rhetoric
  density; the opener clause is the cuttable part.

## Defended, not rowed (false-positive honesty)

- [3-6] the Bruckner epigraph and its two-sentence landing («A scope
  exit joins all its children. Nothing outlives the bar line.») — TONE
  §4.1's worked placement 3, near-verbatim; sanctioned.
- [118-122] «one arrow in and one arrow out, which is Nathaniel J.
  Smith's black-box rule from the nursery design that this construct
  descends from» — credited lineage, exactly the charter's comparison
  rule; no action.
- [234, 241, 245] the bold clause-quote paragraph leads («"every live
  task is blocked…"») — pseudo-headings quoting the trap's own words;
  primary-source discipline (§1 two-voices), and the enumeration is
  real; no action.
- [206-209] «Here is the oldest bug in structured-less concurrency,
  ported to the shelf. A reader is spawned to serve a query. The query
  never arrives.» — staccato, but each sentence is load-bearing setup;
  calibration-quality, no action.
- [419-421] «chapter 9 kept C from reaching into wolf's memory model,
  and this clause keeps wolf's scheduler from reaching into C's
  stack.» — a mirror, but the symmetry is the technical fact; no
  action.
- Tense discipline: clean — the chapter states scope (no detached
  spawn) as decision, cites shipped sections forward, and names no
  schedule; the ledger's own cross-reference accounting row concurs.
