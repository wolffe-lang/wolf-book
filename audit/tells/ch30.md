# ch30 — tells audit (bs14, lane C)

**Counts:** charter 2 · tell 8 · noted 7
**Em-dashes (prose only):** 25 in 2296 prose words = 10.9/1000. Seven are
exercise-stem template dashes; ~18 are running-prose dashes. Under the
lane's 14/1000 constant, still over a K&R budget.
**Negation-family count:** 13 (L3–4 ×2, L103–104, L125–127, L137,
L236–237, L299, L364–365, L404–406 ×2, L445, L453–454, L497–499) — the
heaviest load in the lane.
**Banned vocabulary:** none.
**Verdict:** The measurements and the §30.4 bill-paying are the register
working, but the chapter leans on the negation family harder than any
other file in the lane — the emphasis-by-negation windup («Not "usually"
and not "for the seeds we tried" —») recurs verbatim-shaped in ch31, and
two bold thesis sentences plus two British spellings put real distance
between this chapter and the charter.

## Findings

- [132] «*Sharding* is deciding the division of labour before you have
  anyone to give it to.» — "labour": British spelling; severity: charter
  (TONE §1: American spelling); suggestion: "labor".
- [396–397] «each is four lines that differ from its neighbour in two
  identifiers» — "neighbour": British spelling; severity: charter;
  suggestion: "neighbor".
- [3–4] «This one is not, and the reason is not that wolf is newer. It is
  that the sequential version…» — double negation windup into "It is Y"
  as the chapter-opening move (the lane's signature opener: ch24 L85–86,
  ch28 L7–8); severity: tell; suggestion: "This one you could not have,
  and the reason is the sequential version: forty lines shorter, correct,
  and the one to ship for this input."
- [103–104] «The first decision is not "how do I spawn a task" — it is
  *what does a task get*» — negation family + em-dash + emphasis italics
  in one sentence (the same triple-combo as ch24 L233–235); severity:
  tell; suggestion: "The first decision is what a task gets, and it is
  arithmetic, made before anything spawns."
- [125–128] «There is no shared cursor, no work queue, and nothing for
  two tasks to disagree about» — no-anaphora in rule-of-three rhythm
  (ch24 L133–135's shape); the content is real; the drumbeat is the
  tell; severity: tell; suggestion: "Nothing is shared: the disagreement
  was settled by three lines of arithmetic while the program was still
  single-threaded."
- [236–237] «Notice what the four bodies do not have. No lock, no atomic
  counter, no mutable shared accumulator.» — the
  notice-what-is-not-there device (fourth lane instance: ch23 L56, L189,
  ch28 L272) + a no-triple; severity: tell; suggestion: "The four bodies
  hold no lock and no shared accumulator; each task's only route out is
  `hits.send(i)`."
- [402–407] «the reason is not discipline but that the compiler declines
  the shapes those bugs are made of. What it does not do is write the
  fan-out for you.» — "not X but Y" plus the what-it-does /
  what-it-does-not paired frame closing the section; severity: tell;
  suggestion: "the compiler declines the shapes those bugs are made of;
  the fan-out is still yours to write."
- [445–447] «Not "usually" and not "for the seeds we tried" — the hash
  does not depend on the seed, because the output does not depend on the
  schedule, because…» — emphasis-by-negation windup, the same device
  ch31 uses three times (ch31 L10–12, L60–61, L85–86): an author habit
  spanning the two closing chapters; severity: tell; suggestion: "The
  hash is the same for every seed, because the output does not depend on
  the schedule."
- [453–454, 490] «**A seed does not reproduce a parallel run.**» /
  «**Do not try to reproduce the run. Make the answer not depend on
  it.**» — bold-for-emphasis on full thesis sentences, twice in one
  section, prefaced by "worth reading twice" (editorialized
  significance); the lane's thesis-bolding device (ch23 L61, ch24 L10,
  ch26 §26.5 ×4); severity: tell; suggestion: drop both bolds and cut
  "worth reading twice" — the short declaratives already carry it.
- [497–500] «is therefore not a flaky test. That is not luck, and it is
  not a claim about the scheduler. It is a claim about the program» —
  triple negation anaphora resolving to "It is Y"; severity: tell;
  suggestion: "A test that asserts four exact lines is a claim about the
  program, and the two hundred runs above check it."
- [137–138] «Four is written into the program as four spawns, not
  computed from the input.» — "X, not Y"; single and factual; severity:
  noted; suggestion: none.
- [227–229] «There is no join to remember and no handle to keep, which is
  the whole point of a scope — you cannot leak a task out of one, because
  the brace that ends the block is the join.» — negation pair + reflexive
  dash; the closing clause is the earned one; severity: noted;
  suggestion: "A scope has no separate join: the brace that ends the
  block is it."
- [298–301] «the shared mutable state is not shared. One task owns it,
  three lines own the accumulation, and the other four tasks communicate
  by sending.» — paradox flourish + parallel triple; severity: noted;
  suggestion: "`found` is owned by one task; the other four communicate
  by sending."
- [303–305] «and — much more importantly — it makes the report
  independent of the order the messages arrived in» — em-dash pair
  around an editorial intensifier; severity: noted; suggestion: "and,
  more importantly, …" or drop the ranking.
- [325–327] «A parallel tool whose output you have to squint at is a
  slower tool with extra steps.» — sass; "with extra steps" is meme
  register rather than 1978-dry; within budget, recorded for the
  register call; severity: noted; suggestion: "A parallel tool with
  unstable output is a slower tool."
- [364–366] «The freeze is not a property of the `needles` binding; it is
  a property of the data» — negation family, but the contrast corrects a
  real misreading (that is what the diagnostic's underline teaches), so
  context defends; severity: noted; suggestion: none.
- [491–494] «indices instead of lines, rendezvous instead of buffering, a
  report that walks the input instead of the results» — instead-of
  anaphora in rule-of-three; the three are the section's three real
  decisions; severity: noted; suggestion: unparallel one item if bs15
  thins the chapter's rhythm.
