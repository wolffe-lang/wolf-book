# bs14 tells audit — ch17 (The failing schedule, replayed)

**Counts:** charter 0 · tell 5 · noted 11
**Em-dashes:** 31 in 2737 prose words = 11.3/1000w. 8 of the 31 are the
exercise-header template; 23 in running prose.
**Negation family:** 14 hard instances; "rather than" ×4. Includes a
section heading built on the family ("Say what this is, and what it is
not").
**Banned vocabulary:** none.
**Verdict:** charter and vocabulary clean and much of the chapter is
genuinely in register (the report walkthrough is deadpan and earns it);
the distance is the "worth V-ing" marker at its chapter high (×4) and a
part-closing peroration that stacks the lane's every rhythm. The Cage the
Elephant epigraph is TONE §4.2's own worked placement — not a finding.

- [101, 304, 413, 437] «worth a chapter» / «It is worth naming» / «worth
  having in one sentence» / «it is worth building» — one finding: the
  "worth V-ing" significance marker four times, the lane's peak;
  severity: tell; suggest deleting the marker each time ("Once the races
  are gone, what is left is a small set of bugs a machine can search").
- [10-13] «A leaked task has no spelling; a lock-order deadlock has no
  spelling; a data race needs a second writer the type system will not
  give you.» — anaphoric triple opening the chapter; the three recaps
  are real and cited later, which defends the content if not the
  drumbeat; severity: noted.
- [85] «Say what this is, and what it is not» — negation-family shape in
  a section heading (counted for density); the section genuinely
  separates two bug classes; severity: noted.
- [93-95] «It is not a data race, and this book is not going to blur the
  difference in order to make a better story.» — negation pair; defended:
  the contrast is the technical point and the honesty is the book's
  charter working; severity: noted; keep.
- [102-104] «a *small* set of bugs about the order of events — and a
  small set of bugs about the order of events is something a machine can
  search» — anadiplosis (full-phrase repetition as a hinge) plus em-dash;
  severity: noted; suggest "and a small set is something a machine can
  search."
- [153-154] «a *fact* about the program rather than an anecdote about a
  machine that was busy» — "X rather than Y" flourish; the contrast is
  real (reproducibility); severity: noted.
- [156] «`--explore=N` does not pick a schedule. It searches:» — negation
  reveal (counted); three words, immediately cashed by a transcript;
  severity: noted; keep.
- [199-201] «This is the sentence the whole chapter is written for: the
  finding arrives with its own reproduction.» — self-editorialized
  significance ("the sentence the whole chapter is written for");
  severity: noted; suggest "The finding arrives with its own
  reproduction."
- [210-213] «green means "every schedule I could reach agrees," and
  nothing else means green» — chiasmus epigram as paragraph closer (the
  ch15 aphorism pattern); severity: tell; suggest "green means every
  schedule the tool could reach agrees."
- [274-275] «The `finished` channel is gone, the `select` is gone, the
  worker count is gone, and the loop is a `for`.» — anaphoric "gone"
  triple plus reveal; the three deletions are real, the drumbeat is the
  lane's default; severity: tell; suggest "The fix deletes the `finished`
  channel, the `select`, and the worker count; the loop is a `for`."
- [307-309] «run the test a thousand times, run it under load, run it on
  the CI machine that is slower» — anaphoric triple; three real folklore
  practices, partly defended; severity: noted.
- [318-320] «That is not a criticism of the detectors — it is the reason
  wolf spent Part 2 removing the class detectors are for, and this
  chapter searching the class that is left» — textbook "not X — it is Y"
  reveal; severity: tell; suggest "Detectors are aimed at the class Part
  2 removed; this chapter searches the class that is left."
- [373-379] «The report is not lying — … A green run with an open
  frontier is a run that has not finished asking.» — negation opener plus
  personified aphorism closer; the conditional-verdict explanation is
  real teaching; severity: noted.
- [414-416] «**exploration proves ordering properties over the events it
  can see and permute.** Both halves are load-bearing, and neither is a
  disclaimer — the events it can see are every channel operation, every
  `select`, every acquisition, every spawn and join, and every proc
  lifecycle event» — bold-for-emphasis in running prose plus an "every"
  anaphora ×5; the enumeration is the actual event set, which defends the
  list; severity: noted; suggest unbolding.
- [451-456] «a brace that joins, a channel that carries ownership, a
  `select` that multiplexes, a `when` that cannot deadlock, a proc that
  is a failure domain, two primitives under every supervision policy, and
  a seed that makes any of it repeat» — seven-item parallel inventory as
  part-closer; a recap with real referents, but the parallel-relative-
  clause cadence is template rhythm; severity: noted.
- [466-469] «What none of it does is make a concurrent program correct
  for you. It makes the mistakes *sayable*: the leak became a deadlock
  with a roster, the race became a compile error, the ordering bug became
  a seed. A program can still be wrong. It can no longer be wrong in a
  way nobody can hold.» — closing peroration stacking fronted negation,
  a "became" triple, and a negation-antithesis final beat; each element
  is defensible alone, the stack is the tell; severity: tell; suggest
  keeping the triple (its referents are the part's three chapters) and
  flattening the frame: "None of it makes a concurrent program correct.
  It makes the mistakes sayable: … A program can still be wrong, but not
  in a way nobody can hold."
