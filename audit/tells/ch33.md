# ch33 — tells catalog and register dispositions

Chapter 33 was written at bs30 (2026-09-07), after the bs16 fan-out,
and had not been through the register. This catalog records the
register applied to it in one pass (2026-09-08).

## Densities (standard metric: running prose only, fences and ledger
comments stripped, template dashes excluded)

| | before | after |
|---|---|---|
| prose words | 2,595 | 2,538 |
| running em-dashes | 20 (7.7/1000) | 2 (0.8/1000) |
| «rather than» | 5 | 0 |
| «worth V-ing» | 3 | 0 |
| negation family (`, not` / reveal mold) | 6 | 6 (all comma-form content contrasts) |
| section-close maxims | 4 | 1 |

## Dispositions

**R2 (dash budget), 18 fixed.** Hinge dashes became colons where they
introduced an expansion (§33.2 "without blocking: a door…", §33.3
"whatever the wait names: the door…", §33.4 "children it spawns:
`os_spawn_with`…"). Dash pairs became parentheses (§33.1 loopback,
§33.2 the `0`-deadline aside, §33.3 the compose aside, §33.4 the
exercise's empty-list aside). Two survive, both hinge dashes where the
interruption is the sentence's turn: §33.4 «answers 2 — where counting
the lines of `/proc/cpuinfo`…» and «means something else — it lets a
second process…». Kept-with-defense.

**R7 (dash-pair list), 2 fixed.** §33.1's `net_read` row list and
§33.3's two-budget list moved into parentheses.

**R5 («worth V-ing»), 3 fixed, no survivors.** §33.3 «It is worth
saying what this costs» → «The cost is the point». §33.4 «it is worth
arming even when» → the imperative «Arm it even when». §33.4 «it is
worth telling as history because the history is short» → «and the
history is short».

**R5 extension (ration), 5 → 0.** Every «rather than» was a semantic
contrast and became comma form («more processes, not a bigger loop»;
«the state of the socket, not the history of your looking») or a plain
verb («instead of parking forever»; «decline with the reason named»).

**R3 (negation family).** Six comma-form contrasts stay as content
(«an answer, not a failure»; «a registration, not a task»; «refused and
not a coarser word»; «Readiness is not exclusivity»). The reveal
survivor is §33.4's «Readiness is not exclusivity. One hand takes the
connection and the others find nothing there.» — the section's
teaching. «It is `os_cpus() -> int ! {io}` and never a silent 1» was
restated positively with the row's reason first. «A lost race is not a
new error row: it carries nothing» → «A lost race carries no error row
of its own».

**R8 (maxim closers), 3 cut, 1 kept.** Cut: §33.3's «The machine does
the waiting, and it is better at it than a loop is» (the section now
ends on the measured pass counts); §33.4's «and that is the whole
rule» (self-ranking appendage); §33.4's «What the change buys is the
server nobody is talking to» → plain form «The change buys the server
nobody is talking to» (kept as the paragraph's fact, no longer an
epigram turn). Kept: §33.2's «Readiness is the invitation; the read is
the news.» — the checkable relation the fourth `net_wait` line
demonstrates.

**R9 (schedule narration).** «Until wolf 0.2.6 …» is real history with
a version, which TONE's tense rule permits. Kept. No sprint, pin or
landing is named in reader-facing prose.

**R10.** No non-blessed bold; no D/X citations in prose. Clause
anchors (`[os.net.wait]`, `[os.cpus]`, `[os.net.accept]`,
`[os.proc.inherit]`, `[os.net.listen.opts]`) are the spec's own
apparatus, the book-wide convention, and stay.

**Untouched.** All six code blocks, the console replay, the exercise
stems' program text, and the audit ledger.

## Gates

`cargo xtask verify-docs` exit 0 after the edit. `cargo xtask samples`
run over the whole book after the edit (result recorded in the commit
that lands this file).
