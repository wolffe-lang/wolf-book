# bs14 tells audit — ch22 (Modules: the shape of a wolf project)

**Counts:** charter 0 · tell 6 · noted 7
**Em-dashes:** 20 in 1831 prose words = 10.9/1000w — the lane's lowest
full-chapter density. 8 of the 20 are the exercise-header template; 12 in
running prose.
**Negation family:** 16 hard instances — the lane's highest per word.
"rather than" ×2. The fronted negation triple ("Not a file, not a
declaration, not a line…") is the chapter's default structural move.
**Banned vocabulary:** none.
**Verdict:** the em-dash budget is nearly held and much of the prose is
terse and in register; the distance is concentrated in one habit —
negation anaphora as the way every definition and every absence is
stated, four times in §22.1 alone.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (R1–R3, R5, R7–R8, R10–R11 checked; R9:
one rhetorical «today» dropped, below); line references are to the
bs14 text.

**Em-dashes:** 0 running in 1757 prose words = 0.0/1000w (was 12
running in 1831 = 6.6/1000w, the lane's lowest; template 8 → 0 by
R1). No site met the all-four-substitutions-misread bar.
**Negation family:** 16 hard (the lane's highest per word) → 8; the
§22.1 anaphora ×4 → 1 (the opener keeps its emphasis per this
catalog's own suggestion); the reveal mold → 1 survivor («The
ordering question was deleted, not answered», the section's design
point, in comma form).
**"rather than":** ×2, unchanged (inside the ration).
**Tells:** 6 → 0. All fourteen rows dispositioned below.
**Word count:** 1831 → 1757.
**Reality-check (R9):** «happens today to hold one file» loses
«today» (rhetorical, but the register reserves the word); no
deferral/schedule prose otherwise — the §22.1/§22.2 lupin-only
transcript posture is the ledger's open E0301 row, not the page's.

- [9-10, 62-63, 83-84, 92-93] «Not a file, not a declaration, not a line
  at the top of the file» / «There is no path, no extension, and no list
  of files to keep in step with the disk» / «Nothing else changes. Not
  the entry file, not the import, not `mean`» / «File boundaries create
  no scopes, no namespaces, and no import edges» — one finding: four
  negation triples in §22.1, the definition-by-what-it-is-not reflex;
  each list's items are real, the drumbeat is the pattern; severity:
  tell; suggest keeping the opener (it earns the emphasis) and flattening
  the rest ("The import names the directory; the module is the folder").
  **bs16: fixed-by-R3 (three of four)** — [9-10] kept (the opener
  earns it); [62-63] → two items and a colon; [83-84] → «The entry
  file, the import, and `mean` stand as they were…»; [92-93] → «File
  boundaries create no scopes and no import edges».
- [66-70] «`total` does not and is not. … a seam that is real internally
  and not a promise to strangers» — negation pair (counted); the first is
  terse and good, the second is a flourish; severity: noted.
  **bs16: fixed-by-R8** — the flourish clause cut; the `pub(pkg)`
  sentence ends on its factual content.
- [128-133] «It does not say the name does not exist. … The difference
  between those two messages is ten minutes of your life, once per
  occurrence, forever.» — negation opener plus an escalating adverbial
  triple closer; the diagnostic-quality point is real teaching; severity:
  noted; suggest ending at "ten minutes of your life."
  **bs16: fixed-by-R8** — suggestion adopted; the negation opener
  stays (real teaching).
- [136] «An import you do not use is not a warning:» — double negation as
  pivot (counted); immediately cashed by the transcript, and "A warning
  would be a request." [145] closes it in register; severity: noted;
  keep.
  **bs16: kept-with-defense** — unchanged.
- [200-202] «That is how real cycles are born — not from carelessness but
  from two modules each needing one true thing from the other.» — "not X
  but Y" with em-dash; partly defended (it corrects the real misreading
  that cycles mean sloppiness); severity: tell (cluster member); suggest
  "Real cycles are born from two modules each needing one true thing
  from the other."
  **bs16: fixed-by-R3** — adopted as «Neither import is silly, and
  that is how real cycles are born: two modules each needing one true
  thing from the other.»
- [211-212] «Only one of those is shared vocabulary, and it is not a
  service — it is a fact about rows.» — textbook "not X — it is Y"
  reveal; severity: tell; suggest "Only one of those is shared
  vocabulary: a fact about rows, not a service."
  **bs16: fixed-by-R3** — suggested sentence adopted verbatim.
- [254-272] «Three things the rule buys, in the order you feel them.
  **Builds.** … **Comprehension.** … **Interfaces.** … There is a
  fourth, and it is mechanical.» — the bold-led enumerated-benefits
  template (ch15's four decisions, ch18's three refusal-buys); the
  content under each head is real; severity: noted; see LANE-B systemic
  row.
  **bs16: kept-by-R10/R11 (layout)** — run-in bold leads stay; the
  count opener is already a plain sentence.
- [262-264] «the forty files you would have to touch to break it were
  already one file wearing forty names» — the page's wry clause; lands on
  the design, true claim; severity: noted; keep.
  **bs16: kept-with-defense** — unchanged.
- [288-294] «Splitting a file does not move it. Renaming a private helper
  does not move it. … a hash that did not move is the difference between
  a patch and a lie.» — anaphoric negation pair plus the "difference
  between X and Y" epigram (third use in the lane: ch18 [257-259], ch21
  [123]); the hash claim is checkable and the closer earns its edge;
  severity: noted.
  **bs16: kept-with-defense** — the pair and the closer stay; the
  paragraph's hinge dash goes to a colon under R2.
- [311-314] «Wolf has no code that runs before `main`. No static
  initializers, no module-level constructors, no registration hook the
  linker calls in whatever order it assembled the objects.» — fronted
  no-triple opening §22.3 (the §22.1 reflex again); the closing line "A
  program's first act is its first line" is in register; severity: tell;
  suggest letting that closer do the work and cutting the middle list to
  prose.
  **bs16: fixed-by-R3** — the anaphora folds to one plain negative
  («…the static initializers, module-level constructors, and
  linker-ordered registration hooks of other languages do not
  exist»); the closer does the work.
- [323] «Wolf's replacement is not a hook. It is a value:» — negation
  family, textbook "not X. It is Y"; severity: tell; suggest "Wolf's
  replacement is a value:"
  **bs16: fixed-by-R3** — suggested sentence adopted verbatim.
- [355-357] «does not build it, does not lock anything to build it, and
  cannot observe a moment when it was half-built» — negation triple
  (counted with the chapter cluster); the three absences are the
  section's actual guarantees; severity: noted.
  **bs16: kept-with-defense** — the guarantees stay; the trailing
  dash goes to a colon under R2.
- [360-363] «It was not answered. It was deleted. There is no "before
  main" in which two registrations race, no link order to depend on, no
  initialization sequence to document» — "not X. It was Y" reveal
  followed immediately by a no-triple; the reveal is earned (deletion vs
  answer is the design point) but the stack is the lane's signature;
  severity: tell; suggest "The ordering question was deleted, not
  answered: there is no before-main for registrations to race in."
  **bs16: fixed-by-R3** — suggestion adopted («The ordering question
  was deleted, not answered.» — the chapter's one reveal survivor, in
  comma form); the stage-direction opener («Now look at what
  happened…») cut under R11, and the no-triple thins to two items.

## bs16 — register fixes with no bs14 row

- **R1** — all eight exercise-header separators to a period.
- **R2** — unrowed hinge dashes: «privacy boundary — `total` is
  private» → colon; 22-2's «the diagnostic — including… —» →
  parentheses (R7); 22-5's «validate them — each import» →
  semicolon; «no such moment — the table was assembled» → colon;
  «Everything else — the table, the dispatch… —» → parentheses (R7).
- Zero edits inside code, console, or diagnostic blocks; the ledger
  is untouched; no fence changes.
