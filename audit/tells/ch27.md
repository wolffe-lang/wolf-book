# ch27 — tells audit (bs14, lane C)

**Counts:** charter 0 · tell 3 · noted 7
**Em-dashes (prose only):** 31 in 2174 prose words = 14.3/1000. Eight are
exercise-stem template dashes; ~23 are running-prose dashes, mostly paired
asides. The density matches ch23/24/26 almost exactly — a lane-wide
constant, see the whole-file row.
**Negation-family count:** 6 (L106, L250, L253–254, L287–290 ×2, L470–471).
**Banned vocabulary:** none.
**Verdict:** The error-path argument is the most K&R material in the part —
measured, sourced, no adjectives — and what separates the chapter from the
register is the lane's standard em-dash density (14.3/1000) plus a taste
for polished antithesis at paragraph close ("a decision, not an accident",
"identifiable rather than merely unwelcome").

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (ratified 2026-08-28, three amendments);
line references below are to the bs14 text.

**Em-dashes:** 0 running in 2039 prose words = 0.0/1000w (was ~23
running in 2174 = 10.6/1000w standardized; template 8 → 0 by R1). Every
running dash substituted under R2's ordered hierarchy; none met the
all-four-misread bar.
**Negation family:** 6 → content survivors only ([106–107] Ritchie
correction kept per defense; [470–471] spelling/program contrast kept
per defense; "Refused"-class scope facts untouched). No reveal-mold
instance remains.
**"rather than":** 3 prose instances → 0 (L359 catalog suggestion
adopted; L395's tomorrow/today mirror simplified; the §27.4 char
paragraph rewritten — below). The R5 cap is a ceiling.
**Italics cluster [100, 262, 288]:** kept exactly one (*which token*,
L262) per the row's suggestion.
**Reality check (R9) — the §27.4 char paragraph:** the claim "the
interpreter at this printing's pin does not read `'` yet" is FALSE at
the current pins and retired. Measured on both machines (pin addcd7f /
lupin 0.1.16): a char-arm `match` ('+'/'-'/'*' over `"+-*".chars()`)
prints 13 -1 42 at exit 0 under lupin AND `wolf run`. The paragraph
now states the byte dispatch as the program's consistency with its
byte-view tokenizer, no schedule; the four-arm re-cut (listing, wc
claim, two consoles) is recorded on the #162 ledger row as a
content-pass edit, not made here. The X3-SPREAD hole (list-element
arithmetic) still reproduces per bs17's ledger re-verify; §27.4's
cut-paragraph accounting and the "two lanes agree on rpn's own lines"
claim stay true as written. No fence-status changes.
**s125 class (ch27 ×4):** all four are ledger rows, re-verified with
probes by bs17 (NARROWS); no reader-prose kind-only claim exists in
this chapter. Nothing owed here.
**Word count:** 2174 → 2039.

## Findings

- [253–254] «That is a decision, not an accident» — negation family; third
  instance of the decision/accident/choice-not-limit formula in this lane
  (ch23 L241 «decisions rather than accidents», ch26 L509–510 «a choice
  this chapter made and not a limit it ran into»); severity: tell;
  suggestion: "That is deliberate, and §27.4 comes back to what it buys."
  **bs16: fixed-by-R5 (amendment)** — "That is deliberate"; the
  existing "§27.4 comes back to what it buys" tail already followed.
- [287–290] «Wolf does not need a flag beside the value because the
  failure *is* a value, in a row the signature spells out, and it does not
  need a second call to find out what went wrong because the tag brought
  the evidence.» — negation anaphora pair + emphasis italics; severity:
  tell; suggestion: "The failure is a value, in a row the signature spells
  out, and the tag brought the evidence."
  **bs16: fixed-by-R3** — suggestion adopted, joined to the previous
  sentence with a colon; the emphasis italics on "is" go with it.
- [whole file] 31 prose em-dashes at 14.3/1000 — over a K&R budget, and
  within 0.6/1000 of ch23, ch24, and ch26: the reflex is lane-wide, not
  this chapter's; severity: tell; suggestion: prune paired asides where
  commas or parentheses serve (e.g. L196, L426 survive the swap unchanged).
  **bs16: fixed-by-R2/R7** — every running dash substituted (parens
  for pairs, colon/comma/period for hinges); 0 remain.
- [196] «Hold that number — 28 — for §27.3.» — stage-managed reader
  address (cousin of ch24 L253 «Sit with that line»), em-dash pair for an
  interpolated number; short and functional, so context half-defends;
  severity: noted; suggestion: "§27.3 comes back to that number."
  **bs16: fixed-by-R5** — suggestion adopted verbatim.
- [249–251] «it is not decoration, and there is no spelling that skips
  it» — emphasis-by-negation; severity: noted; suggestion: "the
  `else { return 1 }` is the empty case handled; no spelling skips it."
  **bs16: fixed-by-R3** — suggestion adopted.
- [106–107] «That is not a criticism of Ritchie. In 1978 there was no
  second thing to return.» — negation-led correction, but of a real
  misreading the page just invited, which is the case VOICE.md's rule
  allows; severity: noted; suggestion: none.
- [100, 262, 288]
  **bs16: kept-with-defense** — unchanged.
- [100, 262, 288] «The *difference* between "the answer is zero" and
  "there was no answer"» / «the useful thing to report is *which token*» /
  «the failure *is* a value» — italics-for-emphasis three times in one
  chapter's prose; each defensible alone, recorded as a cluster; severity:
  noted; suggestion: keep at most one.
  **bs16: fixed** — one survivor (*which token*, L262); the other two
  go in their rows' rewrites.
- [359–360] «arrives at the caller identifiable rather than merely
  unwelcome» — antithesis flourish at paragraph close; severity: noted;
  suggestion: "arrives at the caller with its token."
  **bs16: fixed-by-R5 (amendment) + R2** — suggestion adopted; the
  dash pair goes with it.
- [395–397] «a fifth failure tag added to `eval` tomorrow is a compile
  error here today rather than a silent fall through to a default» —
  polished tomorrow/today mirror + "rather than"; severity: noted;
  suggestion: "a fifth failure tag added to `eval` is a compile error
  here, immediately."
  **bs16: fixed** — suggestion adopted verbatim.
- [470–471] «the compiler's opinion about our spelling, not about our
  program» — negation family; the contrast is the technical point (what
  W0603 does and does not judge), so context defends; severity: noted;
  suggestion: none.
  **bs16: kept-with-defense** — unchanged.

## bs16 — register fixes with no bs14 row

- **R1** — all eight exercise-stem separators to periods; stem-body
  dashes in 27-3 (period), 27-5 (colon), 27-8 (comma).
- **R2/R7** — unrowed pairs and hinges: L9–10 (parens), L97 (comma),
  L247–248 (parens), L285 (colon), L298–299 (parens), L306 (colon),
  L413 (colon), L426 (comma + colon), L474 (colon), L490 (comma +
  colon).
- **R5** — L292 «worth a sentence» frame sentence deleted (the two
  paragraphs it announced stand on their own).
- **R9** — the §27.4 char-schedule paragraph retired (header above);
  ledger note added to the #162 char row with the two-machine probe.
- Zero edits inside code, console, or diagnostic blocks; ledger
  comments untouched except the one bs16 note.
