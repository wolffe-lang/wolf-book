# bs14 tells audit — ch21 (Beating C honestly)

**Counts:** charter 0 · tell 2 · noted 11
**Em-dashes:** 31 in 1732 prose words = 17.9/1000w — the lane maximum by
a wide margin. 6 of the 31 are the exercise-header template; 25 in
running prose, many of them interruptive appositives where parens or a
colon serve.
**Negation family:** 9 hard instances; "rather than" ×3.
**Banned vocabulary:** none.
**Verdict:** sentence-for-sentence the closest full chapter to the K&R
register in this lane (the arithmetic-as-argument sections land clean);
the distance is almost entirely the em-dash budget, spent at nearly
triple a K&R page.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (R1–R3, R5, R7–R8 checked; R9 scan
clean — the reserved-sections paragraph is scope prose, verified
against the bs17 records at the addcd7f/0.1.16 pins); line references
are to the bs14 text.

**Em-dashes:** 2 running in 1669 prose words = 1.2/1000w (was 25
running in 1732 = 14.4/1000w, the lane maximum; template 6 → 0 by
R1). The two survivors are §21.1's pair bracketing the lupin console
fence («…enforces the identical rule at its own moment — [console] —
which is the difference between "this could alias" and "this did"»):
the sentence is genuinely interrupted by the display transcript and
resumes after it; all four substitutes break the resumption — the
all-four-misread bar, met (ch16 §16.3's precedent). One pair on that
page, within the cap.
**Negation family:** 9 hard → 7; the reveal mold → 0; survivors are
the defended content rows ([38-40], [76-82]'s antithesis, [123],
[239], and comma-form contrasts).
**"rather than":** ×3 in prose → ×2 ([82] checked/promised, [343]
honesty/embarrassment; 21-9's promotional pair to comma-not form).
**Tells:** 2 → 0. All thirteen rows dispositioned below.
**Word count:** 1732 → 1669.
**The §21.4 shrink (bs17's flag, lane-B extra):** with the s125 site
line in the compiled transcript, the paragraph introducing the two
reports shrinks to one sentence — «The compiled report names the kind
and the site; the interpreter's names everything:» — exactly the
shrink the ledger's papercut row promised when the site landed. The
row itself stays open on its remainder (value/type/decision/clause
are lupin-only), unchanged.
**Reality-check (R9):** the closing reserved-sections paragraph makes
two present-tense absence claims (no `Soa[T]` spelling; no reader
bench instrument). Neither surface is named as landed in the bs17 pin
sprint's records (s125/is27/D60 only), and the ledger's HOLD carries
the rp02 re-measurements; the claims stand as scope statements. The
native lane is dark on this host, so no fresh native probe was run;
flagged in the lane report.

- [3-10] «Chapter 7 proved … Chapter 8 priced … Chapter 3 trapped … What
  none of those chapters did is put a stopwatch next to the proof» —
  anaphoric chapter-recap triple plus the fronted "What none of X did"
  frame (ch17 [466]'s shape); the recaps are real and cited; severity:
  noted.
  **bs16: fixed-by-R3 (frame only)** — «What none of those chapters
  did is put…» → «None of those chapters put…»; the recap triple
  stays (content).
- [33-34] «The interesting word is `restrict`, and what is interesting
  about it is what it does not do.» — repetition-plus-negation flourish;
  severity: noted; suggest "The interesting word is `restrict`, for what
  it does not do."
  **bs16: fixed-by-R5** — suggested sentence adopted verbatim.
- [38-40] «Nothing checks the promise. Call this function with
  overlapping pointers and the program is undefined — no diagnostic, no
  trap, whatever the optimizer's assumptions happened to compile to.» —
  "no X, no Y, whatever Z" cadence; defended: each item is a real fact
  about C UB; severity: noted.
  **bs16: kept-with-defense** — the items stay; the paragraph's two
  hinge dashes go to colons under R2.
- [76-82] «Both machines, both tiers, one answer — … Same information
  reaching the optimizer, different laborer: C trusts the programmer's
  word, wolf makes the caller show its work.» — the numeric-pair cadence
  (ch13's tic) plus a balanced antithesis; the contrast is the honest
  comparison TONE blesses; severity: noted.
  **bs16: kept-with-defense** — the antithesis stays; the dash goes
  to a colon under R2.
- [123] «which is the difference between "this could alias" and "this
  did."» — "the difference between X and Y" epigram (ch18 [257-259]'s
  shape); terse and true; severity: noted.
  **bs16: kept-with-defense** — unchanged; it closes the surviving
  fence-bracket sentence (see header).
- [124-126] «Whether the proved fact buys what the promised fact buys is
  not a question prose can settle; it is one of the questions the
  benchmark suite in §21.4 exists to keep answering» — negation family,
  textbook "not X; it is Y"; severity: tell; suggest "Prose cannot settle
  whether the proved fact buys what the promised fact buys; §21.4's suite
  exists to keep answering it."
  **bs16: fixed-by-R3** — suggestion adopted («Prose cannot settle
  whether the proved fact buys what the promised fact buys; the
  benchmark suite in §21.4 exists to keep answering it…»).
- [208-210] «bumps a pointer ten thousand times — §8.3 named the
  strategy: bump a pointer, free the lot — grows the list's buffer a
  handful of times» — em-dash pair interpolated mid-list, with a colon
  inside the interpolation; reflexive em-dash where parens serve;
  severity: noted.
  **bs16: fixed-by-R7** — parentheses, colon kept inside them.
- [219-222] «So the honest comparison is not "region versus malloc" — an
  arena wins that one in any language … It is "checked arena versus
  disciplined arena,"» — negation reveal with em-dash, the lane's
  signature shape; the correction is the section's real point, which
  half-defends it; severity: tell; suggest "The honest comparison is
  "checked arena versus disciplined arena": an arena wins "region versus
  malloc" in any language, which is why every allocator-heavy C codebase
  grows one."
  **bs16: fixed-by-R3** — suggestion adopted; the second sentence
  flattens to «The allocator math is identical, and the difference
  priced in this section is who catches the escapee.»
- [239] «X3, a decision, not a mood» — "X, not Y" epigram; the page's wry
  clause, three words, lands on the design; severity: noted; keep.
  **bs16: kept-with-defense** — kept as its own fragment sentence
  (the introducing dash goes to a period under R2).
- [261-263] «the release binary — the optimized one, the one a benchmark
  would time — refuses to look away» — em-dash pair plus escalating
  double appositive plus personification; each defensible, the stack is
  the pattern; severity: noted; suggest "the release binary, the one a
  benchmark would time, refuses to look away."
  **bs16: fixed-by-R2** — suggestion adopted (one appositive, comma
  form).
- [285-288] «after an optimizer has hoisted, folded, and
  branch-predicted its way through it … because the number exists, it is
  CI's, and it has a date» — two triples in one sentence-pair; the
  second's items are the chapter's actual rule; severity: noted.
  **bs16: kept-with-defense** — the triples stay; the hinge dash
  before the second goes to a period under R2.
- [327-330] «Neither exception is hidden in an average: the gate reads
  the exceptions file, counts it against the cap, and fails the night the
  file stops telling the truth.» — closing triple with personification;
  the mechanism described is real; severity: noted.
  **bs16: kept-with-defense** — unchanged.
- [362-363] «exactly the kind of number this chapter refuses to print
  once, by hand, hopefully.» — adverb triple as closer; wry, within the
  sass budget, and the honesty is the charter's; severity: noted; keep.
  **bs16: kept-with-defense** — unchanged.

## bs16 — register fixes with no bs14 row

- **R1** — all six exercise-header separators to a period.
- **R2** — unrowed hinge dashes: «10,000 into `free` — 20,000
  allocator interactions» → colon; «by convention — the rule that no
  node pointer…» → colon; «One pass's worth of work —» folds into the
  lead sentence's colon; «in every profile — X3, a decision, not a
  mood» → period (the epigram stands as its own fragment);
  «in-range — so they stay checked» → comma; «humbler reason — its
  string iterator» → colon; «nightly hold — the streak» → colon;
  «pays out — because the number exists» → period; «loss table —
  every kernel» → colon; the five-families pair → parentheses (R7);
  21-3's and 21-5's stem pairs → parentheses.
- **R2 survivors** — the §21.1 fence-bracketing pair (see header):
  kept, defended, counted.
- Zero edits inside code, console, c-run, or diagnostic fences; the
  HOLD comment and the ledger are untouched; no fence changes (the
  §21.4 transcript already carries the s125 site line from bs17).

## bs18 consistency pass (2026-08-30)

Every cited number re-verified against bench/ritual-ledger.jsonl,
bench-exceptions.md, bench/loss-ledger.md and the c24 closeout at the
bs18 pins. Three fixes, all drift, none register: `wrapping[i32]` →
`wrapping[int]` (the kernel's fields are `int`); "two million and
change additions" → "four thousand and change" (2000 + 2147
acc-additions precede the trapping one — the old number counted
nothing in the program); the §21.5 parenthetical pointing at the
retired bench-reader chapter 20 removed (the re-drawn ch20 is Perf
contracts). Chapter ledger carries the full verified-as-printed
list. Density untouched: the two defended §21.1 survivors stand;
no new dashes.
