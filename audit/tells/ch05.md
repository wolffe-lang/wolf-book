# ch05 — tells audit (bs14, lane A)

**Counts by severity:** charter 9 · tell 4 · noted 8 (21 rows)
**Em-dashes (prose only):** 39; 2503 prose words; **15.6 per 1000** — highest in the lane
**Negation-family:** 6 ([251], [396–397], [501–502], [540], [552–554], [653–654])
**Banned vocabulary:** none (verified by eye; "honest/honesty" ×4 is a motif tic, not on the banned list — counted below)
**Verdict:** Sentence-by-sentence the prose is near register, but the chapter narrates the toolchain's schedule nine times — the lane's worst tense-discipline breach — and closes sections on antithesis-plus-maxim.

## bs15 exemplar rewrite — recomputed header

Rewritten against REGISTER.md (rules R1, R2, R3, R6, R7, R8, R9, plus
the R5 connective ration); line references below are to the bs14 text.
Every R9 gap was re-probed at the pin (lupin 0.1.14 at a3591de, wolf at
a900b8c, built for this run) before its prose was rewritten; per-row
verdicts below.

**Em-dashes:** 1 in 2214 prose words, and that one is the compiler's
own note quoted verbatim in a code span (`a temporary has no home —
bind it first.`) — running prose 0.0/1000w (was 39 total in 2503 words
= 15.6/1000w by this header's original count; in the register's
standardized metric: 29 running + 9 template + 1 in-span = 11.6/1000w
running). Template 9 → 0 by R1. Every running dash substituted cleanly
under R2's hierarchy; no survivor met the all-four-substitutions-
misread bar. The budget (≤5/1000w) is a ceiling, not a quota.
**Negation family:** 6 rowed instances → 3, all catalog-defended
content contrasts ([501–502], [540] in comma form, [653–654]); the
one reveal mold ([552–554] «was a schedule. It is now a fact») 1 → 0;
the fronted negation in the machine box («What none of these
operations do») → plain («None of these operations copies»).
**Schedule narration (charter):** 9 → 0. Verified at the pin before
rewriting: `Set`, the combinators, and primitive method sugar are
still absent (scope statements now); the trait system HAS landed (the
history narration deleted, the material stays); container-`dyn` and
`==`/`<=>` trait dispatch are genuinely absent (sentences cut, two
ledger rows added). One healing found: the qualified `Text.text(n)`
on a primitive receiver now resolves under lupin 0.1.14 — recorded in
the wolf-run fence comment; the fence graduates in a lane with fence
authority (bs16).
**Tells:** charter 9 → 0, tell 4 → 0; noted rows with a defense kept.
**Connective ration:** "rather than" ×6 → ×2; "honest" ×4 → ×1 (the
survivor: §5.5's «definition-site honesty»); two "X, not Y" forms
minted by early drafts of the rewrite were removed again (the ration
amendment: the rewrite must not mint its own tell).
**Word count:** 2503 → 2214.

## Findings

### Charter (tense discipline — the chapter's dominant defect)

- [183-184] «That leaves `Set`, and here the book has to be honest: the pinned interpreter has not got one.» — deferral prose + pin apparatus ("the pinned interpreter" is banned from reader text by TONE.md §Tense verbatim); severity: charter; state scope instead: "Part 1 uses a `List` and a linear scan where a set belongs" — or the section waits for the surface.
  **bs15: fixed-by-R9** — verified at the pin: `Set[str]()` still answers `unsupported` (exit 4). The lede is now the scope statement «That leaves `Set`. Part 1's std has no set container:»; "the pinned interpreter" and "the book has to be honest" are gone.
- [186-209] «Exit 4 is the interpreter declining the program rather than running it, and the message blames the element type rather than the missing container.» — scaffold output taught: an `unsupported` refusal printed as a checked sample and explicated, which TONE.md forbids ("scaffold output is not product output"); severity: charter; drop the refusal sample and teach the List-scan idiom without the absence on stage.
  **bs15: prose fixed; fence flagged** — the message-critique sentence («blames the element type rather than the missing container») moved off stage (its ledger row already carries it); the List-scan idiom is taught plainly. The `run(exit=4)` fence itself is outside this lane's write authority (prose-only rule; failure sets must be byte-stable) — retiring the refusal samples is an open call for bs16, flagged in EXEMPLAR-REVIEW.md.
- [251-252] «None of the combinators exist yet — not `sorted_by`, not `take`, not `map`, `filter`, or `sum`.» — deferral prose ("exist yet") + negation anaphora; severity: charter; state the subset as scope: "Part 1's std has one combinator, `count()`; this section builds the loop the chain desugars to."
  **bs15: fixed-by-R9** — verified at the pin: `sorted_by` still `unsupported`. Catalog's suggested scope form adopted («Part 1's std has one combinator, `count()`, and chapter 2 used it»); the negation anaphora goes with the deferral.
- [258-261] «So the section's promise arrives in the honest order. Instead of writing the chain and then desugaring it for understanding, we write the desugaring because it runs» — workaround narrated as concession; fails TONE.md's survive-the-landing test ("because it runs" dates the passage); severity: charter; teach loop-then-chain as pedagogy: "First the loop, because the loop is what the chain means."
  **bs15: fixed-by-R9** — catalog's pedagogy adopted: «So we write the loop, because the loop is what the chain means, and we keep the chain on the page as the thing it is a translation of». Survives the landing: the teaching order stands the day the combinators do.
- [419-424] «When this chapter was first true, there were no traits — the `fn(T, T) -> bool` parameter stood in for a comparison trait that had not landed» — edition/schedule narration ("first true", "had not landed", "has landed since"); severity: charter; keep only the pedagogy: "The function parameter stays: what `best` may do with a `T` is what you hand it."
  **bs15: fixed-by-R9** — the feature landed (the trait fences in §5.5 run at the pin), so the edition narration is simply deleted and the pedagogy stated in the present: «The `fn(T, T) -> bool` parameter is a teaching choice; a trait could name the comparison, and §5.5 builds exactly that kind of capability.»
- [552-554] «Section 5.3 promised a comparison trait "that lands with the trait system," and for several printings that sentence was a schedule. It is now a fact.» — printing/schedule meta-narration + "was X. It is now Y." cadence; severity: charter; open on the material: "A trait names a capability; an `impl` says a type has it."
  **bs15: fixed-by-R9** — catalog's opener adopted; the printings meta-narration and the «was X. It is now Y» reveal deleted. The heading «5.5 Traits: the system lands» (schedule in a heading) is now «5.5 Traits»; EXERCISES.md's echo of the old heading is outside bs15 write scope and rides into bs16 (flagged).
- [598-600] «The method sugar `n.text()` does not reach primitives yet; the compiler refuses it by name, and this book teaches the spelling that works today rather than the one scheduled to.» — deferral prose ("yet", "today", "scheduled to"); severity: charter; teach the qualified form as the form: "On a primitive the call spells the trait's name: `Text.text(n)`."
  **bs15: fixed-by-R9** — verified at the pin: the sugar still refuses on both machines (lupin «`i64` has no method `text`», wolf «cannot compile this yet»), so the scope form is honest: «The method sugar `n.text()` does not reach a primitive receiver; the compiler refuses it by name.» Healing found alongside: the QUALIFIED call on a primitive receiver now resolves under lupin 0.1.14 (prints `int(7)`); recorded in the wolf-run fence comment for bs16's fence-graduation pass.
- [675-677] «A `dyn` crosses a call today; carrying one inside a container is the compiler being honest about its schedule again, and the refusal names itself.» — deferral prose + honest-motif; severity: charter; if container-`dyn` is D-numbered scope, state it as scope; otherwise the sentence waits.
  **bs15: fixed-by-R9 (sentence waits)** — verified at the pin: `List[dyn Draw]` is E0201 at the `dyn` on both machines, a grammar refusal; no D-number names container-`dyn` as scope, so per the catalog's own test the sentence is deleted and the gap filed as a new ledger row.
- [679-683] «`==` and `<=>` do not dispatch through `Eq` or `Ord` at this printing. The design that bridges them is decided; the code that does it has not landed. Until it does, comparison traits are called the way every trait here is called — by name.» — the full deferral family in three sentences ("at this printing", "has not landed", "Until it does"); severity: charter; cut to fact: "Comparison operators do not dispatch through traits; comparison traits are called by name."
  **bs15: fixed-by-R9** — verified at the pin: genuinely absent (lupin's struct `==` is structural — an inverting `impl Eq` is never consulted; `<=>` on a struct is `unsupported`; wolf refuses «`==` on non-primitive types (operator traits)»). Cut to the catalog's fact; the schedule sentences retired to a new ledger row.

### Tell

- [91-99] «builds an empty one — the type in brackets, then the call.» … «every call arrives write-marked — `(mut cents).push(340)` is §3.1's "this call writes `cents`", in its natural habitat now — while `len`, `is_empty`, and `xs[i]` only read» — three em-dashes in one paragraph, two doing a comma's work (over the one-pair budget); severity: tell; first dash becomes a colon, the §3.1 aside goes in parens.
  **bs15: fixed-by-R2 + R6** — exactly as suggested: colon for the first dash, parentheses for the §3.1 aside.
- [312-320] «The chain states the shape of the result and lets the library pick the work; the loop states the work and leaves the reader to infer the shape. When both spell the same answer, prefer the one that says what you meant» — balanced-antithesis mirror capped by a maxim, the paragraph's third figure; severity: tell; keep the trade, drop one mirror: "The chain says what the result is; the loop says how to make it."
  **bs15: fixed-by-R8** — suggested short mirror adopted verbatim; the closing imperative («prefer the one that says what you meant, and reach for the loop when the work is the thing you are choosing») stays as instruction, not maxim. The paragraph's earlier hinge-dash goes to a comma under R2.
- [396-403] «in the file where `total` is written — not at the call, and not in a wall of text about `List[int]`.» plus «That is the pitch: your mistakes are caught where you made them.» — negation anaphora on an em-dash + self-announced pitch line; severity: tell; "The error is on line 5 of `total`, where the mistake is."
  **bs15: fixed-by-R3 + R2** — «not at the call» survives in comma form (definition-site vs call-site is the section's content; R3's test); the «wall of text» pile-on and the self-announced pitch line are deleted — the library sentence carries the claim. («where the mistake is» already appears two paragraphs up, so it is not repeated here.)
- [522-528] «the price of being explicit about absence, and being explicit about absence is the habit the rest of the book rewards — a missing key is an answer, and an answer belongs in the type system rather than in a convention about zero.» — anadiplosis ×2 + em-dash + closing maxim stacked in one sentence; severity: tell; "Two branches where one lookup would do is the price of being explicit, and a missing key is an answer with a type."
  **bs15: fixed-by-R2 + R8** — suggested sentence adopted (with «about absence» kept once); anadiplosis, dash, and closing maxim gone.

### Noted (density record)

- [65-68] «Neither answers the other's question well, which is the honest version of choosing a data structure.» — section-lede maxim + honest-motif; severity: noted; ending at "well." loses nothing.
  **bs15: fixed-by-R8** — ends at «well.»; the honest-motif instance leaves with it.
- [178-181] «print different types on adjacent lines — the distinction chapter 1 met with `"wolf".len`, holding at every container in the language» — reflexive em-dash + trailing participial appositive where a period serves; severity: noted; split at the dash.
  **bs15: fixed-by-R2** — split at the dash: «…on adjacent lines. Chapter 1 met the same distinction with `"wolf".len`, and it holds at every container in the language.»
- [413-414] «The cost is real and somebody pays it: it is paid once, in the compiler's grammar–semantics seam, instead of forever, in user code.» — cost-ledger motif + once/forever antithesis (recurs lane-wide); severity: noted; "The cost is paid once, in the compiler, instead of at every call site."
  **bs15: fixed (suggestion adopted)** — «The cost is paid once, in the compiler's grammar–semantics seam, instead of at every call site.» The cost-ledger frame sentence and once/forever antithesis go.
- [501-503] «`()` is what a lookup yields for a key that is not there — not a trap, not a zero, and not a refusal.» — negation triple as rhythm; context defense: TONE.md §3.4 models exactly this figure ("does not wrap, does not continue, does not negotiate") — the tick is lane-wide recurrence, not this instance; severity: noted.
  **bs15: kept-with-defense** — the triple stays (TONE §3.4's own figure); only its lead-in dash becomes a colon under R2.
- [539-540] «the honest objection to it is the number of comparisons, not the copying» — honest-motif + "X, not Y"; severity: noted; "the objection is the comparisons, not the copying."
  **bs15: fixed (honest budget)** — «the objection to it», the «X, not Y» contrast kept (content); the box's fronted negation also goes plain under R3 («None of these operations copies the elements»).
- [653-654] «That is a rule, not a style.» — "X, not Y", isolated and short; context defends it (the distinction is the paragraph's point); severity: noted.
  **bs15: kept-with-defense** — unchanged.
- [687] «what has to change, and what — pleasantly — does not?» — editorializing dash-aside inside an exercise stem; severity: noted; "what has to change, and what does not?"
  **bs15: fixed-by-R2** — suggestion adopted; the editorializing aside deleted.
- [67, 258, 539, 676] «honest» ×4 in one chapter ("the honest version", "the honest order", "the honest objection", "being honest about its schedule") — motif worn to a tic; severity: noted; budget one per chapter.
  **bs15: fixed (budgeted to one)** — all four leave with their rows' fixes; the chapter's one surviving «honest» is §5.5's «definition-site honesty», which names the mechanism it teaches.

## bs15 — register fixes with no bs14 row

The bs14 catalog rowed findings, not templates; these edits come from
the register's rules and complete the diff's accounting:

- **R1** — all nine exercise-header separators: «*(kind · tool)* —» →
  «*(kind · tool)*.»
- **R2** — unrowed hinge-dashes: the chapter opener's «— one of the two
  containers…» → parentheses; §5.2's «— and for a top-500» → comma;
  §5.4's «— `cents[0 - 1]` traps too» → colon; «Ask `is_empty` first —
  or,» → period.
- **R7** — unrowed dash-pair lists: exercise 5-6's «— one of lines
  seen, one of counts —» → parentheses; §5.3's two-spelling pair →
  parentheses; §5.5's «— a *default* —» → parentheses; «— deciding *at
  run time* —» → commas; «— a binding, a field, an index» →
  parentheses.
- **R6** — «which is a deliberate economy and §5.3's subject» → own
  sentence («The economy is deliberate, and §5.3 returns to it»);
  «which is chapter 6» → «(chapter 6's subject)». Woven references
  («the linear scan §5.1 will have a word about», «the way §4.4's
  returns do», «§5.3 taught for generics») stay, per R6.
- **R5 ration** — «rather than» ×6 → ×2 (kept: the Exit-4 sentence,
  exercise 5-10's stem); «X, not Y» held at its pre-rewrite count —
  two instances the rewrite's early drafts minted («not a stopgap»,
  «not a requirement») were removed again.
- **§5.2 opener** — «Here is what the top category ought to look
  like. It is the spelling…» → «Here is the top category in the
  spelling the language is designed for…» (the «ought» leaned on the
  schedule; the design claim is the scope).
- **Ledger** — two rows added (container-`dyn`; `Eq`/`Ord` operator
  dispatch), and the wolf-run fence comment gains the bs15 re-verify
  (qualified call on a primitive receiver HEALED under lupin 0.1.14).
- Zero edits inside code, console, REPL, or diagnostic blocks; the
  sample failure set is byte-identical before/after (34 environmental
  failures on this aarch64 host; ch05 contributes exactly one, its
  wolf-run fence, which cannot execute off linux/x86-64 — every
  ch05 lupin-lane sample passes).

## Systemic (for LANE-A.md)

1. **Schedule narration**: nine charter rows, all one pattern — prose that teaches the gap between the book and the toolchain ("yet", "today", "at this printing", "has (not) landed", "the pinned interpreter"). One editorial policy fixes all nine.
2. **Section-close aphorism**: §5.1, §5.2, §5.4 all end on a maxim; see also ch06.
3. **Cost/price ledger motif** («the price of being explicit», «The cost is real and somebody pays it») and the **honest-motif** — book-brand phrases recurring at tic frequency.
