# bs14 tells audit — ch16 (Region transfer: fearless messaging)

**Counts:** charter 0 · tell 7 · noted 9
**Em-dashes:** 32 in 2293 prose words = 14.0/1000w, tied with ch15 for the
lane's densest. 10 of the 32 are the exercise-header template; 22 in
running prose.
**Negation family:** 12 hard instances; "rather than" ×5. The anaphoric
"no X, no Y, no Z" triple appears twice; the "not X; it is Y" reveal
closes the comparison section.
**Banned vocabulary:** none ("unlock" at [319] is a literal mutex unlock —
technical, defended).
**Verdict:** charter and vocabulary clean; the distance is the same two
lane rhythms at their highest em-dash density yet, with the "worth V-ing"
marker still running.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (R1–R3, R5–R8, R11 checked; R9: one
schedule-flavored clause retired, below); line references are to the
bs14 text.

**Em-dashes:** 2 running in 2217 prose words = 0.9/1000w (was 22
running in 2293 = 10.0/1000w; template 10 → 0 by R1). The two
survivors are §16.3's pair bracketing the second Rust fence («…drop
the `Mutex` when nothing writes — [code] — which is the *same
program*…»): the sentence is genuinely interrupted by the display
code and resumes after it, and all four substitutes break the
resumption — the R2 all-four-misread bar, met. One dash-pair on that
page, within the one-per-page cap.
**Negation family:** 12 hard → 8; the reveal mold → 0; survivors are
the catalog-defended content rows ([74-75] in colon form, [83],
[88-90], [186], [243-244] in colon form, [360], [391], and the
comma-form contrasts).
**"rather than":** ×5 → ×2 ([103] proc/task, [295] bug-class/compile-
error), inside the R5 ration.
**"worth V-ing":** 2 → 0.
**Tells:** 7 → 0. All sixteen rows dispositioned below.
**Word count:** 2293 → 2217.
**Reality-check (R9):** §16.3's «That trade is decided … rather than
pending» taught a schedule by contrast; «pending» dropped — the
sentence now states the decision («That trade is decided (v1 procs
are in-process)») and nothing else. No other deferral prose found.

- [105, 283] «That is worth pausing on» / «What the copy buys is worth
  stating plainly» — one finding: the "worth V-ing" significance marker,
  ch14's tic, twice more; severity: tell; suggest cutting the marker and
  stating the claim ("The substitution cost nothing, and that is the
  claim the whole part has been building toward" already carries it).
  **bs16: fixed-by-R5** — both deleted: [105] «That is the claim the
  whole part has been building toward»; [283] «The copy buys
  something no `move` can: …».
- [9-10] «Here is the ring from §8.4 — three documents, each pointing at
  the one after it and the one before it, the ends met — handed to a proc
  that never allocated any of it» — em-dash pair as opening parenthetical
  (same shape as ch12 [3-5]); severity: noted; a colon after §8.4 serves.
  **bs16: fixed-by-R7** — parentheses (the sentence already ends on a
  colon into the fence, so the suggested colon would double).
- [74-75] «The region changes owner. Not the documents — the region, one
  value» — fronted-negation corrective with em-dash; context defends: the
  document-vs-region distinction is the section's teaching; severity:
  noted; keep.
  **bs16: kept-with-defense** — the corrective stays; only the dash
  goes to a colon under R2.
- [77-79] «There is no traversal to serialize the graph, no pass to fix
  up pointers on the other side, and no step whose cost depends on how
  many documents the shelf holds» — anaphoric "no X, no Y, no Z" triple
  (the lane's recurring shape); the three absences are real costs;
  severity: tell; suggest two items or one ("No traversal serializes the
  graph and no pass fixes up pointers; the cost does not depend on the
  document count").
  **bs16: fixed-by-R3** — suggested two-item form adopted.
- [83] «The safety argument is equally short, and it is not new.» —
  negation opener (counted), echo of ch14 [44]; here it does
  chapter-lineage work (§8.3, ch7); severity: noted.
  **bs16: kept-with-defense** — lineage; unchanged.
- [88-90] «"who else is touching this?" is not a question the program can
  ask, because it has no way to spell a second reacher» — negation
  family; defended: unsayability is the checkable claim (ch12 [547-548]'s
  sibling); severity: noted; keep.
  **bs16: kept-with-defense** — unchanged.
- [115-116] «The data path does not change, the channel does not change,
  and the payload rule does not change.» — anaphoric negation triple as
  section closer; severity: tell; suggest "Nothing about the data path,
  the channel, or the payload rule changes."
  **bs16: fixed-by-R3** — suggested sentence adopted verbatim.
- [131-133] «is not a workaround; it is the same act as choosing which
  variables are locals of which function» — negation family, textbook
  "not X; it is Y"; severity: tell; suggest "Building the thing you send
  in its own window is the same act as choosing which variables are
  locals of which function."
  **bs16: fixed-by-R3** — suggestion adopted (the «not a workaround»
  clause drops; the positive claim carries).
- [186] «it is one you met in chapter 7, not a new one» — "X, not Y" in
  an exercise stem; mild, corrective; severity: noted.
  **bs16: kept-with-defense** — content; the stem's dash goes to a
  semicolon under R2.
- [243-244] «`freeze` does not mean *forever* — it means immutable for
  the life of that region» — negation reveal with em-dash; defended: the
  row exists to correct a real misreading ("the one people miss");
  severity: noted; keep.
  **bs16: kept-with-defense** — the corrective stays; the dash goes
  to a colon under R2.
- [251-252] «The fourth row is not a gap. Two writers to one datum is a
  design with no owner for the datum» — negation opener, the chapter's
  fourth "not X" pivot; the corrective is real but the shape is the
  default move by now; severity: tell; suggest "The fourth row is a
  redesign: two writers to one datum is a design with no owner for the
  datum."
  **bs16: fixed-by-R3** — suggestion adopted in period form («The
  fourth row is a redesign. Two writers…»).
- [281-282] «the standard remedy — a binary large enough to be
  reference-counted instead of copied — is a special case in the runtime»
  — em-dash pair where parens serve; severity: noted.
  **bs16: fixed-by-R7** — parentheses; the tail's "rather than a
  general answer" → "not a general answer" (R5 ration).
- [291-292] «can also tear — Go's memory model documents this — so the
  failure mode is not only stale data but data that never existed» —
  em-dash pair plus "not only X but Y"; severity: tell; suggest parens
  for the citation and "the failure mode includes data that never
  existed."
  **bs16: fixed-by-R7 + R3** — parens for the citation; «includes
  data that never existed, not only stale data» (the stale-data half
  is content and stays, in comma form).
- [356-358] «the honest summary of the cyclic case is not that wolf can
  do something Rust cannot; it is that both do the same thing, and one of
  them checks it» — negation family, textbook "not X; it is Y" (same
  shape as ch13 [334-338]); severity: tell; suggest "the honest summary
  of the cyclic case: both do the same thing, and one of them checks it."
  **bs16: fixed-by-R3** — suggestion adopted as a full sentence («is
  that both do the same thing, and one of them checks it»).
- [360] «Where wolf pays and the others do not: distribution.» —
  antithesis pivot (counted); defended: the concession is the section's
  honesty and the colon form is already terse; severity: noted; keep.
  **bs16: kept-with-defense** — unchanged; the paragraph's tail loses
  only «rather than pending» (see the header's reality-check note).
- [391] «a channel of bare `List[int]` — not `Copy`, not `imm`, not a
  region, not `sync`» — negation quadruple in an exercise stem; defended:
  it enumerates the four admitted payload classes the question asks
  about; severity: noted; keep.
  **bs16: kept-with-defense** — the quadruple stays (it enumerates
  the payload classes); the stem's dash goes to parentheses under R7.

## bs16 — register fixes with no bs14 row

- **R1** — all ten exercise-header separators to a period (16-3 and
  16-8 carry nested parentheticals; same decision).
- **R2** — unrowed hinge dashes: «the trap chapter 12 printed —
  `use-after-move`» → colon; «meaningless without the region — a
  handle…» → colon; «open them explicitly — §8.6's two open windows»
  → colon; «almost always wrong — the first two rungs» → colon;
  «§8.5's cadence doing concurrency work — `freeze` was
  irreversible…» → colon; 16-1's «— two pushes —» → parentheses (R7);
  16-6's workload pair → parentheses (R7); 16-6's «than the others —
  which?» → colon; 16-8's «which cannot — and why» → comma.
- **R2 survivors** — the §16.3 fence-bracketing pair (see header):
  kept, defended, counted.
- Zero edits inside code, console, or the Rust fences; the ledger is
  untouched; no fence changes.
