# bs14 tells audit — ch15 (Link, monitor, supervision)

**Counts:** charter 0 · tell 7 · noted 9
**Em-dashes:** 29 in 2070 prose words = 14.0/1000w — the densest chapter
so far. 9 of the 29 are the exercise-header template; 20 in running prose.
**Negation family:** 10 hard instances; "rather than" ×3. The "not X; it
is Y" reveal closes both §15.1 and §15.3.
**Banned vocabulary:** none.
**Verdict:** vocabulary and charter clean; the distance from K&R is the
lane's two rhythms plus a new one — the balanced-aphorism closer ("one
less X, one more Y", "cannot fail cannot be supervised") used as section
punctuation.

## bs15 exemplar rewrite — recomputed header

Rewritten against REGISTER.md (rules R1–R3, R5, R7, R8, R10, R11);
line references below are to the bs14 text.

**Em-dashes:** 0 in 1968 prose words = 0.0/1000w (was 29 in 2070 =
14.0/1000w; running prose 20 → 0, template 9 → 0 by R1). Every running
dash substituted cleanly under R2's hierarchy; no survivor met the
all-four-substitutions-misread bar. The R2 budget (≤5/1000w) is a
ceiling, not a quota.
**Negation family:** 10 hard instances → 6, and the reveal mold
("not X. It is Y") 4 → 0; survivors are the catalog-defended
content contrasts ([103-105], [115], [426], [442]) plus the two
catalog-suggested softened forms ("no error is delivered", "None of
these keeps a broken program running"). "rather than" ×3 → ×4 (one
added at [377] by this catalog's own suggested rewrite; flagged as an
open call in EXEMPLAR-REVIEW.md).
**Tells:** 7 → 0. All sixteen rows dispositioned below; noted rows
with a defense are kept unchanged.
**Word count:** 2070 → 1968.

- [110-111, 246-247, 422] «worth stating» / «worth predicting on paper» /
  «the one worth keeping» — one finding: the "worth V-ing" significance
  marker three more times (ch14's tic, still running); severity: tell;
  suggest deleting the marker and letting the sentence carry the claim.
  **bs15: fixed-by-R5** — all three deleted; [246] becomes the
  imperative («predict the off-by-one … on paper before you run it»),
  [110] folds into the box's opening sentence, [422] the frame sentence
  is cut and the paragraph opens on the claim.
- [57-58] «A link does not deliver an error to the linked proc — it
  *ends* it» — negation reveal with em-dash splice; the contrast is the
  section's actual teaching, which half-defends it; severity: tell
  (cluster member); suggest "A link ends the linked proc; no error is
  delivered."
  **bs15: fixed-by-R3** — suggested sentence adopted verbatim.
- [62-64] «the choice between the two is not a matter of taste, and it is
  not "link is a monitor with a shorter handler." It is a question you
  can answer» — double negation plus the "It is" reveal, textbook shape;
  severity: tell; suggest "The choice between the two answers one
  question about any pair of procs in your design:"
  **bs15: fixed-by-R3** — suggested sentence adopted verbatim; the
  «link is a monitor with a shorter handler» strawman drops here and
  survives where it earns its keep, as exercise 15-9's prompt.
- [66-67] «**Is this failure information, or is it fate?** If the
  survivor has something useful to do — retry, reroute, degrade, log and
  carry on — the failure is information» — bolded rhetorical question as
  section glue (the chapter's one, TONE allows it) plus a four-item list
  set off by an em-dash pair; severity: noted; suggest commas or a colon
  for the list.
  **bs15: fixed-by-R10 + R7** — the bold demoted to italics (the
  organizing-question precedent, TONE §3.6); the list moves to
  parentheses. The question itself stays: TONE's one-per-chapter
  rhetorical-question allowance covers it.
- [103-105] «The compressor that has no uploader is doing work nobody
  will receive; the uploader with no compressor has nothing to send.» —
  mirrored antithesis; context defends: the symmetry being illustrated is
  the primitive's actual symmetry; severity: noted; keep.
  **bs15: kept-with-defense** — R3's semantic-contrast test passes
  (the symmetry is `link`'s actual symmetry); unchanged.
- [115] «a link is not convertible into one» — negation (counted); inside
  the Erlang box, doing factual contrast work; severity: noted; keep.
  **bs15: kept-with-defense** — factual contrast with Erlang's
  trappable link; unchanged.
- [120-121] «One less pattern to get right, one more channel to declare.»
  — "less X, more Y" balanced epigram as box closer; severity: tell;
  suggest "The exit arrives on its own channel, which you declare."
  **bs15: fixed-by-R8** — the epigram goes; not the suggested sentence
  (it restates the box's previous clause) but the cost folded into it:
  «…without the service's other traffic interleaving; the cost is one
  more channel to declare.»
- [238-239] «The budget makes the supervisor able to fail, and a
  supervisor that cannot fail cannot be supervised.» — chiasmus aphorism;
  the claim is real and the sentence is the section's thesis, which
  defends it halfway; severity: noted; keep one such closer per chapter
  at most.
  **bs15: kept-with-defense** — R8's one balanced-closer allowance
  spent here: the chiasmus is the section's falsifiable thesis, and it
  is the chapter's only surviving aphorism closer.
- [252-253] «It holds no shard state, parses no queries, and touches no
  memory the shard touched» — anaphoric "no X, no Y, no Z" triple (same
  shape as ch14 [281-283]); the three absences are real; severity: tell;
  suggest "It holds no shard state and touches no memory the shard
  touched."
  **bs15: fixed-by-R3** — suggested double adopted; the trailing dash
  in the same sentence goes to a comma under R2.
- [269-270] «Deep trees look like careful engineering and behave like a
  telephone game.» — the page's sass allocation; lands on the design, one
  per page, within budget; severity: noted; keep.
  **bs15: kept-with-defense** — within TONE §2's budget; unchanged.
- [290-291] «it is a policy rather than an accident — the level above
  knows something this level does not» — "X rather than Y" flourish plus
  em-dash where a colon serves; severity: noted.
  **bs15: fixed-by-R2** — colon for the dash; the "rather than"
  contrast stays (policy vs accident is the sentence's content).
- [294] «What none of these is, is a way to keep a broken program
  running.» — fronted-negation flourish; the corrective is real (the
  paragraph disarms a misreading); severity: noted; suggest "None of
  these keeps a broken program running."
  **bs15: fixed-by-R3** — suggested sentence adopted verbatim (the
  corrective survives; only the fronting goes).
- [377-378] «`child.cancel()` — the polite one — because the shard is not
  misbehaving, it is finished» — em-dash pair where parens serve plus the
  "not X, it is Y" reveal; severity: tell; suggest "`child.cancel()`,
  the polite verb, because the shard is finished rather than
  misbehaving — and §14.2's table says that is the verb whose defers
  run."
  **bs15: fixed-by-R2 + R3** — suggestion adopted in comma form with
  no dash: «Notice which verb stands the shard down: `child.cancel()`,
  the polite one, because the shard is finished rather than
  misbehaving, and §14.2's table says that is the verb whose `defer`s
  run.» (The §14.2 reference is woven grammar, which R6 permits.)
- [415-416] «It is not a special case bolted on so the forest has a root;
  it is the same kind of thing as the other three» — negation family,
  textbook "not X; it is Y"; severity: tell; suggest "It is the same
  kind of thing as the other three, which is why the process's own death
  is describable in a shard's vocabulary."
  **bs15: fixed-by-R3** — suggestion adopted, keeping the original
  tail («…in the same vocabulary as a shard's»); the sentence's
  opening dash goes to a colon under R2.
- [426] «A service that appears in neither list does not exist.» —
  negation closer (counted); defended: it is the section's checkable
  claim in nine words; severity: noted; keep.
  **bs15: kept-with-defense** — checkable claim, R3's content test;
  the paragraph's «worth keeping» frame ([422]) is gone but this
  closer is untouched.
- [442] «the `stdout=` clause — not the exit code — is the part of this
  header that actually verifies the rule» — "X, not Y" corrective with
  em-dash pair in an exercise stem; the distinction is the exercise's
  point; severity: noted; commas serve.
  **bs15: fixed-by-R2** — commas; the corrective stays (it is the
  exercise's point, R3's content test).

## bs15 — register fixes with no bs14 row

The bs14 catalog rowed findings, not templates; these edits come from
the register's template rules and complete the diff's accounting:

- **R1** — all nine exercise-header separators: «*(kind · tool)* —» →
  «*(kind · tool)*.»
- **R2** — five stem/box hinge-dashes not individually rowed (15-1's
  «Predict both fields —» → semicolon; 15-3's «"neither" — which» →
  colon; 15-5's «before running —» → comma; 15-6's «— the flappy
  dependency pattern» → parentheses; 15-9's in-quote dash → period;
  the Erlang box's «trappable — `process_flag…`» → colon; §15.2's
  «`spawn proc shard(attempt, out)` —» → colon; «into denial —» →
  colon; «the program itself —» → colon).
- **R11** — §15.2's meta-count opener: «Walk the four decisions in it,
  because they are the four decisions every supervisor makes and no
  library removes them:» → «Four decisions are in it, and no library
  removes them:». The four bold run-in leads stay (layout, TONE §1).
- Zero edits inside code, console, REPL, or diagnostic blocks; the
  audit ledger comment is untouched.
