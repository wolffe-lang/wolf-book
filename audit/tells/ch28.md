# ch28 — tells audit (bs14, lane C)

**Counts:** charter 0 · tell 3 · noted 9
**Em-dashes (prose only):** 24 in 1875 prose words = 12.8/1000. Seven are
exercise-stem template dashes; ~17 are running-prose dashes. Below the
lane's 14/1000 constant but still over a K&R budget.
**Negation-family count:** 9 (L7–8, L157–158, L274–276, L277–278,
L282–286, L288–289, L397–398, L450–451, L458).
**Banned vocabulary:** none.
**Verdict:** The strongest argument in the part and mostly in register —
measurements carry the pitch and the sass is dry — but the chapter's
default rhythm is the negation drumbeat ("There is no X… not one of
them… because there is no Y"), which the subject (absence of `malloc`)
half-defends and the repetition un-defends.

## bs16 rewrite — recomputed header

Rewritten against REGISTER.md (ratified 2026-08-28, three amendments);
line references below are to the bs14 text.

**Em-dashes:** 0 running in 1801 prose words = 0.0/1000w (was ~17
running in 1875 = 9.1/1000w standardized; template 7 → 0 by R1). Pairs
to parentheses (R7), hinges to colon/comma/period per R2's order; no
survivor met the all-four-misread bar.
**Negation family:** 9 → the §28.2 drumbeat stated positively
("allocation failure is unreportable in this language"), the §28.5
brace passage kept as the chapter's one earned drumbeat (comma form),
[272]/[274–280]'s "There is no `NULL`" device kept per defense, and
the closer «cannot be written wrong … cannot be written at all» kept
as the chapter's R8/R3 thesis survivor. The opening reveal mold
(«not really about a tree. It is about `malloc`») → positive-first
per the row's suggestion.
**"rather than":** 2 → 1 (container/number kept — content; the
operator/call instance dropped with the row's suggested rewrite).
**R10:** the §28.5 measurement bold (115 against 41) demoted — the
systemic measurement-section unbold, third member with ch26/ch32.
**R5:** «worth stating» (L389) deleted; ch32 forward-reference
«worth seeing once from below» → «to see the floor once from below».
**Reality check (R9):** no schedule prose. The interpreted-only
posture re-verified at the pin: `wolf build wordtree.lu` still refuses
("`mut` arguments beyond local places (c06)", exit 1, probed at
addcd7f), so the chapter's lupin transcripts and ch27 §27.5's
middle-program claim stay true. No fence-status changes.
**Word count:** 1875 → 1801.

## Findings

- [7–8] «K&R §6.5 is not really about a tree. It is about `malloc`.» —
  negation family ("not X. It is Y.") as the chapter-opening move, the
  lane's signature (ch24 L85–86 opens a section identically); severity:
  tell; suggestion: "K&R §6.5 is about `malloc`; the tree is the shape
  the chapter needed in order to have something to allocate."
  **bs16: fixed-by-R3** — suggestion adopted; the lesson-underneath
  dash pair moves to parentheses (R7) in the same sentence.
- [282–289, 448–452] «not one of them can hand back a failure … is not a
  value the program gets to see … they are gone for one reason: the
  failure they guarded against is not reportable in this language» /
  «freed in one motion — not one call per node, and not in an order
  anybody had to get right, because there is no order» — the same
  negation-anaphora drumbeat twice, three-plus negations per breath; the
  chapter's subject is an absence, which defends any single sentence and
  not the recurring rhythm; severity: tell; suggestion: keep one drumbeat
  (the §28.5 brace passage is the earned one) and state §28.2's version
  positively ("allocation failure is unreportable in this language; the
  guards go with it").
  **bs16: fixed-by-R3** — §28.2 stated positively per the suggestion;
  §28.5's brace passage kept as the earned drumbeat, its dash to a
  comma (R2).
- [412–415] «Code lines only: **115 against 41.**» — bold-for-emphasis on
  the measurement, the same device ch26 §26.5 uses («**76 lines of C
  against 52 lines of wolf**») — a recurring measurement-section shape,
  likely systemic to the contrast-chapter template; severity: tell;
  suggestion: drop the bold; the number is the loudest thing on the page
  already.
  **bs16: fixed-by-R10** — bold dropped (systemic measurement-section
  unbold with ch26/ch32).
- [10–11] «every allocation is yours, every pointer can be null, every
  `malloc` has a `free` somewhere with your name on it» — anaphoric
  rule-of-three inside an em-dash aside; the three facts are real and the
  clause is the page's sass allocation; severity: noted; suggestion:
  none required; unparallel one item if bs15 thins the chapter's rhythm.
  **bs16: kept-with-defense** — the triple stays (the page's sass
  allocation); its dash pair moves to parentheses (R7).
- [156–158] «`treeprint` is 8 lines and it is the algorithm … `treefree`
  is 9 lines and it is not the algorithm at all.» — negation antithesis;
  the contrast is the section's technical point, so context defends;
  severity: noted; suggestion: none.
- [272]
  **bs16: kept-with-defense** — unchanged; the L159 post-order dash
  pair in the same passage moves to parentheses (R7).
- [272] «the first one is a word that is missing» — the
  "notice-what-is-not-there" device, third lane instance (ch23 L56, L189);
  severity: noted; suggestion: keep — this chapter is the device's
  strongest use; thin it in ch23 instead.
  **bs16: kept-with-defense** — kept; ch23's instances were thinned in
  that chapter's rewrite.
- [274–280] «There is no `NULL`. … It cannot be dereferenced by accident,
  because there is nothing to dereference» — negation stack + "rather
  than" ("about a container rather than about a number"); counted in the
  drumbeat row's family; severity: noted; suggestion: covered by the
  L282–289 row's rewrite.
  **bs16: fixed via the drumbeat row** — the "There is no `NULL`"
  opening stays (the [272] device); the container/number contrast
  keeps its one "rather than" (content, within the R5 cap).
- [297–299] «it is an operator rather than a call, so the three-way
  branch reads as a three-way branch» — "rather than" + doubled phrase;
  severity: noted; suggestion: "it is an operator, so the three-way
  branch reads as one."
  **bs16: fixed** — suggestion adopted.
- [337–343] «The C twin needs 23 lines of `getword` to do it — a
  character loop, a case fold, a pushback with `ungetc`, a `lim` to keep
  the word inside its buffer — and every one of those lines is there
  because…» — em-dash pair around a four-item list, closing on "does not
  come up, because there is no buffer"; severity: noted; suggestion:
  parenthesize the list.
  **bs16: fixed-by-R7** — parenthesized.
- [384–387] «Both programs fold case … both order by the same byte
  comparison … both count repeats the same way, because there is only one
  way» — anaphoric triple; the three are the three real checks CI makes;
  severity: noted; suggestion: none.
  **bs16: kept-with-defense** — unchanged.
- [397–398] «`words()` is the convenient answer and not the complete
  one.» — negation antithesis at paragraph close; the sentence corrects a
  real misreading the side-by-side invites, which defends it; severity:
  noted; suggestion: none.
  **bs16: kept-with-defense** — unchanged; the frame sentence above it
  loses its «worth stating» marker (R5).
- [457–458] «the reason `treefree` cannot be written wrong here is that
  it cannot be written at all» — antithesis epigram; single, earned, the
  chapter's thesis in one line; severity: noted; suggestion: keep — thin
  the family elsewhere so this lands.
  **bs16: kept-with-defense** — the chapter's thesis closer; family
  thinned around it.

## bs16 — register fixes with no bs14 row

- **R1** — all seven exercise-stem separators to periods; 28-3's
  stem-body dash to a period.
- **R2/R7** — unrowed hinges and pairs: L102 (period), L110 (colon),
  L291 (colon), L393 (parens), L415 (parens), L438 (comma), L461
  (colon).
- Zero edits inside code, console, or diagnostic blocks; the audit
  ledger comment and the Mahler epigraph are untouched.
