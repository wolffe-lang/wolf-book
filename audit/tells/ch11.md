# ch11 — tells audit (bs14, lane A)

**Counts by severity:** charter 0 · tell 6 · noted 10 — 16 rows.
**Em-dashes (prose only, fences and ledger stripped):** 22 in 1592 prose
words = **13.8 per 1000** — mid-lane.
**Negation family:** 8 instances (lines 3–4, 10–12, 48–49, 58–60,
131–132, 141–142, 146–147, 217).
**Banned vocabulary:** none (verified by eye and grep; zero
"simply/just/easy/of course", no marketing adjectives, no copula
avoidance).
**Verdict:** a short chapter whose argument is genuinely economical —
the refresher section is nine lines of code doing the persuading — but
whose connective tissue is inherited templates: the corrective
"Not X — Y" frame twice, the "stated plainly" cost paragraph shared
with ch08, and the worth/credit formulas throughout.

## Findings

### Tell

- [10-12, 141-142] «Not a helper that starts a reader and waits for it —
  a helper that starts one and returns, leaving the task running» and
  «Not "until someone remembers to stop it" — the brace after
  `stop.close()` will not complete while the refresher lives» — the
  corrective "Not X — Y" negation frame twice in one short chapter
  (fragment opener, dash hinge, both times); severity: tell; state the
  positive first: "a helper that starts a reader and returns, leaving
  the task running" / "The refresher lives exactly as long as `shelf`:
  the brace after `stop.close()` will not complete while it lives."
- [58-60] «What wolf has removed is the third option, which is the
  function that starts work outliving its own return *without saying
  so*. There is no signature for it.» — the "it has no spelling /
  there is no signature for it" epigram, near-verbatim ch10 line 264's
  «it has no spelling» one chapter earlier; severity: tell; one of the
  two chapters keeps the line; the other says "wolf removed the third
  option."
- [131-132] «Four things in that program are worth naming, and none of
  them is a library.» — the "worth X-ing" formula (five in ch10 alone)
  + "none of them is" negation punch; severity: tell; "Four things in
  that program, none of them a library:"
- [160-165] «The cost of this pattern, stated plainly: … That is the
  bill. What it buys is on the same page — the answer to "can this
  program exit with work still running" is readable from the braces,
  the refresher has an owner, and the shutdown path is a line of code
  rather than a convention.» — the "The cost, stated plainly" template
  (verbatim ch08 line 1021's paragraph opener) + bill/buys ledger motif
  + rule-of-three + "rather than a convention"; severity: tell;
  SYSTEMIC candidate — the cost-paragraph template recurs lane-wide;
  "The cost: every call site carries a parameter it would not need
  under detached spawn. What it buys: exit-safety readable from the
  braces."
- [217-223] «That is not a stack sample. Read it as a tree … The dump's
  *contents* are the implementation's business and may grow; the dump's
  *existence* is contract, which is a distinction worth keeping in mind
  when you rely on one.» — negation opener + italic-stressed antithesis
  mirror + "…'s business" formula + "worth keeping in mind"; severity:
  tell; "Read it as a tree: every edge was declared by the program. The
  contents may grow; the existence is contract."
- [225] «The lesson here is borrowed and worth crediting.» — the
  credit-formula (ch06 "Credit where it is due", ch08 "Credit where it
  is owed") fused with the worth-formula; severity: tell; "Java's
  structured concurrency work (JEP 444) made the same observation" —
  the credit lands harder without the announcement.

### Noted

- [3-6] «Chapter 10 ended on a sentence with a consequence in it: what a
  function needs in order to spawn is not a keyword but a scope. … that
  one fact is the whole of this chapter.» — negation recap +
  self-ranking; severity: noted; the recap quotes the previous
  chapter's own closer, which defends the negation; "the whole of this
  chapter" is the cuttable flourish.
- [46-48] «The pattern is Nathaniel J. Smith's "nursery as a
  capability", credited» — the word "credited" notarizing the credit
  the sentence is already giving; severity: noted; drop the word — the
  attribution is the credit.
- [48-50] «nothing in that signature is a lifetime and nothing in it is
  a promise about duration. It is a name for a brace.» — no-anaphora
  pair + staccato epigram closer; severity: noted; the closer is
  earned; counted toward closer-cadence density.
- [55-56] «A function that takes a `Scope` is the honest spelling of
  the other contract» — honest-motif (lane-wide tic); severity: noted;
  motif-count entry.
- [66-68] «Two capabilities, two searches, the same reason.» — the
  "N X, N Y" numeric-parallel closer formula (ch01, ch03, ch08
  siblings); severity: noted; formula-count entry; defensible content —
  the mutation-grep parallel is real.
- [90-91] «for as long as the shelf is serving, and not one moment
  longer» — dash-appended flourish; severity: noted; "for exactly as
  long as the shelf is serving."
- [146-147] «a deadlock you find in the first test run, not a process
  that never exits in production» — X-not-Y; severity: noted; context
  defends it — the class-of-bug contrast is the section's claim
  (ch10 §10.2 makes the same argument); counted for density.
- [154-156] «There is no sleep in this program. A sleeping task is a
  task that cannot be told anything» — negation + definitional epigram;
  severity: noted; both sentences carry real content; density only.
- [172-183] the Coming-from-Go box — «the honest comparison is with
  `context.Context`» + «Two differences are worth stating.» — honest-
  and worth-formula hits inside an otherwise fair, credited comparison
  whose closer («the difference is where the enforcement lives») is the
  right register; severity: noted; trim the two formula clauses.
- [234-237] «a dump whose entries are `task@37` and `task@80` tells you
  the shape, and a dump whose entries say `reader` and `refresher`
  tells you the program.» — balanced mirror; severity: noted; the
  contrast is content-bearing — but note the seam: the ledger records
  that user task-naming has no spelling at this pin, so the second half
  of the mirror describes a dump no current tool can print; the truth
  question is the ledger's (its task-name papercut row), the rhythm is
  counted here.

## Defended, not rowed (false-positive honesty)

- [134, 141, 149, 154] the four bold paragraph leads («The shutdown
  signal is a channel close.» …) — pseudo-headings over a real
  four-item enumeration, the ch07 precedent; no action.
- [42-46] «you extended `readers`'s lifetime, `readers` is written at
  the call site, and the brace those three tasks die at is nine lines
  below in the same function» — a triad, but each clause is a distinct
  checkable fact about the program on the page; no action.
- [167-170] «One shape this chapter deliberately does not build is the
  daemon … That wants a failure domain rather than a scope, and chapter
  15 gives it one.» — scope statement with a bound forward reference,
  the shape TONE's tense discipline permits; the ledger's
  cross-reference accounting row concurs.
- [225-232] the JEP 444 passage — credited lineage with a fair
  technical contrast (list-of-stacks vs tree); the register TONE's
  comparison rule asks for; only the opener sentence is rowed.
