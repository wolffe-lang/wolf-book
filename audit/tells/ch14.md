# bs14 tells audit — ch14 (Procs: the unit of failure)

**Counts:** charter 0 · tell 7 · noted 7
**Em-dashes:** 28 in 2810 prose words = 10.0/1000w. 9 are the
exercise-header template; 19 in running prose.
**Negation family:** 12 hard instances; "rather than" ×9 (one spans a
line break at 294-295). The "not X — it is Y" reveal appears in every
section.
**Banned vocabulary:** none.
**Verdict:** the Armstrong material is close to the register (credited
lineage, checkable claims); the distance is the same two rhythms as
ch12–13 — the negation reveal, the anaphoric "No X. No Y. No Z." triple —
plus a "worth V-ing" significance-marker tic that peaks in this chapter.

- [46, 76, 167, 280, 571] «worth compressing» / «worth knowing» / «worth
  carrying» / «worth stating» / «worth knowing» — one finding: "worth
  V-ing" as a significance marker five times in one chapter
  (editorialized significance, soft form); severity: tell; suggest
  deleting the marker and letting the sentence make the claim ("The
  thesis reports a number about AXD301: around 92%…").
- [37-38] «`main` did not handle an error — it was *told* one, by name»
  — negation reveal with em-dash; the handled/told distinction is the
  chapter's point, which half-defends it; severity: tell (cluster
  member); suggest "`main` was told an error, by name, and went on
  serving."
- [44] «The design above is not new and this book will not pretend it
  is.» — negation opener (counted); defended by TONE's credited-lineage
  rule — the sentence is doing attribution; severity: noted; keep.
- [50] «Not the errors in your row — those are chapter 6's» — fragment
  negation with em-dash; severity: noted; a colon serves.
- [62-64] «**the unit of service, the unit of failure, and the unit of
  ownership are the same unit.**» — bold-for-emphasis in running prose
  (the chapter's only instance, so not sprinkle); the triple is
  Armstrong's actual alignment and is defended; severity: noted; suggest
  italics or plain text for the emphasis.
- [64] «"let it crash" is not recklessness, it is arithmetic» — negation
  family; the aphorism earns part of its shape; severity: tell; suggest
  "Draw those three boundaries on top of each other and "let it crash"
  is arithmetic."
- [111-118] «Three shapes, three predicates (`is_normal`, `is_error`,
  `is_killed`)» — the numeric-cadence tic (see ch13's cluster);
  severity: noted; the enumeration is real, the drumbeat is the pattern.
- [125-126] «There is no callback, no registry, and no supervisor type
  in the language — which is the point of §15.2.» — "no X, no Y, no Z"
  triple; three real absences, partly defended; severity: noted.
- [169-171] «The claim is not that 92% of the code was straightforward;
  it is that 92% of it was *sequential*» — negation family "not X; it is
  Y"; severity: tell; suggest "The claim is that 92% of it was
  sequential — written and tested as ordinary functions."
- [274-275] «Region `#1` is not closed, not emptied, and not queued for
  anything. It is gone» — negation triple plus "It is" reveal, the same
  shape as ch12 [272-275]; severity: tell; suggest "Region #1 is gone,
  and no line of the program freed it."
- [281-283] «No destructor ran. No finalizer queue drained. No garbage
  collector noticed.» — anaphoric rule-of-three; the three absent
  mechanisms are real, but the drumbeat is the default rhythm; severity:
  tell; suggest folding into one sentence ("no destructor, no finalizer
  queue, and the garbage collector that would have noticed does not
  exist" — or cut to two).
- [337] «`kill` is not that.» — short negation pivot (counted);
  severity: noted; keep — three words, does its job.
- [541-542] «the fix is never a timeout inside the handler — it is to
  make the waiting somebody else's job» — negation reveal with em-dash;
  severity: tell; suggest "the fix is to make the waiting somebody
  else's job, never a timeout inside the handler."
- [568-570] «A proc is not an OS process. … what separates them is the
  type system's refusal to let a writable reference cross, not a page
  table» — negation pair opening the costs section; defended: the
  correction is the fact being taught, and the section is otherwise
  deadpan; severity: noted; keep the first, consider flattening the
  second.
