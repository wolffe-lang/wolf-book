# bs14 tells audit — ch18 (Comptime: one tier, no macros)

**Counts:** charter 0 · tell 9 · noted 12
**Em-dashes:** 41 in 2993 prose words = 13.7/1000w. 9 of the 41 are the
exercise-header template; 32 in running prose — the lane's largest raw
count.
**Negation family:** 14 hard instances; "rather than" ×7. The chapter
opens on a negation triple and closes on a "no X, no Y, no Z, no W"
recap.
**Banned vocabulary:** none.
**Verdict:** vocabulary clean and much of the middle is deadpan and in
register; the distance is the "worth V-ing" marker at ×5 (ties ch14's
peak), the numeric-pair cadence as section punctuation, and openings and
closings that stack negation anaphora.

- [145, 261, 382, 548-549, 622] «worth memorizing» / «worth naming» /
  «worth holding onto» / «worth having an opinion about» / «worth
  separating» — one finding: the "worth V-ing" significance marker five
  times, tying ch14's peak; severity: tell; suggest cutting the marker
  ("Two properties of that error: it is a type error in the ordinary
  sense, and it arrives at the line that stated the requirement").
- [15-19] «Not a second language, not a template dialect, not a macro
  expander over tokens. The same `while`, the same `if`, the same checked
  arithmetic, the same `assert` — running on a different clock, inside a
  box with nothing in it.» — fronted negation triple immediately followed
  by a "the same" anaphora ×4 with em-dash reveal, in the chapter's
  second paragraph; the content is D29's real claim; severity: tell;
  suggest "Comptime is wolf, evaluated during compilation: the same
  `while`, `if`, and checked arithmetic, on a different clock, inside a
  box with nothing in it."
- [101-102] «There is no program. Nothing has been linked, nothing has
  run, and the loop above has already gone around four times to disagree
  with you.» — negation anaphora; the closing personification is the
  page's wry clause and lands on the machine; severity: noted.
- [141-142] «It will hold 16 when the program runs, and the program is
  not what is running.» — antithesis epigram (counted); terse and does
  the teaching; severity: noted; keep.
- [181, 594-595, 823] «One semantics, two moments.» / «One error code,
  three different reasons, and the catalog entry sorts them into exactly
  two categories» / «Four spellings, one mechanism.» — one finding: the
  numeric-pair cadence as section punctuation (ch13's tic); the numbers
  are real each time; severity: tell; suggest keeping one per chapter at
  most.
- [183-185] «comptime is not a separate dialect with its own arithmetic:
  the rule did not change, the clock did» — negation plus antithesis
  closer; defended: it restates the chapter's thesis directly after the
  evidence; severity: noted.
- [223-224] «A type arrives as an argument. Not as a parameter in angle
  brackets, not as a template placeholder — as a value, of type `type`,
  passed the way `900` is passed.» — fronted negation pair with em-dash
  reveal, textbook shape; severity: tell; suggest "A type arrives as an
  argument: a value, of type `type`, passed the way `900` is passed."
- [257-259] «which is the difference between a shape you rely on and a
  shape you hope for» — balanced-antithesis epigram; severity: noted.
- [302-304] «What a derivation actually contributes is not the four
  lines; it is the *precondition*.» — negation family, textbook "not X;
  it is Y"; severity: tell; suggest "What a derivation actually
  contributes is the precondition."
- [386-390] «no expansion step, no generated source to debug. … There is
  no token stream to inspect, no syntax to rewrite, and therefore no
  possibility that a derivation quietly means something other than what
  its inputs say» — two "no X, no Y" runs in one paragraph, the second a
  triple; severity: tell; suggest one run, two items.
- [396-399] «The door is not bricked over: a v2 could add a syntactic
  tier if this one proves insufficient.» — negation flourish plus
  future-hypothetical prose; borderline against TONE's tense discipline
  (scope statements are fine, but "a v2 could add" edges toward
  schedule); no D-number is cited for the revisability claim; severity:
  noted — flagged for the human, not for bs15's pen.
- [429-431] «A runtime formatter finds out at the moment of printing, on
  the unlucky code path, in production.» — escalating triple; the three
  phases are one real scenario, which half-defends it; severity: noted.
- [549-550] «the refusals are not an incomplete allowlist waiting to be
  filled in. They are the feature.» — negation reveal, textbook "not X.
  They are Y"; severity: tell; suggest "the refusals are the feature."
- [558-560] «no filesystem, no network, no environment, no clock, no
  randomness, and no way to call into native code» — "no X" ×6; defended:
  it is D33's actual sandbox list and reappears verbatim in the quoted
  catalog entry; severity: noted; keep.
- [625, 632, 637] «**Caching gets a key it can trust.** … 
  **Reproducibility becomes checkable rather than aspirational.** …
  **Auditing becomes reading.**» — bold-sentence-led parallel triad as
  section structure (same template as ch15's bold-led decision list);
  borderline bold-sprinkle; severity: noted; see LANE-B systemic row.
- [634-635] «the reason is not discipline — it is that the axes along
  which they differ are not reachable» — negation family with em-dash,
  textbook shape; severity: tell; suggest "the reason is that the axes
  along which they differ are not reachable."
- [648] «a comptime loop that does not finish is a build that does not
  finish» — mirrored-parallel epigram (counted); the identity is the
  actual hazard; severity: noted; keep.
- [748-751] «Budgets are raised, never removed. … a metered evaluator
  with an off switch is an unmetered evaluator with an extra step» —
  aphorism closer; the "X with an extra step" snowclone is meme-shaped
  but is the page's one wry clause and lands on the design; severity:
  noted.
- [755] «Three absences, stated plainly, because each of them is
  something a reader arriving from another language will look for.» —
  the "N absences, stated plainly" section-opening template, verbatim
  echo of ch17 [422] ("Two absences, stated plainly…"); severity: tell
  (template-baked, see LANE-B systemic row); suggest varying the frame
  or dropping the meta-sentence.
- [758-761] «it does not reach into your struct and add a field, and it
  does not attach itself to somebody else's function. What you see in a
  file is what the file says.» — negation pair plus epigram closer; the
  epigram is the section's checkable claim; severity: noted.
- [829-834] «no ambient inputs, no unbounded evaluation, no reaching
  into declarations, no looking at syntax. … and that sentence is the
  whole reason the tier is shaped like this» — closing "no X" ×4 recap
  plus self-editorialized significance ("that sentence is the whole
  reason", ch17 [199-201]'s shape); severity: noted; suggest cutting the
  self-reference.
