# 2. Strings, honestly

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 2.1 Literals, methods, interpolation

<!-- promise: write `"{x}"` in any literal and format anything with `"{total:>8.2}"`. -->

## 2.2 Multiline and raw

<!-- promise: use `"""` dedented blocks and `r"…"` without escape archaeology. -->

## 2.3 Bytes, honestly

<!-- promise: predict `"é".len`, slice by byte offset with checked ranges, and say why there is no `s[i]`. -->

## 2.4 Iterating meaning

<!-- promise: walk `words()` and `lines()` and pick the iterator that spells its unit. -->

## 2.5 What the machine does

<!-- promise: know what a slice costs (two words) and what an f-string compiles to. -->
