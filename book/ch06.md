# 6. Errors are values

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 6.1 `!T` and the row

<!-- promise: read `int ! {Empty, NotDigit(Bad)}` as the complete list of what can go wrong. -->

## 6.2 `?`, `else`, `else |err|`

<!-- promise: propagate with one character, default with one keyword, handle with a match on the row. -->

## 6.3 `errdefer`

<!-- promise: run cleanup only on the error path, and predict both paths. -->

## 6.4 Hardening by refactor

<!-- promise: take chapter 1's panicky script to production honesty one construct at a time. -->

## 6.5 Capstone: wordcount

<!-- promise: build Part 1's whole toolkit into one real program, with a boxed promise: in Part 3 this loop parallelizes by changing one call. -->
