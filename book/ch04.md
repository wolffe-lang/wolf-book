# 4. Functions

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 4.1 Signatures are the contract

<!-- promise: write signatures on items, nothing in bodies, and read a definition as its own documentation. -->

## 4.2 Functions as values

<!-- promise: pass closures with `fn(a, b) expr`, compose them, return them. -->

## 4.3 `defer`

<!-- promise: put cleanup next to acquisition and predict LIFO order. -->

## 4.4 Borrow by default

<!-- promise: pass parameters with no sigils, return by move, and state the rule in one sentence. -->
