# 1. Hello, Wolf

<p class="epigraph">Bruckner's Fourth opens with one horn call over a
tremolo: everything the symphony will do, said once, quietly, before
anyone explains it.</p>

## 1.1 A program worth keeping

Books like this one open with a program that prints a greeting. That
program is in this chapter too, and you should type it. First, though, a
program you might keep.

You have a file of rows: a name, a comma, an amount in cents. The
question is what they add up to.

```wolf,run(exit=0)
fn main() -> !int {
    let rows = """
        espresso,340
        pastry,275
        tip,100
        """
    var total = 0
    for row in rows.lines() {
        var i = 0
        while row[i..i + 1] != "," { i += 1 }
        let name = row[..i]
        let cents = row[i + 1..].to_int() else 0
        total += cents
        print("{name:<10}{cents:>6}")
    }
    print("{"total":<10}{total:>6}")
    0
}
```

It answers:

```console
$ lupin receipt.lu
espresso     340
pastry       275
tip          100
total        715
```

Eighteen lines, and nothing in them is a preview of syntax you meet
later: this is the language. `let` names a value; `var` names one that
changes. `"""` opens a block literal whose common indentation is trimmed
away, so the data sits where you would put it in a file. Braces inside a
string interpolate the expression between them, and `{cents:>6}` means
right-align in six columns. `row[..i]` slices bytes. `for` walks what
`lines()` hands it.

Two lines lie for convenience. The `while` loop trusts every row to hold
a comma, and `else 0` decides that an amount which is not a number is
worth nothing. §1.5 shows what the first lie does when the input
disagrees, and chapter 6 takes both apart; this program is Part 1's
running example.

Now the greeting. The smallest wolf program that does anything:

```wolf,run(exit=0, stdout="hello, wolf")
fn main() -> !int {
    print("hello, wolf")
    0
}
```

`main` is where a program starts. `print` writes one line to standard
output. The `0` on its own line is the value `main` returns, and the
process exit code (§1.3 makes that concrete). The `!` in `!int` says
`main` may fail instead; chapter 6 is where that becomes a tool, and
until then it is the shape `main` has.

Nothing sits above `fn main`, because a wolf file needs nothing there:
no imports, no enclosing class, no guard against being imported.

**Exercise 1-1** *(fingers · lupin)*. Type the hello program exactly as
printed and run it. Then make it print a second line of your choosing.

**Exercise 1-3** *(fingers · lupin)*. Braces interpolate expressions,
not only names. Print a sentence that computes `6 * 7` twice inside one
string literal.

## 1.2 Two implementations, one language

Wolf is one binary. `wolf` compiles your program, runs it, formats it,
and answers your editor's questions; there is no separate package
manager and no build layer. Save the greeting as `hello.lu` and point
the compiler at it:

```console
$ wolf build hello.lu && ./hello
hello, wolf
```

`hello` is a program. It needs no runtime, starts as fast as the kernel
can map it, and runs for somebody who has never heard of wolf. The
compiler said nothing on its way
there, which is the only thing a compiler should say when it agrees
with you.

While you are still editing, the build and the run fold into one
command:

```console
$ wolf run hello.lu
hello, wolf
```

`wolf run` compiles into a cache beside your file and executes what
comes out, so an unchanged program's second run does not visit the
compiler at all. It is the same machinery with the shell step folded
in.

Now the same file through the other door:

```console
$ lupin hello.lu
hello, wolf
```

Nothing was compiled that time. `lupin` read the program and ran it,
and the bytes on your terminal are the same bytes.

### Why there are two

`lupin` is not the compiler with the optimizer switched off. It is an
independent implementation of the wolf specification, written from the
spec rather than from the compiler's source, and its day job is to
disagree: a program whose behavior differs between the two is a bug in
one of them or a hole in the spec, and it gets found before you meet
it. With one implementation, there is no way to tell what the language
specified from what it happens to do.

So the answer to "which one is wolf" is neither. The specification is
wolf, and these are two readings of it that have to keep matching.
Every page in this book says which of them is speaking. Slow down at
the pages where they speak in turn; §1.5 begins the habit.

Ask either who it is and it tells you:

```console
$ wolf --version
wolf 0.2.0 (wolfgang, pin c88ab64)
paired with lupin 0.1.17 (reference interpreter), pin addcd7f
$ lupin --version
lupin 0.1.18 (wolf-interp, reference interpreter at pin addcd7f)
```

Each names the other for a reason. The compiler's second
line is the interpreter revision it was tested against; the
interpreter's is the compiler revision it was tested against. Two
implementations agreeing is worth something only when you can find out
*which* two agreed.

The colophon records the versions this printing is true for; those lines
are how you check that they are yours. If your terminal differs from the
book's printed output, check the versions before calling it a bug.

Both are cargo projects, and both build the same way on every tier-1
platform (linux x86-64 and aarch64, macOS aarch64, windows x86-64).
Given a Rust toolchain and the two repositories, two commands build
them:

```console
$ (cd wolf-lang && cargo xtask dist)
$ (cd wolf-interp && cargo build --release --bin lupin)
```

The first command is wolf's own release builder: it compiles the
compiler and the runtime library it links programs against, and it
stamps the build with the commit it was made from — the stamp is what
the bare `wolf 0.2.0` line above is. A plain `cargo build` compiles
the same compiler and runs everything in this book, but its first
line reads `wolf 0.2.0+dev.unknown`: a build the release machinery
did not stamp does not claim the release. Cargo leaves `wolf` under
`wolf-lang/target/release/` and `lupin` under
`wolf-interp/target/release/`. Put both on your `PATH`, run the two
`--version` commands above, and the rest of the chapter works.

**Exercise 1-7** *(fingers · wolf + lupin)*. Compile the greeting with
`wolf build`, run the binary, then run the source under `lupin`. Compare
the two outputs byte for byte: `diff <(./hello) <(lupin hello.lu)` will
do it. Then say which of the two runs could have printed something
different, and what it would mean about the language if it had.

## 1.3 Scripts before projects

A wolf program can be one file, run in place:

```console
$ wolf run hello.lu
hello, wolf
```

There is no project to create first, no manifest, no lockfile to drift
out of date. The file is the program. Chapter 22 covers what
happens when one file stops being enough, and that is a later problem
than most languages make it.

> **Coming from Python:** the equivalent Python is about as short:
>
> ```python
> print("hello, wolf")
> ```
>
> The difference is around the file, not in it. Python's
> one-liner needs an interpreter on the machine that runs it, of a
> version the script never states; wolf's compiles to a program that
> states its own requirements by being one. Neither answer is free.
> Python's costs you an environment to reproduce; wolf's costs you a
> compile, and §1.5 is where you watch what that compile buys.

The value `main` returns is the process exit code:

```console
$ wolf run hello.lu
hello, wolf
$ echo $?
0
```

That is the last expression of `main`, handed to the shell. Wolf has no
`return` ceremony at the end of a function: the last expression of a
block is its value, a rule you will meet again in every `if` and
`match` you write.

Loops come in the two shapes you expect. `for x in xs` walks a sequence;
`while cond` repeats until the condition fails. Both are in §1.1's
program, and the exercises below are where you write one.

**Exercise 1-2** *(comprehension · lupin)*. Before running, write down
what this program prints and what `echo $?` shows afterward:

```wolf,run(exit=3, stdout="working")
fn main() -> !int {
    print("working")
    3
}
```

**Exercise 1-5** *(fingers + extension · lupin)*. The first table in
*The C Programming Language* converts Fahrenheit to Celsius. Write
wolf's: 0 to 120 degrees in steps of 20, one line per row. Then look
hard at the 20-degree row. Is it right?

## 1.4 The REPL: a spec you can interrogate

Run `lupin` with no arguments and it opens a session. Expressions
evaluate and print their value and their type; declarations persist for
as long as the session lives.

```wolf-repl
wolf> 6 * 7
42 : i32
wolf> "wolf".len
4 : i64
wolf> :type "wolf".len
i64
wolf> let howl = "awoo"
wolf> "{howl} {howl}"
awoo awoo : str
wolf> fn double(n: int) -> int { n * 2 }
defined fn `double`
wolf> double(21)
42 : i64
wolf> :quit
```

The type is
printed on every line, so a question about types is one keystroke and
never a guess: `6 * 7` is `i32` because that is what an integer literal
infers to, while `"wolf".len` is `i64` because a length is. And `:type`
answers without running the program for effect, which makes the prompt a
way to interrogate the language rather than a calculator that happens to
speak it.

Because this is the reference interpreter, the answers are the
specification's answers. When a later chapter claims wolf does
something, you can ask.

A fault does not end the session:

```wolf-repl
wolf> let s = "wolf"
wolf> s[2..9]
trap(bounds): byte range 2..9 is outside a 4-byte string [mem.ub.defined] at 0..7
the session survives the trap; the world is as the fault left it [repl.trap.alive]
wolf> s.len
4 : i64
wolf> :quit
```

The prompt comes back, the world is whatever the fault left behind, and
`s` is still four bytes long. Chapter 2 explains that message.

One directive to note and set aside: `:mem` prints the state of the
memory model (what exists, what owns it, what is still open). Part 2
leans on it. `:help` lists the rest.

**Exercise 1-4** *(fingers · lupin REPL)*. Open the REPL. Compute the
number of seconds in a day, ask `:type` what type that expression has,
then define a function mid-session and call it twice. Before you ask
`:type`, write down your guess.

## 1.5 What `run` was doing for you

`wolf run hello.lu` looks like one action. It is six: the file is
tokenized, parsed, its names resolved, its types checked, its memory
discipline checked, and only then run. `lupin hello.lu` is the
same six. The two implementations part company at the last one, and
only there: the compiler writes machine code and hands it to the
kernel; the interpreter walks the program itself.

The phases divide the ways a program can end. There
are three verdicts, and the interpreter gives each of them a distinct
exit code.

**The program never starts (exit 2).** Delete a closing brace and the
file is not a wolf program at all:

```wolf,fail(E0202)
fn main() -> !int {
    print("almost")
    0
```

```console
$ lupin almost.lu
almost.lu: E0202: the file ends where `}` was required [gram.expr.block] at 3:6
$ echo $?
2
```

The compiler refuses the same file, and takes more room to do it:

```console
$ wolf build almost.lu
error[E0202]: this `{` is never closed
 --> ./almost.lu:1:19
  |
1 | fn main() -> !int {
  |                   ^ opened here
...
4 |
  | - the parser expected the closing `}` by here
  |

wolf build: the package does not compile; fix the errors above (`wolf --explain E0201` explains any code by name)
```

One rule, two voices: the
interpreter reports the line and column where the file ran out, the
compiler points at the opener because that is where your hands go. Neither is
paraphrasing the other, and neither of them produced a program. `wolf
--explain E0202` prints the rule behind both.

**The program runs and hits a rule (exit 3).** §1.1's scan trusted every
row to hold a comma. Here is the row that does not, and the loop alone:

```wolf,run(exit=trap(bounds))
fn main() -> !int {
    let row = "tip 100"
    var i = 0
    while row[i..i + 1] != "," { i += 1 }
    print(row[..i])
    0
}
```

```console
$ lupin scan.lu
scan.lu: trap(bounds): byte range 7..8 is outside a 7-byte string [mem.ub.defined] at 4:11
$ echo $?
3
```

This program was legal. It started, it ran, and it walked off the end of
a seven-byte string, and wolf defines that as a fault: the run stops,
names the operation, the range, the length,
and the clause it enforces. Chapter 3 gives faults like this one their
own section, and chapter 6 is where the receipt learns to say "this row
has no comma" instead.

**The program hands back an error (exit 1).** The `!` in `!int` has
been sitting in every `main` since §1.1. It says the function may
finish with an error instead of a number, and `?` is how one travels:

```wolf,run(exit=1, stdout="error: NotAnInt")
fn main() -> !int {
    let cents = "twelve".to_int()?
    print("{cents}")
    0
}
```

```console
$ lupin amount.lu
error: NotAnInt
$ echo $?
1
```

Nothing went wrong with the machine. `"twelve"` is not a number,
`to_int` said so, `?` handed the answer up,
and `main` ended the process with it. The `print` never ran. This is
the verdict chapter 6 spends itself on; here it is enough to know that
an error is a value that travels, and that a program which ends this
way ended on purpose.

### The thing you can keep

Those three verdicts are what the interpreter reports, and the
interpreter is where most of this book's programs run. What the
compiler adds is a file:

```console,from(book/ch01/s2)
$ wolf build hello.lu
$ ./hello
hello, wolf
$ echo $?
0
```

`main`'s value is the process's value in both implementations, so the
shell sees the same `0` whichever door the program came through. The
binary is built with debug information, so a debugger breaks on the
line you name in your own source, in your own file, and prints the
numbers in scope there.

**Exercise 1-6** *(spelunking · lupin)*. Delete the closing brace of a
working program's `main` and run it. Read the whole diagnostic: the
code, the message, the clause tag, the span. Then run `echo $?`. Which
of this section's exit codes is this, and why is it not the code a trap
would produce?

**Exercise 1-8** *(comprehension · wolf + lupin)*. Build the greeting,
then run `wolf build hello.lu --verbose` a second time without editing
the file. A successful build is silent by default; `--verbose` is how
you watch it work. Predict what the second build does before you run
it, and where it put what it kept.

<!-- AUDIT LEDGER (unpublished) — bs01, chapter 1. Filed to the
     `book-audit` loop on wolf-lang (labels: book-audit + one ba:*
     severity + from:bs01). Each row names the sample that exposed it,
     what the prose wanted to say, and what it had to say instead.

     rp-M1 (2026-08-11) rewrote §1.2 and §1.5 against the M1 toolchain.
     Rows the pass closed are marked [x] with what closed them; rows it
     opened are at the bottom.

     - [x] ba:doc-only — `wolf --version` prints `wolf 0.0.1 (wolfgang)`
           where the corpus's exercise 1-7 solution showed `wolf 0.0.1
           (pre-alpha)`. CLOSED at rp-M1 two ways: the solution is
           re-captured, and the runner now REPLAYS `console` blocks
           whose commands are all pinned tools (`xtask samples`,
           `console.rs`) — 92 of the book's 94 console blocks are
           executed and byte-compared on every run, the other two named
           in the log. The corpus's own EXERCISES.md files are still
           outside that lane; they are hand-checked at each bump.
     - [ ] ba:papercut — §1.2 wanted one install command per tier-1
           platform. There is no release channel and no published
           binary, so the section prints two cargo builds from source.
           The one-binary claim is no longer a promise — §1.2
           demonstrates `wolf build`, `wolf run`, and the artifact — but
           getting the binary is still a from-source story. Owner:
           c13/s66 (packaging). bs12 re-verify (pin fc7fdec/c9da6d9):
           NARROWED, not closed — wolf-lang now publishes release
           v0.1.0 with a linux-x86-64 tarball (2026-08-12), so the
           channel exists; the other three tier-1 platforms are still
           from-source, and §1.2 still prints the cargo builds.
           bs13 re-verify (pin a900b8c / lupin 0.1.14): reproduces at
           the narrowed shape — the channel still holds exactly one
           release (v0.1.0, one linux-x86-64 tarball, no new platform
           assets since 2026-08-12), and §1.2 still prints the two
           cargo builds.
           → filed wolf-lang#160 (bs12 theme batch).
     - [x] ba:papercut — §1.3's frontmatter-deps thesis. CLOSED as
           written at rp-M1: `wolf run script.lu` exists and fronts the
           section, and the Coming-from-Python box no longer promises
           frontmatter dependency resolution. The dependency half of the
           original thesis is not claimed anywhere in the chapter; if
           s53 lands it, §1.3 gains a sample, and until then the section
           makes no statement about it.
     - [x] ba:doc-only — exercise 1-5's stem in the corpus opens "The
           first program in *The C Programming Language* prints a
           Fahrenheit-to-Celsius table". K&R's first program is hello,
           world; the table is the first *table*. The chapter prints the
           corrected wording; the corpus stem and the exercise number are
           otherwise untouched, and principles/exercises/ch01 wants the
           same one-word fix. FIXED at bs12 (pin fc7fdec/c9da6d9): the
           corpus stem now carries the chapter's wording ("The first
           table … converts Fahrenheit to Celsius"); solutions
           regenerated by `cargo xtask backmatter`.
     - [ ] ba:papercut — the `else 0` in the cold open is chapter 6
           vocabulary appearing in chapter 1, because `to_int()` returns
           an error row and Part 1's first program has to add numbers.
           The alternative (leaving the row unhandled) is `unsupported`
           under lupin: `+` is not defined on i64 and error NotAnInt.
           Wanted: a std spelling that yields a plain int with a
           documented default, or an honest reason there is none.
           bs12 re-verify (pin c9da6d9): reproduces — no `to_int_or`
           (`unsupported: `str` has no method `to_int_or``); `else 0`
           is still the only spelling.
           bs13 re-verify (pin a900b8c / lupin 0.1.14): reproduces —
           lupin: `unsupported: `str` has no method `to_int_or` in
           this machine's std subset` (exit 4); wolf run: "cannot
           compile this yet — this `str` method (outside the builtin
           set)". `else 0` is still the only spelling.
           → filed wolf-lang#154 (bs12 theme batch).
     - [ ] ba:diagnostic — `lupin` reports a static rejection as
           `E0202: the file ends where `}` was required … at 50..50`,
           a byte offset with no line, column, or source excerpt, while
           the compiler renders the same code with both spans. §1.5 turns
           that into a teaching point about two voices; a learner reading
           only the interpreter's line has to count bytes. bs12
           re-verify (lupin 0.1.13 pin c9da6d9): reproduces byte-for-
           byte — `E0202: … [gram.expr.block] at 50..50`, offset only.
           bs13 re-verify (pin a900b8c / lupin 0.1.14): reproduces —
           `E0202: the file ends where `}` was required
           [gram.expr.block] at 51..51`, offset only, no line/column
           or excerpt; wolf renders the same file with spans on both
           the opening `{` and the expected `}`.
           bs17 re-verify (pin addcd7f / lupin 0.1.16): NARROWS — is27
           renders every file-door line `line:col` (§1.5's transcript
           now reads `[gram.expr.block] at 3:6`; an editor jumps, no
           one counts bytes). Still a one-line report: no source
           excerpt or caret, which is the remainder the row holds on.
           → filed wolf-lang#158 (bs12 theme batch).
     - [x] ba:doc-only (pin) — the tools on disk were ahead of
           `wolf-toolchain.toml`. CLOSED at rp-M1: the pin is bumped to
           wolf-lang 29a9d9c (M1) and lupin v0.1.4, and the drift that
           made the row necessary is now caught mechanically — §1.2's
           `--version` block and the colophon's are replayed console
           blocks like any other.
     - [ ] ba:doc-only (rig, not language) — the pin file marks the
           `wolf-repl` lane `pending = "is08"`, so this chapter's two
           transcripts are counted and not replayed. `lupin repl
           --script` already exists at the pinned interpreter and
           byte-replays every transcript in the book (8/8 OK by hand at
           the rp-M1 bump). The replay lane can be wired now instead of
           at is08; that is a runner change, and rp-M1 spent its runner
           budget on the console lane. bs12 re-verify: reproduces —
           is08 shipped long ago (the interpreter is at is16+), the pin
           file still says `pending = "is08"`, and `cargo xtask
           samples` still reports "12 wolf-repl block(s) counted, not
           replayed". Rig work, not a toolchain gap.
           bs13 re-verify (pin a900b8c / lupin 0.1.14): reproduces —
           `wolf-toolchain.toml` still reads `pending = "is08"` under
           `[wolf-repl]`; the transcripts remain counted, not
           replayed.

     Opened at rp-M1:

           → filed wolf-lang#161 (bs12 theme batch).
     - [ ] ba:blocker (conformance, not the book) — the two
           implementations disagree on PROCESS EXIT CODES for two of the
           three verdicts. A static rejection is exit 2 under lupin and
           exit 1 under `wolf build`; a trap is exit 3 under lupin and
           134 under a compiled binary, whose whole message is
           `wolf-trap: overflow` — kind only, no operation, no value, no
           clause. `[conf.trap.set]` pins trap KINDS as the comparison
           alphabet and says nothing about the exit code a conforming
           implementation must produce, so neither tool is wrong today.
           The book has scoped its exit-code list to the interpreter's
           runs (front/notation, §1.5) rather than teach a number that
           changes with the door. Wanted: a `[conf.exit]` clause fixing
           0/N, error, static-rejection, and trap codes for every
           implementation, and a native trap reporter that says what
           lupin's says. Owner: spec/05 + c07 runtime. bs12 re-verify
           (pin fc7fdec/c9da6d9): reproduces on every count — static
           rejection is exit 2 (lupin) vs 1 (wolf build); a runtime
           overflow is exit 3 with the full line (lupin) vs
           `wolf-trap: overflow`, exit 134 (native). Also found while
           probing: an out-of-range `i32` literal ICEs `wolf build`
           (`wir verify error [const-range] … iconst.i32
           9223372036854775807`) instead of drawing a static E-code.
           bs13 re-verify (pin a900b8c / lupin 0.1.14): reproduces on
           every count — static rejection exit 2 (lupin) vs 1 (wolf
           build); an `i32` overflow is exit 3 with the full
           [arith.checked] line (lupin) vs `wolf-trap: overflow`,
           exit 134 (native, kind only); the out-of-range `i32`
           literal still ICEs (`wir verify error [const-range] …
           iconst.i32 9223372036854775807`, exit 2) with no static
           E-code.
           bs17 re-verify (pin addcd7f / lupin 0.1.16): NARROWS on
           three of four counts. The exit-code half is RULED, not
           fixed: D60 makes each status a documented per-machine fact
           (native 134, lupin 3; [conf.trap.exit], appended by s125)
           and pins the KIND as the only comparison surface — the
           "wanted: a [conf.exit] clause" ask is answered, in the
           per-machine direction. The native reporter now names its
           site (`wolf-trap: overflow` + `  at ./file.lu:l:c`,
           [conf.trap.report]) though still not the value, type or
           clause. The out-of-range `i32` literal HEALS: it draws
           static E0415 (`does not fit i32`, with the range and an
           annotate help), exit 1, no ICE. Static rejection is still
           exit 2 (lupin) vs 1 (wolf build) — the row holds on that
           and on the native line's missing riches.
           → filed wolf-lang#150 (bs12 theme batch).
     - [ ] ba:papercut (debug) — `wolf build` produces a debuggable
           binary and the s31 gate proves it (break, step, backtrace,
           locals). Two warts kept the break-and-print moment off the
           page. First, the linked binary carries a `.debug_gdb_scripts`
           section inherited from the Rust runtime staticlib, so gdb
           opens every wolf program with `warning: Missing auto-load
           script at offset 0 …` and the reader's absolute path.
           Second, `break main` stops twice — once in the C entry shim,
           once in wolf's `main` — so the honest incantation is `break
           file.lu:N`. With both worked around, the session is clean and
           correct: `info locals` prints `a = 340 / b = 275 / total =
           615` on a three-line program. §1.5 states the capability in
           prose and prints no transcript, because a transcript
           containing a warning about an unrelated missing file is not
           the product. Owner: wolf-lang (drop the inherited section;
           consider a `main` that gdb resolves once). bs12 re-verify
           (pin fc7fdec/c9da6d9): HALF-HEALED — the binary no longer
           carries `.debug_gdb_scripts` and gdb opens it with no
           warning; `break main` still stops twice (2 locations), so
           the row narrows to the double-stop.
           bs13 re-verify (pin a900b8c): reproduces at the narrowed
           shape — no `.debug_gdb_scripts` section and gdb opens the
           binary with no warning; `break main` still resolves to
           "(2 locations)" (entry shim + wolf `main`), so the
           double-stop stands.
           → filed wolf-lang#163 (bs12 theme batch).
     - [ ] ba:papercut (debug, scope) — DWARF at this pin carries
           scalar locals only; `print who` on a `str` answers `No symbol
           "who" in current context`. Chapter 1's greeting binds exactly
           one local and it is a string, which is why §1.5's debug
           sentence promises numbers rather than values. Owner: s54/s58
           (struct/str DIEs and pretty-printers). bs12 re-verify (pin
           fc7fdec): reproduces — `info locals` prints the three ints,
           `print who` on a `str` is still `No symbol "who" in current
           context`.
           bs13 re-verify (pin a900b8c): reproduces — at a line
           breakpoint `info locals` prints `a = 340 / b = 275 /
           total = 615`; `print who` on a `str` is still `No symbol
           "who" in current context`.
           → filed wolf-lang#163 (bs12 theme batch).
     - [ ] Running example recorded here (STYLE.md §The running example,
           which assigns the choice to the part's first sprint): Part 1's
           running example is the receipt — `receipt.lu`, §1.1 — a rows
           summarizer that trusts its input. It advances in ch2 (parsing
           the rows byte-honestly), and TOC §6.4 hardens it construct by
           construct before the wordcount capstone. The sprint file could
           not be edited from the book repo; bs02 inherits this line.

     - [ ] ba:papercut (bs11, upstream) — a failed `wolf build` now closes
           with "(`wolf --explain E0201` explains any code by name)", and
           `E0201` is hard-coded as the example regardless of what the
           run reported. §1.5's block prints that line directly under an
           `E0202` error, so the page invites the reader to explain a
           code they did not see; the prose one paragraph later names
           `E0202` correctly, which is the page working around the tool.
           The hint is a good addition and its example should be the
           run's first code. bs12 re-verify (pin fc7fdec): reproduces —
           an E0202 failure still closes with "(`wolf --explain E0201`
           explains any code by name)".
           bs13 re-verify (pin a900b8c): reproduces — an E0202 failure
           (unclosed `{`) still closes with "(`wolf --explain E0201`
           explains any code by name)"; the hint's example code is
           still hard-coded, not the run's first code.
           → filed wolf-lang#157 (bs12 theme batch).
-->
