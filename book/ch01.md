# 1. Hello, Wolf

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 1.1 A program worth keeping

<!-- promise: run a complete 15-line script that does something you would keep, before any installation ceremony. -->

## 1.2 Installing the one tool

<!-- promise: install `wolf` on your platform and verify it; the book never asks for a second tool. -->

## 1.3 Scripts before projects

<!-- promise: run `.lu` files directly with `wolf run`, with dependencies declared in the file itself: no venv, no requirements.txt, no drift. -->

## 1.4 The REPL: a spec you can interrogate

<!-- promise: start the interpreter's session, try expressions, and learn `:mem` exists before you need it. -->

## 1.5 What `run` was doing for you

<!-- promise: meet `wolf build` and the shape of a compiled artifact. -->
