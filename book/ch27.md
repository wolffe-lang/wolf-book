# 27. `rpn`, twice

<!-- STUB (bs10): this chapter's prose waits for its gate; the gate
     note and the reason are in principles/TOC.md §Deltas. Structure
     below is TOC.md structure; TOC.md wins on conflict. -->

## 27.1 A stack and a switch

<!-- promise: read the C twin, and find the value that means both "zero" and "empty". -->

## 27.2 The operand stack as a `List`

<!-- promise: build the stack, push, pop. -->

## 27.3 Parse errors as payload-carrying tags

<!-- promise: replace the error flag with a row, and propagate it with `?`. -->

## 27.4 The operator dispatch as a `match`

<!-- promise: complete the calculator. -->

## 27.5 Develop interpreted, ship compiled

<!-- promise: run the same source under lupin while writing it and `wolf build` it when done. -->
