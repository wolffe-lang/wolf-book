# 13. Dividing one job

Chapter 12 closed by naming a subject it had not covered. Everything
concurrent in the three chapters behind us was concurrent because two
*different* jobs were in flight — one reader totalling words while
another looked for the busiest document. Cutting a single job into
pieces is a different question, and it begins in the same place every
time: the pieces have to add up somewhere.

<!-- HOLD (rp01) — §13.1 `par` is not in this chapter, and the reason is
     recorded here because this is the one place a future author looks.

     `par` has no surface at the pin in wolf-toolchain.toml. Under lupin
     `xs.par(fn(x) x * x)` answers `unsupported: `List` has no method
     `par` in this machine's std subset` (exit 4); under wolf the whole
     program answers `unsupported` at `resolve`. `[conc.task.par]`
     specifies it; nothing implements it. bs06 held the whole chapter on
     this and on E1101; E1101, E1102 and E1103 all landed, in both
     tools, so §13.2 ships and §13.1 does not.

     The numbering does not close up, and that is deliberate. book/ch06.md
     §6.5's boxed promise names §13.1 as the place the wordcount's one-call
     diff is checked, and that box is shipped, reader-facing text. So
     §13.2 keeps its number, §13.1 stays vacant, and the day `par` lands
     the section arrives in the slot ch06 already points at — nothing is
     renumbered, and no shipped page has to be edited to make room.
     principles/TOC.md carries the held row marked `(held)`, which is the
     spelling `cargo xtask verify-docs` skips; drop the marker when the
     section is written.

     What lands with it: exercises 13-1, 13-6 and 13-8 (all three are
     `par`'s — two pending rows on samples-pending.toml and one design
     stem about `par`'s decomposition contract), and the wordcount diff
     ch06 §6.5 is owed, in the form that box specifies: the same `count`
     and `top`, the same `main`, one call changed.
-->

The shelf's word count is one job. Its loop runs on one core, and the
obvious way to spread it is to give the loop to a reader:

```wolf,fail(E1101)
struct Doc { title: str, words: int }

fn main() -> !int {
    let store = freeze region {
        var docs = List[Doc]()
        (mut docs).push(Doc { title: "regions", words: 900 })
        (mut docs).push(Doc { title: "moves", words: 640 })
        docs
    }
    var total = 0
    scope readers {
        readers.spawn(fn() { for d in store { total += d.words } })
    }
    print("{total} words")
    0
}
```

That program does not run, in either implementation, and the rest of
this chapter is why.

## 13.2 The race that does not compile

Here is the compiler on it:

```diagnostic,from(book/ch13/s1)
error[E1101]: this task writes to `total`, which it captures from the enclosing function
  --> ./s1.lu:15:47
   |
15 |         readers.spawn(fn() { for d in store { total += d.words } })
   |         ----------------------------------------------------------- the task's closure captures it at this spawn
   |                                               ^^^^^ tasks cannot mutate captured state
   |
   = note: task captures are copies, `imm` shares, or region moves (D14) — never mutable windows
     onto the parent's locals; two tasks writing one binding is the data race the memory
     model forbids. Three ways out: send results over a `channel` and let one owner mutate;
     guard truly shared state with a `Mutex` acquired in a `when` block; or, for loop-shaped
     work, use `par` with a reduction.

warning[W1101]: this write to `total` stays inside the task
  --> ./s1.lu:15:47
   |
15 |         readers.spawn(fn() { for d in store { total += d.words } })
   |                       -------------------------------------------- the closure captured it at spawn
   |                                               ^^^^^ lands on the task's own copy
   |
   = note: task captures copy (or move); the enclosing binding never sees this assignment. Send the
     result over a channel, or return it through the scope's join.
```

And here is the interpreter, which does not run it either:

```console
$ lupin divide.lu
divide.lu: E1101: this task writes to `total`, which it captures from the enclosing function: unsynchronized mutable capture across tasks (D14 — copy, share `imm`, or `move`; a `sync` type mediates shared writes) [conc.task.spawn] at 347..352
$ echo $?
2
```

Two tools, one code, one span — `347..352` is the same five bytes the
compiler underlines. This is the part of the book where the differential
stops being an argument and becomes a boring fact: the specification
says `total` may not be written from a task, and both readings of the
specification say so before the program starts, in the same words about
the same bytes. Chapter 3 set the pattern as two tools, one rule, and
two enforcement moments — the compiler proving it before the program
starts, the interpreter catching it in the act. Here the two moments
collapse into one, which is what a rule looks like when both readings
have finished arguing about it.

Now read *where* the error points, because it is not where you would put
it. There is one task in that program. One task cannot race with
anything — there is nothing to interleave with, and the sum it computes
would be arithmetically correct. The compiler rejects it anyway, at the
capture, and the phrase it uses is `tasks cannot mutate captured state`.

That is the rule chapter 10 stated positively and this chapter cashes.
D14 gives a task body three ways to touch anything it did not create: it
copies, it reads `imm` data, or it takes a region by move. A mutable
window onto the parent's local is not on the list, and the check does
not wait to see how many tasks want one. Wolf is not detecting your race.
It is declining to compile the shape a race is made of, and the shape has
one instance in it.

The consequence is worth stating plainly, because it is the pitch. Add
the second reader and you have written the classic lost-update bug —
two tasks, one accumulator, an answer that depends on timing. You will
never see it. The program that would have contained it does not build,
and it did not build back when it was still a one-task program that
worked. Exercise 13-3 is the two-task version, and part of what it asks
is how much the two tools say about it.

`W1101` is the same fact from the other side, and it is what makes the
rejection teachable rather than merely correct. The closure captured
`total` by value, so if the write were allowed to stand it would land on
the task's private copy and the parent's `total` would print `0` — not a
race at all, a silently wrong answer. The error says the shape is
forbidden; the warning says what the shape would have done. Read them
together and you know both that you may not write it and why you did not
want to.

### The two answers this program has

The note ends with three ways out. Here are the two that answer the
program above, and you have already met both.

The first is a channel, which is the route §10.1 said existed and left
to chapter 12 to build. Each reader computes its piece and sends it; one
owner — `main`, after the join — does the adding:

```wolf,run(exit=0, stdout="1540 words")
struct Doc { title: str, words: int }

fn main() -> !int {
    let store = freeze region {
        var docs = List[Doc]()
        (mut docs).push(Doc { title: "regions", words: 900 })
        (mut docs).push(Doc { title: "moves", words: 640 })
        docs
    }
    let parts = channel[int](2)
    scope readers {
        readers.spawn(fn() { parts.send(store[0].words) })
        readers.spawn(fn() { parts.send(store[1].words) })
    }
    var total = 0
    for _ in 0..2 { total += parts.recv() else |_| { return 1 } }
    print("{total} words")
    0
}
```

```console
$ lupin fanout.lu
1540 words
```

The accumulator did not disappear; it moved. `total` is still a plain
`var` and it is still added to twice, but both additions now happen in
one task, after the brace that guarantees the other two are finished.
Nothing is guarded because nothing is shared: the readers own their
partial sums, the channel carries copies, and `main` owns the total.
This is the shape to reach for by default, and the reason is that it has
no critical section to get wrong.

The explorer agrees, over every interleaving it can reach:

```console
$ lupin conform-run fanout.lu --explore=16
fanout.lu: explored 2 schedule(s) in 2 execution(s) (DPOR; 0 slept, 0 pruned), frontier closed
  outcomes: 1 distinct — observably deterministic (every schedule agrees)
    exit(0) ×2 stdout=1540 words\n leaks=0 forest=ok — replay: --seed=0
  deadlocks: 0 · races: 0 · max depth: 3 decision(s)
```

`outcomes: 1 distinct` is §17.2's verdict arriving early: two schedules
exist, both were run, and the program has one answer. Addition commutes,
so the order the two pieces arrive in cannot be observed — which is a
property of the operation you combined them with, not of the language,
and is the first thing to check when a divided job starts giving two
answers.

The second way out is for state that is genuinely shared, where no
amount of rearranging makes one owner enough. Two numbers that must
agree with each other are the honest case, and §12.4 built the
instrument:

```wolf,run(exit=0, stdout="1540 words in 2 documents")
fn main() -> !int {
    let words = Mutex(0)
    let docs = Mutex(0)
    scope readers {
        readers.spawn(fn() { when (words, docs) { words += 900; docs += 1 } })
        readers.spawn(fn() { when (docs, words) { words += 640; docs += 1 } })
    }
    when (words, docs) { print("{words} words in {docs} documents") }
    0
}
```

```console
$ lupin guarded.lu
1540 words in 2 documents
```

Two things about that program are the point. The two `when` lines name
their operands in opposite orders and it does not matter, for the reason
§12.4 gave: the set is acquired whole, in a canonical order the program
never writes. And `when` needs a *set* — two operands, minimum, which is
why this example guards two counters rather than one. That is not a
limitation to work around. A single number under a lock is a job for the
program above: one owner and a channel, with no lock at all.

### The same law, one boundary over

The rule the capture check enforces is not really about closures. It is
about what may exist in two places at once, and a channel is the other
place a value can go:

```wolf,fail(E1102)
struct Doc { title: str, words: int }

fn main() -> !int {
    let inbox = channel[List[Doc]](1)
    0
}
```

```diagnostic,from(book/ch13/s4)
error[E1102]: `List[Doc]` cannot be sent through a channel
 --> ./s4.lu:7:25
  |
7 |     let inbox = channel[List[Doc]](1)
  |                         ^^^^^^^^^ not a sendable payload type
  |
  = note: channel payloads must be `Copy` data, `imm` data, a region value (the send is its affine
    move), or a `sync` type ([conc.chan.type]) — sending anything else would give two tasks
    one mutable value. D14's verbs are the ways out: `move` the data into a region and send
    the region, `freeze` it into shareable `imm` data, or guard it with a `Mutex`.
```

```console
$ lupin inbox.lu
inbox.lu: E1102: `List[Doc]` cannot be sent through a channel: a payload must be `Copy`, `imm`, a moved region, or a `sync` type — a bare region-interior `List` is none of those. Send the region instead [conc.chan.type] at 83..92
$ echo $?
2
```

Same two tools, same agreement, same clause vocabulary — and notice
that no `send` appears in the program. The channel's *type* is the
claim, so the declaration is where the claim is checked, which means a
sendability mistake is found at the line where you designed the queue
rather than at the line where you finally used it.

Read the four admitted classes back slowly. `Copy` data, `imm` data, a
region that moves, and a `sync` type: that is D14's list again, the one
§10.1 gave for what a task body may touch and §12.1 taught three of by
showing them work. Two error codes, two boundaries, one rule — and the
same note under both, offering the same three ways out. A language that
had written this twice would have left you a way to smuggle a mutable
value across one boundary by declaring it at the other.

**Exercise 13-2** *(fingers · lupin)* — Nine numbers, squared and
summed, on one core: build the list, square each into a second list, add
them up, print the total. Run it and keep the program. The number it
prints is the number every divided version of this job has to agree
with, and a divided job that does not reproduce its sequential answer is
not faster — it is wrong.

**Exercise 13-3** *(comprehension · wolf + lupin)* — Two tasks
increment a captured `var`:

```wolf,fail(E1101)
fn main() -> !int {
    var hits = 0
    scope s {
        s.spawn(fn() { hits += 1 })
        s.spawn(fn() { hits += 1 })
    }
    hits
}
```

Before running it, predict the three fixes the note offers and which two
apply here. Then run it under both tools and account for the difference
in what they print — the codes and the spans agree, and the amount of
output does not. Which tool tells you about the second `spawn`, and what
does the extra warning on it say that `W1101` did not?

**Exercise 13-4** *(spelunking · lupin)* — Two programs differ in one
`print`. Explore both and read the verdicts: `ex13-4a.lu` prints the
arrival order and the sum, `ex13-4b.lu` prints only the sum, and one of
them is `SCHEDULE-DEPENDENT`. Same tasks, same channel, same schedules.
Why do the verdicts differ, and what does that mean for how you design a
divided job's output?

## Where this leaves the shelf

The count is divided and the answer is still 1540. What made that
uneventful was not care; it was that the two shapes available to write
it in are both shapes the machine can check. A task that touches only
what it copies, reads only what is frozen, and sends its answer home
needs no reasoning about ordering at all, and the explorer will say so
in one line. A task that must share needs the whole lock set at once,
and there is no spelling for taking them one at a time.

What you cannot do is the third thing: reach out of the closure and add
to the parent's variable. C and Go both let you write that and find out
later — Go's race detector will tell you about it in a run that happens
to hit it, which is a different offer from being unable to write it.
Rust refuses it too, and refuses it earlier than its reputation
suggests: `Send` and the borrow checker close the same hole with
different machinery, and wolf's claim here is not that it caught
something Rust misses. The claim is narrower and it is about the
diagnostic. The accumulator in the closure is not a bug this book
teaches you to avoid. It is a program that does not build, in both
implementations, with a note that names the three shapes you meant
instead.

**Exercise 13-5** *(extension · lupin)* — grep, wolfished: write
`grep(text, pattern) -> List[str] ! {EmptyPattern}` returning the
matching lines. Substring search is yours to write with byte slices. Why
is the empty pattern an *error* here, when POSIX grep happily matches it
everywhere?

**Exercise 13-7** *(comprehension · lupin)* — One Euler step for two
bodies on a line, gravity only, equal masses. Before running: what is
`v1 + v2` after the step, and is your answer exact or approximate for
f64 arithmetic?

<!-- AUDIT LEDGER (unpublished) — rp01, chapter 13. Filed to the
     `book-audit` loop on wolf-lang (labels: book-audit + one ba:*
     severity + from:rp01).

     - [x] ba:blocker (found at rp02's pin bump while writing chapter 30
           §30.4, and it is the loudest thing THAT sprint found — it is
           filed here because the claim at risk is §13.2's) — **E1101 does
           not see `f(mut x)`.** The check catches an assignment to a
           captured binding (`total += d.words`, `hits += 1`) and does not
           catch a `mut` argument at a call site. This program compiles
           clean on BOTH implementations, with no error and no W1101:

             fn main() -> !int {
                 var xs = List[int]()
                 scope s {
                     s.spawn(fn() { (mut xs).push(1) })
                     s.spawn(fn() { (mut xs).push(2) })
                 }
                 xs.len
             }

           Two tasks, one captured `List`, unsynchronized writes — the
           shape [conc.task.spawn] forbids and the shape §13.2 says wolf
           declines to compile. Both tools admit it, and then they
           disagree about what it means: natively `xs.len` is **2** (the
           parent sees both pushes, so the capture is a real shared
           mutable window and this is the data race D14 exists to
           prevent), under lupin it is **0** (capture by copy, both writes
           lost). Neither is wrong about the other's behavior — the
           program has no defined meaning, which is why it should not
           compile.

           What is at risk on the page: §13.2's "It is declining to
           compile the shape a race is made of, and the shape has one
           instance in it." That sentence is true of the program the
           section prints and of the rule it cites, and it overstates the
           *check* by exactly this hole. No shipped prose was edited at
           rp02, because editing it would mean writing a caveat about a
           checker gap into reader-facing text, which is what this ledger
           exists to avoid. If the hole is not closed, §13.2 needs its
           last paragraph narrowed from a claim about shapes to a claim
           about writes, and exercise 13-3 gains a second half. Owner: the
           capture check in both implementations; the divergence is
           secondary to the missing rejection and closes with it.

           Re-measured at the bs11 pin, from a real run rather than from
           this note: the program above still compiles clean, `wolf build`
           produces a binary that prints **2**, and lupin runs it to
           **0**. The upstream issue is open at the sha this book pins
           (wolf-lang#71), so the row stands and §13.2 is not edited. The
           edit this row prescribes is one paragraph, and it should be
           made only if the hole outlives the fix in flight.

           Re-measured again at the pass-three bump (wolf 2c6980a): the
           compiler half CLOSED. wolfc now answers `error[E1101]: this
           task writes to `xs`, which it captures from the enclosing
           function` at the first `(mut xs)` — wolf-lang#71's fix
           arrived, the hole did not outlive it, and §13.2's paragraph
           keeps its claim without a caveat, exactly as this row hoped.
           What did NOT close: lupin 0.1.10 still runs the program to a
           write-dropping exit 0, so the divergence inverts instead of
           vanishing — one machine rejects the shape, the other silently
           loses both writes. Filed as wolf-interp#30; the row narrows
           to the interpreter's half and stays open on it. The pending
           rows this chapter DOES hold (13-1, 13-6, on `par`) are
           untouched.
           bs12: box ticked, FULLY HEALED at pin fc7fdec/c9da6d9 — the
           remaining interpreter half closed: lupin now rejects the
           `(mut xs).push` capture shape with the same E1101 wolf
           prints (wolf-interp#30's fix landed). Both machines refuse
           the program this row minimized; §13.2's claim holds without
           caveat on either tool.
     - [x] ba:blocker (differential, and the loudest thing this sprint
           found) — lupin's `--explore` path does not run the E11xx
           static checks. Every one of the three programs this chapter
           rejects is *explored* and blessed by the same binary that
           refuses to run it:

             $ lupin conform-run divide.lu
             divide.lu: verdict=fail(E1101) phase_reached=resolve …
             $ lupin conform-run divide.lu --explore=8
             divide.lu: explored 1 schedule(s) in 1 execution(s) …
               outcomes: 1 distinct — observably deterministic (every schedule agrees)
                 exit(0) ×1 stdout=0 words\n leaks=0 forest=ok — replay: --seed=0

           The verdict is not merely permissive, it is wrong twice: the
           program is a static rejection, and the run it certifies as
           deterministic is the lost-update run W1101 describes (`0
           words`). `--seed` is fine — it reaches `fail(E1101)`. The
           chapter therefore never points `--explore` at a rejected
           program, and §17.2's instrument keeps a hole in it until the
           explore path shares the resolve rung's checks. Same shape for
           E1102 and E1103.
           HEALED at bs12 (pin c9da6d9): `conform-run --explore` now
           refuses statically-rejected programs — "cannot explore —
           E1101 … statically rejected; the explorer runs the same
           admission ladder as `run`". §17.2's instrument no longer
           blesses programs the resolve rung rejects.
     - [x] ba:blocker (checking) — CLOSED IN BOTH TOOLS at this pin, and
           recorded because chapter 12's ledger asked for it by name:
           E1103 (a `when` lexically inside another `when`) now exists,
           in wolf and in lupin, with the same code and the same span
           (`130..152` on the probe both were given). §12.4's argument —
           canonical whole-set acquisition rules out cycles only if no
           task can hold one set while acquiring another — is complete
           at this pin for the first time. The editorial half is not
           this sprint's: §12.4 is shipped prose and states the
           demonstrable claim (E0201, no one-operand `when`) without
           claiming nesting is rejected, so it is sound as written and
           printing the new sample inside it is bs11's review decision,
           exactly as chapter 16's E1102 row was left. Chapter 12's
           highest-priority row is answered; what remains is a page
           edit, not a gap.
           bs12: box ticked — re-verified at pin: nested `when` is
           E1103 from lupin with [conc.when.nonest]; the explore-path
           differential this row flagged is CLOSED too (row above).
     - [ ] ba:papercut — the E0201 hint has no referent. `when (m) { … }`
           says "for one, call the method on the sync type", and a
           `Mutex` has no methods: `m.set(900)` is `unsupported: `Mutex`
           has no method `set` in this machine's std subset`, and
           `print("{m}")` renders `mutex#0`. So the diagnostic's own
           suggested spelling for the single-lock case cannot be
           written. This chapter's guarded example therefore guards two
           counters — which is the better example anyway, and the prose
           says why — but the hint should either name a method that
           exists or stop offering one.
           bs12 re-verify (pin c9da6d9): reproduces — lupin's hint
           still says "call the method on the sync type" and `Mutex`
           still has none (wolf's side already states the rule).
           → filed wolf-lang#158 (bs12 theme batch).
     - [ ] ba:papercut — the note's third fix names `par`, which no
           implementation has. The diagnostic is a primary source and is
           printed whole, so the page carries a suggestion it cannot
           walk the reader through; the section names the two it can
           answer and does not count aloud. Nothing to fix in the
           prose — the row exists so the day §13.1 is written its author
           knows the sentence is already on the page, and joins it up.
           bs12 re-verify (pin fc7fdec): reproduces — the note still
           closes "…use `par` with a reduction", and §13.1 is still
           held. The marker stands for §13.1's author.
           → filed wolf-lang#157 (bs12 theme batch).
     - [x] ba:diagnostic — E1101 repeats its whole four-line note once
           per offending `spawn`. Two tasks writing one accumulator is
           57 lines of output for one mistake with two instances, which
           is why this chapter's printed program has one task and
           exercise 13-3 has two. A second instance wanting `= note: (as
           above)` is the ordinary Rust-style treatment and would make
           the two-task program printable.
           HEALED at bs12 (pin fc7fdec): two offending spawns now print
           two E1101s and the four-line note ONCE — 22 lines total for
           the two-task program, which is printable.
     - [ ] Contract delta (bs06 target 4) — the flagship demo is half
           landed. "The racy variant rejected" ships, with both tools
           agreeing; "each suggested fix compiling green" ships for two
           fixes of three, the third being `par`'s. The `par` half of
           the demo — take the sequential word count, add `par`, watch
           it scale — is §13.1's and is held with the section.
     - [ ] Contract delta (bs06 non-target) — the chapter prints no Go
           side-by-side. Same reason chapter 10's ledger gives: there is
           no Go toolchain in the contrast lane, and what is being
           contrasted is what Go *permits*. Unlike chapter 10 this
           chapter does not even need the box, because the contrast is
           already on the page in two voices.
     - [ ] Running example — the shelf continues. `Doc { title, words }`
           and the frozen store arrive unchanged from chapters 7–12;
           what this chapter adds is the count divided over its
           documents. The guarded example is the one program that leaves
           the shelf behind, because two counters that must agree is a
           shape the shelf does not have.
     - [ ] Cross-reference accounting (TONE.md tense discipline) — five
           backward references (§12.4 twice, chapter 10's route home,
           §12.1's payload classes, §17.2's verdict), all to shipped
           pages, none falsifiable. Zero forward references: the one the
           chapter would naturally make — to `par` — is exactly the one
           the tense discipline forbids, so the section that would
           receive it is not mentioned in reader-facing text at all.
     - [ ] Exercise accounting — five of eight stems are printed: 13-2,
           13-3, 13-4 in §13.2, and 13-5, 13-7 in the chapter batch.
           13-1 and 13-6 are `par`'s and hold their rows on
           samples-pending.toml; 13-8 is a design stem whose subject is
           `par`'s decomposition contract, so it is written, on file,
           and not printed. All three land with §13.1. Note that 13-2 is
           printed in §13.2 while the generated index assigns it to
           §13.1 — the ch18 precedent (18-6) — because its subject, the
           sequential baseline a divided job must reproduce, is the
           material §13.2 opens on.
     - [ ] Recorded as good news — the two-tool agreement is byte-level.
           E1101, E1102 and E1103 all produce the same code, the same
           clause tag, and the same span offsets from both
           implementations at this pin. The book has spent twelve
           chapters saying the two readings have to keep matching; this
           is the first chapter where the claim is checkable on three
           consecutive diagnostics without a single caveat.
-->
