# 13. Parallel iterators

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

<!-- HOLD (bs06) — this chapter's gate has not opened, and the reason is
     recorded here because it is the one place a future author looks.

     bs06 shipped chapters 10, 11, and 12 and held this one. Both of its
     sections need surface that neither implementation has at the pin in
     wolf-toolchain.toml:

     - §13.1 needs `par`. `xs.par(fn(x) x * x)` answers `unsupported:
       `List` has no method `par` in this machine's std subset` (exit 4)
       under lupin, and the compiler answers `unsupported` at `resolve`
       for the whole concurrency surface. `[conc.task.par]` specifies it
       (a desugaring to scope-and-spawn, adding shape and no semantics);
       nothing implements it.
     - §13.2 needed E1101, and at the bs10 pin bump it HAS IT. bs06
       recorded that unsynchronized mutable capture across tasks was
       checked by neither tool — the store-buffer program ran under
       lupin, to exit 0, having silently lost both increments to
       capture-by-copy. The compiler now rejects it, and the diagnostic
       arrives with exactly the three fixes this section's promise names
       (a channel with one owner mutating, a `Mutex` acquired in a
       `when`, `par` with a reduction), plus W1101 and W1102 on the
       dynamic half. Exercise 13-3 was the FLIP that reported it; its
       row left samples-pending.toml and its solution now shows the real
       rejection. §13.2 is therefore writable, and the author who writes
       it should take the diagnostic from `principles/exercises/ch13/`
       rather than re-deriving it.

     The chapter still waits, because a chapter waits for its gate
     *whole* and §13.1's gate is `par`, which has not landed. Splitting
     it would publish §13.2's anchors and then renumber them when
     §13.1 arrives, which is the one thing the numbering rules forbid.

     Consequences a future author needs to know:

     1. **The §13.1 obligation is unpaid.** book/ch06.md §6.5 carries a
        boxed promise — the wordcount loop parallelizes by changing one
        call, checkable at §13.1, "and the sprint that writes chapter 13
        is contractually bound to the diff. Hold us to it." bs06 is that
        sprint and cannot cash it. The promise is not broken (nothing
        has shown the claim false) and not kept (nothing has shown it
        true); it is outstanding, and the box stays as written, which is
        row 26 of docs/audit/promissory-prose-audit.md. The first sprint
        whose gate opens on `par` owes the diff, and owes it in the form
        the box specifies: the same `count` and `top`, the same `main`,
        one call changed.
     2. **The exercise corpus is ready.** All eight ch13 stems and their
        solution prose are written in principles/exercises/ch13/, and
        13-1, 13-3, and 13-6 have directive-headed programs on disk
        stating the outcomes their features will produce. The mechanism
        worked as designed: 13-3 flipped at the bs10 bump and its row is
        gone. 13-1 and 13-6 remain rows on samples-pending.toml, both on
        `par`, so the day it lands the runner reports two more FLIPs and
        this whole hold note comes down with them.
     3. **What did ship, so this chapter does not repeat it.** §12.2
        teaches the seeded scheduler and the explorer's verdicts, and
        §13.2's determinism material (exercise 13-4's pair) leans on
        that section. Chapter 12 closes by naming the gap this chapter
        fills — turning one job into many rather than running several
        different jobs — without naming a schedule.
-->

## 13.1 `par`

<!-- promise: parallelize Part 1's wordcount by changing one call (the promise, kept). -->

## 13.2 The race that does not compile

<!-- promise: introduce a real race and read the rejection with its three suggested fixes. -->
