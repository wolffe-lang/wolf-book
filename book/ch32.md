# 32. Coda: the allocator you never needed

<p class="epigraph">Tchaikovsky's Pathétique ends with a movement that goes
down rather than up, and its last bars are the double basses alone,
underneath everything: the piece does not conclude, it reaches the floor.</p>

This is the floor. Every program in this book has stood on an allocator and
not one of them has mentioned it.

*The C Programming Language* ends at §8.7 with a storage allocator, and it
ends there for a reason that has nothing to do with wanting a grand finale.
In C, allocation is a library written in C. A reader who does not understand
`malloc` cannot read the standard library, cannot read a heap dump, and
cannot be handed a `char *` with any confidence about what happens next.
Kernighan and Ritchie put it last because it was the hardest thing they had
to say, and because they had to say it.

Wolf does not have to. So this chapter builds one anyway.

## 32.1 Why C ends here

Chapter 8 answered the allocation question with a granule instead of a
function. A region is the allocator: you allocate into it freely, nothing in
it outlives anything else in it, and the closing brace is the free. Chapter
28 is what that looks like at the bottom of a data structure — a binary tree
whose nodes, strings, and list buffers all go at once, in one motion, with no
`treefree` to write and therefore none to get wrong.

That is the whole reason this chapter is a coda and not a requirement.
Nothing later in the book depends on it. Skip it and you lose nothing you
will need.

What you get for reading it is the floor itself, and the floor is worth
seeing once from below, for the reason chapter 9 gave: the raw tier is not
the dangerous end of the language, it is the *simple* end. Inside an `unsafe`
block a pointer is an address, arithmetic on it is arithmetic, two pointers
may name the same byte, and no rule about aliasing is in force. The rules
are C's rules. An allocator is the program those rules were designed for,
which is why it is the one worth writing in them.

Two honesties before the program, because this is a page and not a chapter.

Our allocator is **first-fit with splitting, and it does not coalesce.**
A freed block goes back on the list whole; two adjacent free blocks stay two
blocks. K&R's version does coalesce, and the difference matters to any
program that runs long enough to fragment its heap. Ours is the version that
fits on a page.

Our allocator **does not think about alignment.** It hands out cell indices
into an array of eight-byte cells, so everything it returns is
eight-byte-aligned and nothing finer is available from it. A real allocator
takes a size in bytes and an alignment, and the arithmetic gets longer.

## 32.2 A free-list allocator in a page

The arena is one `malloc` from C. Inside it, everything is a cell index —
which is the trick that makes this short, so read the layout comment twice:

```wolf,part(alloc)
import c "stdlib.h"

// A block is two header cells and then its payload:
// [size][next][payload…]. `size` counts payload cells; `next` is the cell
// index of the next free block, and 0 ends the list, because cell 0 is
// never a header and so 0 can mean nothing.
struct Heap { cells: *i64, head: int }

fn arena(cells: int) -> Heap {
    // # Safety: the allocation owns `cells` eight-byte cells and lives
    // until the free at the end of `main`; every index below is inside it.
    unsafe {
        let base = c.malloc((cells * 8) as uint) as *i64
        base[1] = (cells - 3) as i64
        base[2] = 0
        Heap { cells: base, head: 1 }
    }
}
```

`Heap` holds a raw pointer in a field, and that is the shape chapter 9's
E1302 note asks for. There are no `unsafe fn`s in wolf and no `*T` crosses a
function signature, so an allocator's state cannot be "the pointer, passed
around"; it is a value with the pointer inside it, and the module is the
audit granule.

The zero is the other thing to read. `next == 0` means "no next block"
because cell 0 is never a header — cell 0 is the arena's own first cell and
the first block's header starts at 1. That is a sentinel, exactly like the
ones chapters 26 and 27 spent their pages arguing against, and here it is
correct: this is the tier where an integer is allowed to mean two things,
because there is no second channel to put the other meaning in and no
compiler asking for one.

Now the allocation itself. Walk the free list, take the first block big
enough, split it if the remainder can hold a header and at least one payload
cell:

```wolf,part(alloc, cont)

#[trusted("every `next` is a header cell inside the arena, and no block sits on the free list twice")]
fn carve(mut h: Heap, want: int) -> int {
    var prev = 0
    var b = h.head
    while b != 0 {
        var size = 0
        var next = 0
        // # Safety: `b` is a header the free list put there, so cells `b`
        // and `b + 1` are inside the arena.
        unsafe {
            size = h.cells[b] as int
            next = h.cells[b + 1] as int
        }
        if size >= want {
            var link = next
            if size >= want + 3 {
                let rest = b + 2 + want
                // # Safety: both halves of the split lie inside the block
                // at `b`, which had `size` payload cells.
                unsafe {
                    h.cells[rest] = (size - want - 2) as i64
                    h.cells[rest + 1] = next as i64
                    h.cells[b] = want as i64
                }
                link = rest
            }
            if prev == 0 {
                h.head = link
            } else {
                // # Safety: `prev` is the header before `b` on the list.
                unsafe { h.cells[prev + 1] = link as i64 }
            }
            return b + 2
        }
        prev = b
        b = next
    }
    0
}
```

`#[trusted]` is chapter 9 §9.7 arriving at the program it was written for.
Look at the obligation in the attribute and then look at the function: every
`next` is a header cell inside the arena, and no block sits on the list
twice. Nothing in the type system says either of those things. Nothing can:
they are properties of a graph the program maintains by being written
correctly. The attribute is where a person takes the proof over, the string
is the sentence a reviewer checks the body against, and §32.3 shows what the
tooling does with it.

Freeing is one store, and the rest of the program is a demonstration:

```wolf,part(alloc, cont),run(exit=0)

fn recycle(mut h: Heap, p: int) {
    let b = p - 2
    // # Safety: `p` came from `carve`, so `b` is its header cell.
    unsafe { h.cells[b + 1] = h.head as i64 }
    h.head = b
}

fn main() -> !int {
    var h = arena(64)
    let a = carve(mut h, 4)
    let b = carve(mut h, 4)
    print("a={a} b={b} free={h.head}")
    // # Safety: both payloads own four cells each.
    unsafe {
        h.cells[a] = 40
        h.cells[b] = 2
        print("{h.cells[a] + h.cells[b]}")
    }
    recycle(mut h, a)
    let d = carve(mut h, 2)
    print("d={d} free={h.head}")
    // # Safety: the arena is freed exactly once, and nothing reads it after.
    unsafe { c.free(h.cells as *u8) }
    0
}
```

```console,from(book/ch32/part-alloc)
$ lupin allocator.lu
a=3 b=9 free=13
42
d=3 free=13
```

Three lines, and each of them is the allocator answering a question.

`a=3 b=9` is the split working: the first request takes cells 1 and 2 as its
header and hands back cell 3, the remainder becomes a new free block, the
second request repeats it four cells further along. `free=13` is where the
free list points after both: past the two blocks it handed out.

`42` is the payload being used as payload — two stores and two loads through
a raw pointer, which is the entire reason the arena exists.

`d=3` is the interesting one. `recycle` put block `a` back on the front of
the list, so the next request finds it first and reuses the same cells. It
asked for two and got a block that holds four, because 4 is not big enough
to split — a header costs two cells and a payload at least one, so a
four-cell block cannot become a two-cell block plus anything legal. Those
two wasted cells are internal fragmentation, they are visible in this
program's arithmetic, and there is no version of a hand-rolled allocator in
which you do not have to think about them.

## 32.3 The brace you already had

Now the two measurements, which are the coda's whole argument.

The first is the one this part has been making since chapter 26:

<!-- WC (verify-docs): samples/projects/allocator/allocator.lu=84 samples/projects/wordtree/wordtree.lu=45 -->

```text
$ wc -l samples/projects/allocator/allocator.lu samples/projects/wordtree/wordtree.lu
  84 samples/projects/allocator/allocator.lu
  45 samples/projects/wordtree/wordtree.lu
```

Comments and blank lines removed, the allocator is **65 lines**. Chapter 28's
whole word-frequency program — node, insert, in-order walk, tokenizer, and
`main` — is 41. The allocator is not a program that does anything a reader
wanted; it is the machinery underneath one, and it is half again as long as
the thing it would have been underneath.

Of chapter 28's 41 lines, the ones doing this chapter's job are two: `region
words {` near the top of `main`, and the `}` ten lines later. That is the
comparison, and it is not close, and the reason it is not close is that the
two programs are not solving the same problem. `carve` and `recycle` maintain
a free list because the program asks for and returns individual objects at
unpredictable times. Chapter 28's tree never returns anything: it allocates
until it is finished and then everything goes at once. Wolf did not write a
better allocator. It changed the question to one that has a two-line answer.

The second measurement is the one only wolf can print. Ask the compiler what
it would take to review this file:

```console,from(book/ch32/part-alloc)
$ wolf audit-surface ./allocator.lu
unsafety surface (D11 rings)
module root:
  trusted fn carve — "every `next` is a header cell inside the arena, and no block sits on the free list twice"
  unsafe blocks: 7 · assume sites: 0 · re-entry doors: 0 · import c: 1 · inline c/asm: 0
error[E1303]: module `root` holds `#[trusted]` code, but the package manifest does not declare it
  --> ./allocator.lu:21:4
   |
21 | fn carve(mut h: Heap, want: int) -> int {
   |    ^^^^^ the first trusted function is here
   |
   = note: add `trusted = root` to `wolf.pkg` — the trusted roster is the supply-chain surface
     `wolf audit` diffs (D11 ring 2), so every trusted module is declared, visibly.
   = note: this package has no `wolf.pkg` yet; create one next to the entry file.

wolf audit-surface: undeclared `#[trusted]` module(s) — the manifest is the deal
```

Seven blocks, one C import, one obligation stated in English — and the deal
chapter 9 §9.7 described, arriving as a refusal, because the manifest line
that would declare this module is not written. The count is the reviewer's
budget: seven places where the compiler stopped proving things, and one
sentence a person has to believe.

Ask the same question of chapter 28's program:

```console,from(book/ch28/part-wordtree)
$ wolf audit-surface ./wordtree.lu
unsafety surface (D11 rings)
(clean — no unsafety rings in this package)
```

Nothing to count, nothing to declare, nothing to believe.

So the book ends where it began, with a question about granules. Chapter 7
asked who owns a value. Chapter 8 asked how big the thing that dies together
is. This page is what the answer bought: the program that would have been at
the bottom of every wolf program ever written — the one C had to teach in its
last chapter, because in C you cannot ship without it — turns out to be an
exercise. Seven unsafe blocks and one obligation, against a brace you already
had.

<!-- AUDIT LEDGER (unpublished) — bs10 pass two, chapter 32 and the part's
     remaining gates. Filed to the `book-audit` loop on wolf-lang (labels:
     book-audit + one ba:* severity + from:bs10).

     The first four rows are NOT about chapter 32. They are this pass's
     gate-verification findings for chapters 29, 30 and 31, filed here
     because chapter 32 is the only chapter this pass shipped and a
     finding in a held chapter's stub is a finding nobody reads.

     - [x] ba:blocker (COMPILER ICE — the finding of this pass, and the
           reason the pin did not move) — two `select` statements with
           `timeout` arms in one function crash the compiler:

             fn main() -> !int {
                 let a = channel[int](1)
                 select { v from a => { print("got {v}") },
                          timeout(5.ms) => { print("timed out") }, }
                 a.send(9)
                 select { v from a => { print("got {v}") },
                          timeout(5.ms) => { print("timed out") }, }
                 0
             }

             wolf build: ICE: lowered WIR failed verification
             wir verify error [dominance] in @main: b10:
               `%55 = icmp.eq %48, %28`: %28 is not dominated by its definition

           `%28` is `iconst.i64 -2`, the timeout sentinel, CSE'd out of the
           first select's arm into b2 and then used from b10 — the same GVN
           cross-arm dominance class that was fixed once for if-join
           coercion, recurring at select arms. Reproduces on `wolf build`
           and on `wolf conform-run` at every green trunk sha carrying
           native conc lowering. **This program is `book/ch12.md`'s §"a
           timeout" sample**, shipped since bs06, and at the book's own pin
           it reports `unsupported` at `resolve` and passes. So the ICE is
           what a pin bump onto native concurrency costs today: `cargo
           xtask samples` does not merely fail, it aborts, because the
           runner bails when `conform-run` returns no verdict JSON.
           Owner: the mid-end (GVN/dominance). Nothing in the book can
           work around it, and nothing should: the sample is correct wolf.
           bs12: box ticked — re-verified at pin fc7fdec: the two-
           timeout-select program builds and runs (`timed out` then
           `got 9`, exit 0), exactly as the rp02 close below records.
     - [x] CLOSED at rp02's pin bump — the ICE above is fixed. The
           mid-end's dominator-scoped GVN no longer hoists the timeout
           sentinel out of a select arm, and the two-`timeout`-`select`
           program builds and runs to `exit(0)`, printing `timed out` then
           `got 9`. `cargo xtask samples` completes; chapters 30 and 31
           ship on the pin that carries it. Kept rather than deleted
           because it is the record of a gate holding, which is the
           mechanism these notes depend on.
     - [x] RESOLVED at rp02's pin bump, and it was worth investigating
           after all — the second blocker, `book/ch15/s5` reporting
           `fail(E0402)`, is the compiler missing `w.link()`, the
           one-argument spelling chapter 15 §15.1 teaches and `wolf_rt`'s
           own `link` doc-comment names. sema's proc method table declares
           the partner parameter unconditionally; lupin implements the
           defaulting. Both of the chapter's `w.link()` programs run under
           lupin, and exercise 15-2's fence now carries the `run(exit=1)`
           directive its stem already named, so no reader-facing text
           moved. `book/ch15.md`'s ledger carries the full row and the
           owner. The seven diagnostic snapshots predicted to drift did
           NOT drift at the sha that shipped: the mid-end changed no
           diagnostic the book prints, and `cargo xtask samples` needed
           exactly one `--bless`, for chapter 30's new E1012 sample.
     - [x] CLOSED at rp02's pin bump — **argv exists, and the book now
           says so.** Chapter 26's ledger row is closed, §26.5's
           input-surface paragraph is rewritten, and chapters 30 and 31
           are built on `env_args()`. The original finding, kept for the
           record: **argv exists.** Chapter 26's ledger and TOC.md's
           "no argv" delta record the part's highest-priority finding: no
           wolf program could read its command line, so every project held
           its input as data. That is history on trunk. `env_args()`
           returns a `List[str]`, lowers natively, and `wolf run p.lu a b`
           and `./p a b` both deliver real arguments (verified). It is not
           yet history in the book, because the pin that carries it is the
           pin the ICE above blocks. When that pin lands, chapter 26 §26.5's
           input-surface paragraph and its ledger's first row both want
           revisiting, and `pargrep` becomes the part's first program with
           a real command line.
     - [x] CLOSED at rp02's pin bump — **chapters 30 and 31 ship.** Every
           measurement was taken again at the adopted pin and none of them
           moved: 97 lines, two hundred consecutive runs, one output hash,
           and one `stdout_sha256` across six seeds. The three
           conservatism edges still hold and are re-recorded in chapter
           30's own ledger. The original finding: **P5 and the solo are
           written, run, and measured; only the pin is missing.** `pargrep` was built whole
           against the candidate toolchain: file names and a `|`-separated
           pattern list from `env_args()`, the line table and the pattern
           table each in a `freeze region { … }` and shared by reference
           across four tasks with no copy, hits and completions on two
           rendezvous channels drained by one `select` collector, output
           printed in input order. 97 lines, 200 consecutive runs, one
           output hash. `logden`'s milestone shapes verify too (per-shard
           `freeze region` values held in a `List[Shard]`, queried from
           separate tasks, `for l in shard.lines` walking an imm list
           without consuming it). Three conservatism edges shaped the
           program and are worth knowing before anyone rewrites it:
           `s.spawn` inside a loop is refused ("env-slot lifetime, c05
           follow-up"), so shard count is a written-out constant; channel
           payloads beyond one word are refused ("s39 std sync"), so
           results are line indices rather than `str`; and a `channel[T]`
           parameter loses its method surface ("methods on generic std
           data, s05"), so a task's sends are inline in its own closure.
     - [x] ba:blocker (X12 — re-measured at rp02's pin and MOVED, in the
           wrong direction; chapter 30's ledger carries the current form
           and this row is superseded by it. `wolf test --schedules=N` on
           a body carrying structured concurrency now reports
           `unsupported (structured concurrency in checked execution)`, so
           the exploration below cannot be reproduced at all: the command
           prints its reproduction sentence without having explored
           anything. §30.5 is written on the native lane instead.) The
           finding as measured before the bump — `wolf test --schedules=N`
           prints "reproduce any run with --replay=SEED", and for the
           class of divergence it most often finds on native bodies the
           replay does not reproduce. Measured: an order-dependent
           assertion over four spawning tasks was caught by
           `--schedules=24` in two of three explorations; the failing
           seed it printed then passed 8 of 8 replays. A fixed
           `WOLF_SCHED_SEED` gave three different arrival orders in 20
           runs of the same binary. What the seed *does* reproduce is the
           PRNG-driven part — two simultaneously-ready `select` arms
           replay identically, 8 of 8 in both directions — so the promise
           holds for tie-breaks and not for interleavings. A tool that
           prints a reproduction command should not print it for a run it
           cannot reproduce.
           bs12: box ticked as superseded — the row says so itself;
           ch30's X12 row carries the current form, re-verified at this
           pin (`wolf test --schedules` still prints a replay sentence
           for an unexplored structured-concurrency body).
     - [x] ba:papercut (the part's C-twin question, closed by
           measurement) — P5 has no C twin, and the reason is the book's
           own rig rather than a judgment about C. A parallel grep in C is
           a pthreads program; `cargo xtask contrast` compiles every twin
           with one hard-coded flag set (`-std=c99 -Wall -Werror`, no
           per-file flags) on ubuntu, macOS **and windows**, and there is
           no pthreads on the third. Measured on linux/glibc: the
           pthreads twin does compile and link under exactly those flags,
           which is what makes the trap worth naming — it would pass
           locally and fail one lane of CI. `<threads.h>` is C11 and
           absent on macOS; `fork`/`pipe` do not survive strict `-std=c99`
           (`unknown type name 'pid_t'`, measured). So the honest form of
           the contract's "designs C could not host" is narrower and
           truer: C hosts this one, not portably, and not under the
           compile line this book holds its C to. If a future author wants
           the twin, the rig needs a per-file flags channel in
           `cases.toml` and the Windows lane needs a decision, in that
           order.
           bs12: consolidated — the same finding lives in ch30's ledger
           (its C-twin row), which stays open and carries the rig ask
           (per-file flags channel + a Windows-lane decision); this
           copy is closed to keep one tracking row per finding.
     - [ ] ba:papercut (chapter 32's own, and small) — the coda's
           transcripts are the interpreter's, as chapter 9's cast-using
           samples are. `as *i64` is refused by native lowering
           ("raw-pointer casts (unsafe-tier WIR ops, deferred from s26)")
           and a raw-pointer store is refused separately ("index writes
           outside List (raw-pointer writes c10)"), so `wolf build
           allocator.lu` produces no binary. Chapter 9 §9.1's opening
           sample — `malloc`/`memset`/`memcpy`/`free` with no casts — does
           build, which is why that one chapter has a native transcript and
           everything downstream of a cast does not. The one thing the
           allocator wanted and could not have is `wolf audit-surface` on
           a *built* program; the audit runs on source, so §32.3's second
           measurement is unaffected.
           bs12 re-verify (pin fc7fdec): reproduces — `as *i64` is still
           "raw-pointer casts (unsafe-tier WIR ops, deferred from
           s26)" (ch09's probe), so the coda still runs on the
           interpreter alone.
           → filed wolf-lang#156 (bs12 theme batch).
     - [ ] Recorded as good news — `#[trusted]` is exactly as useful as
           chapter 9 §9.7 claimed, and this is the first program in the
           book that earns it. `wolf audit-surface` prints the obligation
           string back beside the function name, counts the seven blocks,
           and refuses the undeclared module with E1303 naming the first
           trusted function and the missing manifest entry. §9.7's example
           obligation is literally "an allocator's free-list is
           well-formed"; §32.2's is that sentence about a real free list.
           The two pages were written a part apart and did not need
           reconciling.
     - [ ] Recorded as good news — the interpreter runs the whole raw tier
           this chapter uses, including `as *i64` word-granularity
           indexing, stores through a raw pointer held in a struct field,
           `mut` parameters carrying that struct, and the `c.malloc` /
           `c.free` pair. The allocator's three-line output is stable and
           the arithmetic in it is checkable by hand, which is what makes
           §32.2's paragraph-per-line reading possible.
     - [ ] Contract delta — the sprint file's coda spec asks for "a
           free-list allocator in the unsafe tier in ~a page". What ships
           is first-fit with splitting and **no coalescing**, and §32.1
           says so before the program rather than after it. Coalescing is
           K&R's version and it roughly doubles the page. Alignment is the
           other omission, also stated in §32.1: the arena is an array of
           eight-byte cells and the allocator's unit is a cell, so
           alignment never becomes arithmetic. Both recorded rather than
           faked.
     - [ ] Epigraph accounting — this chapter carries the part's second
           and final dark-Romantic placement, which PERMISSIONS.md §1 has
           reserved for the coda since bs10's first pass: Tchaikovsky's
           Sixth, public domain, untracked in that ledger by design, with
           the descent-to-the-floor image tied to the section it belongs
           to in the first sentence of prose under it. The part's single
           Cage placement was still unspent when this chapter
           shipped; it is spent at rp02, on chapter 31 ("Back Against the
           Wall.", PERMISSIONS.md row 6), which is where this note
           reserved it.
     - [ ] Cross-reference accounting — five backward (chapter 8's granule
           twice, chapter 9's E1302 note, chapter 9 §9.7 twice, chapter 26
           and 27's sentinel argument, chapter 28 §28.5 three times) and
           none forward, which is correct: this is the last chapter before
           the back matter, and chapter 28's forward reference to it — the
           one the sprint contract requires the coda to cash — is cashed
           in §32.3 by measurement rather than by assertion.
-->
