# 29. `tinyvm`

<!-- STUB (bs10): this chapter's prose waits for its gate; the gate
     note and the reason are in principles/TOC.md §Deltas. Structure
     below is TOC.md structure; TOC.md wins on conflict. -->

<!-- HOLD (re-measured at the bs10 pass-three pin bump, wolf 2c6980a) —
     the gate LOST ITS SECOND HALF and kept its first, measured rather
     than assumed, on the chapter's own spellings.

     What opened: monomorphization. `pick(a, 0)` on a `List[int]` —
     the exact program the rp02 note measured — now compiles, links,
     runs, and prints the same 7 on both lanes (`wolf build`, the
     binary, `wolf run`, and lupin agree). The compiler instantiates
     the generic body per concrete type on one worklist (c21), so
     §29.1's dispatch loop and any generic helper the VM wants are no
     longer held. One narrower fact replaces the old second half: the
     EXPLICIT application spelling `pick[int](a, 0)` still refuses —
     "a generic function used as a value (comptime)" at resolve —
     so the chapter's listings should call generic functions and let
     the checker bind the types, which is the idiom the book teaches
     anyway.

     What holds, unchanged, re-measured at this pin in chapter 8's own
     spelling —

       region h: pool(Cell) {
           var heap = Pool[Cell]()
           let a = (mut heap).reserve()
           (mut heap).init(a, Cell { value: 1, next: 0 })
           heap[a].value = 42
       }

     — `wolf build` answers "cannot compile this yet — Pool/shared
     constructor lowering (runtime shapes, c06)" (the refusal grew a
     parenthetical since rp02: "pipeline is honest through `mem`; the
     conservatism ledger, not a bug in your program" — same verdict,
     better manners), and `lupin run` answers "unsupported:
     `heap["handle#0[0]@0"].value` does not denote a place at run
     time", exercise 8-7's blocker since bs04. Chapter 8's read-only
     pool programs still run under lupin, stale-handle trap included,
     so §29.4's transcript still exists in isolation; a VM that can
     never write to a heap object is still not a VM.

     The surviving gate, in one line — and it is now genuinely one
     line, not two: `Pool[T]` construction in native lowering, and a
     field write through a handle in the interpreter's std subset.
     The chapter needs one lane where both work, and there is none at
     this pin. Upstream owns both halves under one name now: the
     compiler's refusal string says c06 runtime shapes (tracked with
     wolf-lang#31's place-writes), the interpreter's is
     wolf-interp#25/#28's value model.

     For whoever writes this chapter: everything the rp02 note said
     about §29.1 and §29.2 stands — the dispatch `match` and the
     fixed-`List` registers build and run on both lanes and were never
     the hold — and the warning about the 0-or-1-`List` child trick
     stands with it. Generic helpers are newly available to the
     walkthrough. When the Pool refusal or the handle-write refusal
     lifts, the chapter is a walkthrough of the stepper the bs00x
     corpus seeded for it.

     bs19 re-check (2026-08-30, pins wolf v0.2.0 c88ab64 / lupin
     0.1.18): the one-line gate holds, both halves, refusals
     byte-identical to the note above — `wolf build` on this note's
     own pool program still answers "cannot compile this yet —
     Pool/shared constructor lowering (runtime shapes, c06)", and
     lupin still answers "unsupported: `heap["handle#0[0]@0"].value`
     does not denote a place at run time" (exercise 8-7's
     samples-pending row re-ran at these pins: still refusing, zero
     flips). The stub stands. -->


<!-- promise: build the dispatch loop as one `match`. -->


<!-- promise: give the machine its state. -->


<!-- promise: allocate VM objects behind generational handles. -->


<!-- promise: inject one and read the trap, not the exploit. -->

This edition does not carry this chapter.

`tinyvm` is a virtual machine whose heap is a pool of objects reached
through generational handles, so that a dangling reference in the guest
program becomes a stale handle in the host — a trap with a name instead
of an exploit. That is chapter 8's `Pool` and `handle` spent on a real
program, and it is the mechanism §29.3 and §29.4 exist for. Neither
implementation in this edition can run it: the compiler declines to
lower a `Pool` constructor, and the interpreter declines a field write
through a handle. A project chapter ships whole or not at all, so the
section numbers are reserved and the pages are not written.

The other five projects are here. Chapter 30 is the concurrent one, and
chapter 32 spends the unsafe tier on an allocator, which is the closest
this edition comes to a machine of your own.
