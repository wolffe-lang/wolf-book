# 29. `tinyvm`

<!-- STUB (bs10): this chapter's prose waits for its gate; the gate
     note and the reason are in principles/TOC.md §Deltas. Structure
     below is TOC.md structure; TOC.md wins on conflict. -->

<!-- HOLD (re-measured at rp02's pin bump) — the gate is closed on BOTH
     lanes, measured rather than assumed, and what is closed is the
     chapter's central mechanism. The bs10 pass-two note said the same
     thing about a different pin; every measurement below was taken again
     at this one, and none of them moved.

     The VM's heap is a `Pool` of objects reached through generational
     handles (§29.3), and a stale-handle read is the trap §29.4 exists to
     show. Written in chapter 8's own spelling —

       region h: pool(Cell) {
           var heap = Pool[Cell]()
           let a = (mut heap).reserve()
           (mut heap).init(a, Cell { value: 1, next: 0 })
           heap[a].value = 42
       }

     — `wolf build` answers "cannot compile this yet — Pool/shared
     constructor lowering (runtime shapes, c06)" and `lupin run` answers
     "unsupported: `heap["handle#0[0]@0"].value` does not denote a place
     at run time", which is the blocker exercise 8-7 has held on since
     bs04. Chapter 8's read-only pool programs do run under lupin,
     stale-handle trap included, so §29.4's transcript exists in
     isolation; a VM that can never write to a heap object is not a VM.

     Monomorphization is the gate's second half and is refused natively
     either way it is spelled: `pick[int](a, 0)` is "a generic function
     used as a value (s16 comptime)", `pick(a, 0)` is "generic-function
     lowering (monomorphization)". lupin evaluates both and prints `7`.

     WHAT THE PIN BUMP CHANGED HERE: nothing. Native concurrency and the
     mid-end are what opened chapters 30 and 31; neither touches c06's
     runtime shapes or s16's monomorphization, which is why this chapter
     is the one Part 5 project still held. Its gate is a compiler gate and
     it is named in one line: `Pool[T]` construction in native lowering,
     and a field write through a handle in the interpreter's std subset.
     Either one alone is not enough — the chapter needs a lane on which
     both a `Pool` and a write through a handle work, and there is no such
     lane at this pin.

     For whoever writes this chapter: §29.1 (fetch/decode/execute as one
     `match`) and §29.2 (registers as a fixed `List`) build and run on
     both lanes today and were never the hold. Do not reshape §29.3
     around the 0-or-1-`List` child trick chapter 28 used — that shape is
     what a `Pool` was meant to replace, and chapter 28's ledger records
     it as a constraint rather than a preference. When either refusal
     lifts, the chapter is a walkthrough of the stepper the bs00x corpus
     seeded for it. -->


<!-- promise: build the dispatch loop as one `match`. -->


<!-- promise: give the machine its state. -->


<!-- promise: allocate VM objects behind generational handles. -->


<!-- promise: inject one and read the trap, not the exploit. -->

This edition does not carry this chapter.

`tinyvm` is a virtual machine whose heap is a pool of objects reached
through generational handles, so that a dangling reference in the guest
program becomes a stale handle in the host — a trap with a name instead
of an exploit. That is chapter 8's `Pool` and `handle` spent on a real
program, and it is the mechanism §29.3 and §29.4 exist for. Neither
implementation in this edition can run it: the compiler declines to
lower a `Pool` constructor, and the interpreter declines a field write
through a handle. A project chapter ships whole or not at all, so the
section numbers are reserved and the pages are not written.

The other five projects are here. Chapter 30 is the concurrent one, and
chapter 32 spends the unsafe tier on an allocator, which is the closest
this edition comes to a machine of your own.
