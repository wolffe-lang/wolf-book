# Colophon

<!-- STUB (bs00): the full colophon lands in bs11. The pins below are
     the live wolf-toolchain.toml values; doc-truth CI compares them. -->

This printing is true for the toolchain pinned in `wolf-toolchain.toml`
at the repository root: wolf (wolf-lang) and lupin (wolf-interp) at the
revisions recorded there, and the wolf grammar vendored from wolf-lsp at
its recorded pin. Every code sample was executed by CI against those
pins; the samples runner's report is the guarantee, not this sentence.

Errata address: lands with hosting decisions in bs11.
