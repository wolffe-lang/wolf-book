# Solutions

<!-- STUB (bs00): every exercise in chapters 1–30, collapsed by default
     in the web edition (plain <details>, no JS), extracted and CI-run
     like all samples. Solution pages land with their chapters (bs01+).
     bs10: the solo (chapter 31) publishes milestone checkpoints
     instead — the one exception, and the only one (EXERCISES.md §4).
     The coda (chapter 32) carries no exercises. -->

*Stub — the solutions back matter. Each solution renders collapsed by
default and links back to its exercise and to the section that taught
it. The programs already exist and already run in CI: 170
directive-headed `.lu` files under `principles/exercises/`.*
