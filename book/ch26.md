# 26. `count`, twice

<!-- STUB (bs10): this chapter's prose waits for its gate; the gate
     note and the reason are in principles/TOC.md §Deltas. Structure
     below is TOC.md structure; TOC.md wins on conflict. -->

## 26.1 The tool, twice

<!-- promise: read the two-column form, and the honesty rule it runs on. -->

## 26.2 Ritchie's state machine

<!-- promise: read the C twin whole, and name the two conventions it is built from. -->

## 26.3 The same machine as a `match`

<!-- promise: write the wolf loop and say which column is shorter, from `wc`. -->

## 26.4 Per file, and a total

<!-- promise: grow the program to many inputs, reading each through `fs_read_text` over rows. -->

## 26.5 Where wolf is not shorter

<!-- promise: read the places the C wins, with the measurement. -->
