# 26. The capstone

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 26.1 Architecture

<!-- promise: recall which chapter taught each decision in the target design. -->

## 26.2 Milestone 1: single-shot search

<!-- promise: ship the walking skeleton with Parts 1–2 skills only. -->

## 26.3 Milestone 2: a region per request

<!-- promise: serve HTTP with request arenas and structured shutdown. -->

## 26.4 Milestone 3: shards as procs

<!-- promise: supervise crashing shards into degraded-but-correct service. -->

## 26.5 Milestone 4: the C membrane

<!-- promise: decompress shards through an audited twenty-line wrapper. -->

## 26.6 Milestone 5: deterministic tests

<!-- promise: find a real ordering bug with a seed, fix it, chaos-harden it. -->

## 26.7 Milestone 6: the profiling pass

<!-- promise: execute measure → constrain → restructure → re-measure, and publish v1.0. §Solutions policy). generated from the spec's `grammar.ebnf`, never hand-maintained. `[conf.trap.set]`, each with kind, fault, and clause. cross-checked against the compiler's catalog in CI. for readers who want the normative text. in the web edition, extracted and CI-run like all samples. The capstone publishes milestone checkpoints instead (the one exception, by design). and flag; section numbers, not page numbers, in the web edition. CI-verified-samples guarantee, and the errata address. reader gets the lab bench before the strings chapter uses it; chapter 2 then opens already holding the tool. bs01's "REPL as the chapter's lab bench" framing transfers to §1.4 intact. chapter-end aside; the promise "the book never asks for a second tool" is stated in §1.2 where installation happens. spine) is a named section (§6.4) distinct from the wordcount capstone (§6.5), so the capstone stays a build, not a rescue. running-example seed (`shelf` value types) lives inside §7.2 and §7.6 rather than as its own section — two forward-gestures to chapter 8 are the counted maximum. the `when (a, b)` material stays in the channels chapter (§12.4) rather than the parallel-iterators chapter its subject might suggest, because deadlock-freedom is a synchronization story. obligation — saying plainly that the heisenbug is an *ordering* bug, not a data race — is promoted to the section promise, since that honesty is the pitch's credibility. ("where C wins today") is a first-class section, not a caveat box, and its regenerated-from-CI requirement is inherited from the sprint file. compresses to one chapter with seven numbered sections plus a closing discussion inside §26.7. Rationale: milestone chapters share one running repo and one architecture; section-level structure keeps checkpoint tags (`m1`–`m6`) as the real units. If drafting shows the milestones need chapter-scale room, bs10's author may split them back out — this delta records the default, not a straitjacket. cross-reference), which bs11's checklist implies ("claims traced to spec clauses") but never lists as an artifact. The index doctrine (section numbers as anchors) is stated here and in DESIGN.md so bs11 inherits it rather than deciding it. -->
