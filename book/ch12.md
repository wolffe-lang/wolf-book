# 12. Channels and select

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 12.1 Typed channels

<!-- promise: synchronize with send/recv and one paragraph of happens-before. -->

## 12.2 `select` with timeouts

<!-- promise: multiplex completion-based I/O without a task per idle connection. -->

## 12.3 When channels are the wrong queue

<!-- promise: cite the numbers, reach for `std` queues. -->

## 12.4 `when (a, b)`

<!-- promise: acquire lock sets whole, without ordering folklore. -->
