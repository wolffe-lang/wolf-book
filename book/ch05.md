# 5. Collections and generics without fear

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 5.1 `List`, `Map`, `Set`, tuples

<!-- promise: build and index the workhorses. -->

## 5.2 The combinator style

<!-- promise: chain `.pairs().sorted_by(…).take(n)` and then see what it desugars to. -->

## 5.3 Generics in square brackets

<!-- promise: write `fn top[T](…)`, get errors at the definition site, and never type a turbofish. -->

## 5.4 Indexing that traps

<!-- promise: predict `xs[10]` on a one-element list. -->
