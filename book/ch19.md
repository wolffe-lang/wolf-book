# 19. Perf contracts

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

<!-- HOLD (bs08; re-measured rp02, 2026-08-24) — this chapter's gate
     has not opened, and the reason is recorded here because it is the
     one place a future author looks.

     bs08 shipped chapter 18 and held this one, chapter 20, and chapter
     21. rp02 re-checked every claim below at the current pin in
     wolf-toolchain.toml (wolf fc7fdec, lupin 0.1.13) — the pin that
     opened three of chapter 21's sections — and this chapter's gate
     did not move. The chapter's subject is promises the compiler
     keeps; at this pin it keeps none of them. Measured with `wolf
     run`, `wolf build`, `wolf doc`, `wolf interface`, `wolf --explain`
     and `lupin`:

     - **Nothing verifies a contract (unchanged, re-run 2026-08-24).**
       `#[noalloc]` on a function whose first line allocates a
       `List[int]` draws no contract diagnostic from either tool:
       `wolf run` compiles the file and executes it to exit 0, `wolf
       build` is equally silent, and lupin runs it to exit 0 —
       attributes are inert in both tiers. The four attributes parse
       and mean nothing, which is exactly the "comment with better
       syntax" failure the chapter exists to contrast against. §19.1's
       whole method — write the function, add the contract, read the
       compile error that points at the allocating path — has no
       compile error to read.
     - **No diagnostic code exists to teach (re-run at bs17, pin
       addcd7f).** There is still no contract family in the catalog
       (168 codes at this pin, up from 164 — the newcomers are E0211,
       E0415, E0813 and W0317, none of them contracts; the bs13
       re-run's E16xx warning stands, E1601–E1607 still the consttime
       checker's):

         $ wolf --explain E1600
         wolf --explain: `E1600` is not a registered diagnostic code
         (codes look like E0101; see the diagnostic catalog)

       E1700 answers identically. NOTE the collision the bs13 bump
       created: E1601 is now a REGISTERED code — "a branch condition
       depends on a secret", the consttime checker's — so the old
       assumption that the E16xx block was free for contracts is dead;
       a contract family will need a different range, and this
       chapter's eventual samples must not cite E16xx. The s10 catalog
       import the sprint's acceptance asks for still has nothing to
       import.
     - **§19.2's surface half-moved and still does not carry the
       attribute (re-measured 2026-08-24).** The section's promise is
       that a contract is part of the definition hash and shows up in
       generated documentation. `wolf doc` no longer refuses — the
       bs08 transcript (`not yet (grows at its own campaign; D34's
       single binary)`) is retired, and the command now writes a real
       doc tree:

         $ wolf doc
         wolf doc: wrote 4 file(s) to doc (doc coverage: 0/1 items
         documented, 0/1 carry an example)

       But the emitted item row is `"sig": "fn double(x: int) -> int"`
       with no attribute on it, and `wolf interface ./iface.lu` prints
       an `export_hash` with the same bare row:

         items:
           [0] pub double — fn double(x: int) -> int

       for a `#[noalloc] pub fn double`. The hash is the right place
       for the semver argument to land and the attribute still does
       not reach it, so "a dependency silently losing `#[noalloc]` is
       a visible break" still cannot be shown as a diff of anything.

     Consequences a future author needs to know:

     1. **The exercise corpus is ready and is honest about the gap.**
        All nine ch19 stems and their solution prose are written in
        principles/exercises/ch19/. Six are prose (19-2, 19-3, 19-5,
        19-7, 19-8 and, in part, 19-6) and read correctly today because
        they reason about what a verifying compiler must do; 19-4 and
        19-9 run under lupin and prove their bodies rather than their
        attributes, and each says so in its own solution. 19-1 is the
        pending row, and it carries no samples-pending entry because its
        program runs green — its pending-ness is the unverified
        attribute, not the run.
     2. **What the chapter should not do when its gate opens.** The
        prose written for 19-4's solution is the line to keep: an
        unverified contract is a comment with better syntax. The
        chapter's credibility comes from teaching each contract by
        breaking it, so the first section cannot be written until the
        breakage produces a diagnostic with a code, a span on the
        allocating path, and a note naming the WIR fact that
        contradicted the promise.
     3. **Chapter 18 is where the reader already is.** Chapter 18 ships,
        and its §18.4 teaches budgets and fail-fast — the reader arrives
        here already used to the idea that the compiler refuses things
        with a named reason and hands back the spelling that widens the
        limit. A contract diagnostic that does the same will need no
        introduction.
-->


<!-- promise: break `#[noalloc]`, `#[inplace]`, `#[nopanic]`, `#[bounded_stack]` one at a time and read the errors. -->


<!-- promise: see a dependency lose `#[noalloc]` and semver notice. -->


<!-- promise: smell contract noise. -->

This edition does not carry this chapter.

Perf contracts are the four promises a function can make about its own
execution — no allocation, in-place work, no trap on the failure path, a
bounded stack — and the whole method of the chapter is to break one and
read the compiler's objection. The objection is what a reader would
learn from, and the toolchain this edition names does not raise it: the
attributes parse and nothing verifies them. A chapter that showed the
promises without the enforcement would teach a comment with better
syntax, so the section numbers below are reserved and the pages are not
written.

What Part 4 does carry is chapter 18 on comptime, chapter 21 on the
performance argument itself, and chapters 22 through 24 on modules,
packages, and the covenant. Chapter 8's region claims and chapter 12's
channel costs both point here for their measurements; until this
chapter and the next exist, those claims stand on the numbers their own
chapters print and on the one suite-level number chapter 21 cites from
CI.
