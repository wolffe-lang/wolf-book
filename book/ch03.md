# 3. Values and expressions

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 3.1 `let`, `var`, and handing values over

<!-- promise: declare, rebind, and read "assignment hands the value over" without the deep story (deferred, explicitly, to chapter 7). -->

## 3.2 Everything is an expression

<!-- promise: get values out of `if`, `match`, and blocks; live without a ternary. -->

## 3.3 Arithmetic that traps

<!-- promise: predict overflow and division by zero in every build profile, and spell intentional wrapping. -->

## 3.4 `match`, exhaustively

<!-- promise: write a match the compiler proves total. -->
