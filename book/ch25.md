# 25. Editions, stability, publishing

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

<!-- HOLD (bs09) — this chapter's gate has not opened, and the reason is
     recorded here because it is the one place a future author looks.

     bs09 shipped chapter 22 whole, chapter 24 whole, two of chapter 23's
     four sections, and held this one whole. All three of its sections
     are policy that the toolchain does not yet keep, measured at the pin
     in wolf-toolchain.toml with `wolf run`, `wolf fix`, `wolf publish`
     and `wolf interface`:

     - **§25.1 (editions per package).** `edition` is a manifest key that
       takes a string, is carried, and means nothing. Change a project's
       `edition: "1"` to `edition: "2027"` and it builds and runs exactly
       as before; there is no edition registry, no per-edition parsing,
       no rejection of an edition the compiler does not know, and no
       `wolf fix --edition` (`wolf fix: unknown flag `--edition``). So
       the section's whole method — compile an N-edition package
       warning-free under an N+1 toolchain, then migrate it with one
       command — has nothing to demonstrate, and the promise a reader
       would take away ("upgrade on your clock") would rest on prose
       alone. What IS true and stays true: the key exists, it is
       per-package, and it is in the manifest rather than the toolchain.
       That is the shape of the answer with none of the mechanism.
     - **§25.2 (the stdlib posture).** The section grades three imports —
       `std.list`, `std.os.path`, `std.x.http` — by the promise each
       carries. The book has no std on the page at all: nothing in
       `wolf-toolchain.toml` pins wolf-std, no sample in any chapter
       writes `use std.…`, and the compiler resolves `std` from a
       `path`-sourced dependency named `std` or from `--std-root`. There
       is no `std.x` namespace to point at and no penumbra/core split a
       reader can observe, so "depend on core, penumbra, or `std.x` with
       eyes open" cannot be taught by doing. Pinning wolf-std is the
       enabling change, and it is a pin-file plus CI-lane change rather
       than a chapter change — the same one chapter 24's E1504 row wants.
     - **§25.3 (publishing).** `wolf publish` answers `not yet (grows at
       its own campaign; D34's single binary)`. That is a scaffold
       refusal, which TONE.md §Tense discipline forbids teaching, and
       there is no local-registry fixture to publish against because
       there is no client to run. The pieces that DO exist are worth
       knowing about, because they are the section's future spine and
       they are already correct: `wolf_pkg::lock::hash_tree` gives a
       package a content address over its source-filtered tree, and
       `wolf_pkg::log` pins the transparency-log record format —
       `owner/pkg@version tree=b3:… manifest=b3:… interface=b3:…`,
       append-only, with re-publishing an existing key refused loudly
       ("a published version is immutable"). Neither has a verb. So the
       honest state is: the identity and the record are designed and
       tested, and nothing in the CLI reads or writes them.

     What the reader loses by this chapter waiting, stated so a future
     author can weigh it: the semver-with-teeth argument has its
     mechanism already shipped and shown — `wolf interface` prints a
     module's `export_hash` over its `pub` surface, and chapter 22 §22.2
     spends a paragraph on exactly that number and points here. That
     pointer is the one cross-reference this chapter owes, and it is the
     first thing to cash when the gate opens.

     The exercise batch is written and stays unprinted: 25-1 through
     25-8 are all prose or design, all answerable from the decisions
     (editions per package, the three-ring stdlib posture, semver
     computed against interface facts), and none of them needs a tool —
     which is why they read correctly today and why printing them would
     be printing homework for a chapter that does not exist. They carry
     no samples-pending rows, because none of them is a program.
-->

## 25.1 Editions per package

<!-- promise: upgrade on your clock; no wolf 2.0, ever. -->

## 25.2 The stdlib posture

<!-- promise: depend on core, penumbra, or `std.x` with eyes open. -->

## 25.3 Publishing

<!-- promise: ship `owner/pkg` with capabilities declared and semver with teeth. -->
