# 7. Who owns this?

<!-- STUB (bs00): chapter prose lands with its chapter sprint (bs01+).
     Structure below is TOC.md structure; TOC.md wins on conflict. -->

## 7.1 The error we owed you

<!-- promise: re-read chapter 3's use-after-move with its full explanation. -->

## 7.2 Values are trees

<!-- promise: draw an ownership tree, move a subtree with `take`, copy only on purpose. -->

## 7.3 Borrowing without the word

<!-- promise: state the parameter rule in one sentence and see why it needs no syntax. -->

## 7.4 `mut` at both ends

<!-- promise: write `grow(mut list)` and grep a codebase for its entire mutation surface. -->

## 7.5 Field-granular exclusivity

<!-- promise: pass `mut p.x, mut p.y` legally, and read the diagnostic when paths overlap. -->

## 7.6 Why there are no lifetimes

<!-- promise: judge the trade with both sides shown, including the zero-copy parser wolf loses and what it does instead. -->

## 7.7 What the machine does

<!-- promise: connect `mut`/`read`/moves to `noalias`, freezing, and memcpy-and-forget. -->
