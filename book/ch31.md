# 31. `logden`, alone

<!-- STUB (bs10): this chapter's prose waits for its gate; the gate
     note and the reason are in principles/TOC.md §Deltas. Structure
     below is TOC.md structure; TOC.md wins on conflict. -->

<!-- HOLD (bs10 pass two) — held on chapter 30's pin blocker and nothing
     else; the note there is the reason, and `book/ch32.md`'s ledger has
     the repro. The solo's own shapes were verified independently, because
     "everything the solo needs was taught" is a claim the chapter makes
     about surface and a claim like that is measured or not made.

     VERIFIED, natively, at the candidate pin: per-shard indexes each in
     their own `freeze region { … }`, the resulting imm `Shard` values
     held together in a `List[Shard]`, queried from separate tasks inside
     one `scope`, results over a channel. Two details worth keeping:
       * `for l in shard.lines` walks an IMM list without consuming it.
         Chapter 28's ledger records that `for x in l` consumes an owned
         `List` ([mem.tier0.move]) and that a non-consuming walk needs an
         index loop; on a frozen list that cost is gone, which is what
         makes "freeze, then serve queries from many tasks" a shape the
         reader can actually reach.
       * the three conservatism edges in chapter 30's HOLD note apply here
         too, and the shard count is the one that bites: no `s.spawn` in a
         loop means the milestone ladder has to fix its shard count, so
         the problem statement should NAME a shard count rather than
         leaving it to the reader and then refusing what they write.

     THE SOLUTIONS EXCEPTION, resolved — a "checkpoint" is an OBSERVABLE,
     not a program. EXERCISES.md §4 publishes milestone checkpoints and
     never answers, and the two readings of that fight each other: a
     checkpoint that is source is an answer key with extra steps. So each
     milestone publishes its INPUT (a shard corpus, on disk) and its
     EXPECTED OUTPUT (a transcript), and the reader diffs their own
     program's output against it. No `.lu` for `logden` belongs in this
     repository, which also means `samples/projects/logden/` must NOT
     exist: `verify-docs` requires every `samples/projects/<name>/<name>.lu`
     to be printed verbatim by some chapter, and this chapter prints no
     program. Produce the expected transcripts from a reference
     implementation kept OUTSIDE the tree, and say in the ledger that it
     was run at the pins — that is the honest form of an unpublished
     answer.

     THE LADDER (six milestones, one hint each, per the sprint contract's
     ration; the hint register is the margin notes of someone who has
     already made your next mistake):
       M1 ingest — read the named shard files, report line counts per
          shard. Everything here is chapter 26's.
       M2 index — one region per shard, frozen. The hint's subject is
          that a shard is not a file: decide what a shard IS before you
          decide what a region holds.
       M3 one query, one task — search a frozen shard from inside a
          `scope`, report over a channel.
       M4 many tasks — fan the query out; the hint's subject is the shard
          count being a constant, without saying why.
       M5 the query — several terms, AND semantics; report which shard
          each hit came from.
       M6 the CLI — argv, exit status, and output that does not depend on
          which task finished first. The hint's subject is that the last
          one is the whole point.

     EPIGRAPH: the part's single Cage placement is unspent and this
     chapter is the only remaining candidate (PERMISSIONS.md §1). It has
     NOT been spent here, because a placement in a held chapter is a
     ledger row that claims a page nobody can read. Spend it when the
     chapter ships, or let it lapse — and never spend it as well as a
     dark-Romantic one, which the coda has already taken. -->

## 31.1 The problem

<!-- promise: read the whole specification of the program, and nothing else. -->

## 31.2 The milestone ladder

<!-- promise: check your own work against six tagged checkpoints, each with one hint. -->
