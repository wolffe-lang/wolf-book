error[E1001]: `a.x` is used here after its value moved away
 --> ./s8.lu:8:13
  |
7 |     let b = a
  |             - `a` moved here
8 |     print("{a.x} {b.y}")
  |             ^^^ used after the move
  |
  = note: `a.x` is part of `a`; moving one empties the other. Disjoint fields stay usable.
  = note: re-initializing the place (assigning to it) also makes it usable again.
help: to keep the original, copy it at the move
  |
7 |     let b = copy a
  |
