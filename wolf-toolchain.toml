# The pinned toolchain the book is true for (bs00 contract, target 2).
# Bumps are deliberate, in their own commit, CI-green — same policy as
# wolf-lang s00's rust-toolchain. The colophon repeats these values; the
# doc-truth CI job compares them.

[wolf]
# wolf-lang at the v0.2.10 release tag (`662b14c`). D57 (r03): a build
# made exactly at its own release tag prints the bare version with a pin
# clause, and every other build prints `+dev.<commit>` and claims
# nothing. This one is the FIRST kind:
#
#   wolf 0.2.10 (wolfgang, pin 662b14c)
#
# TAKEN FROM THE RELEASE ARCHIVE, not a source build. At a tag those are
# the same build, and the archive is what a reader downloads — which is
# also the only way to be sure, because the LAST bump's finding was that
# a source build's stamp depends on the clone it was made in.
#
# PREDICTED FROM SOURCE BEFORE THE ARCHIVE WAS UNPACKED, and measured
# byte-identical: `Cargo.toml`'s `[workspace.package] version` is
# `0.2.10`, the tag POINTS AT this revision, and
# `crates/wolf_driver/PAIRING` reads `lupin-version = 0.1.31` /
# `lupin-pin = 4c60946`.
#
# THE PIN CLAUSE IS SEVEN CHARACTERS BY DECISION NOW, and this file is
# where that decision was bought. bs36 recorded `+dev.e0ce018` locally,
# went green on every gate, and CI failed both `--version` blocks by one
# character on two hosts: `git rev-parse --short` takes its width from
# the clone's object count, so a long-lived checkout said seven and a
# fresh clone said eight. That is wolf-lang#301, filed by this lane and
# CLOSED by r15 at `bf4014a` — `STAMP_SHA_WIDTH = 7`, `--short=7`, a
# width chosen rather than inherited. `.github/workflows/book.yml` takes
# the same `--short=7` in this bump, because that lane computes
# `WOLF_COMMIT` by hand instead of running `dist` and the decision does
# not reach it on its own. Two places, one number, or the stamp is a
# coin flip on how many objects a runner happened to fetch.
#
# WHAT THIS REVISION IS. Twenty-two commits past the last pin, and
# `e0ce018` is an ancestor of the tag (checked, not assumed): s148's
# three rulings, the lupin-0.1.31 pairing re-stamp, #301's width, two
# xtask/bench folds and the release commit. The three rulings are what
# bound every prediction below:
#   * #292 — the interface digests are over the INTERFACE, not over the
#     compiler's release string. The stamp stays in the `.wolfi` header
#     and in what `wolf interface` prints; `digest_text` replaces
#     `pretty` in what is hashed.
#   * #293 — `s[a..b] = v` and `s[i] = v` on a `str` are E0416 with the
#     fix in the note, not conservatism. `[mem.str.imm]` is the clause.
#   * #284 — `[type.fn.ret]` and `[type.row.operand]`: a body tail is
#     checked against the declared result, and a `!T` is two values that
#     no operator reads (E0409 on either side).
#
# WHAT THE BOOK MEASURED, PREDICTION FIRST. Every number here was
# written into /private/tmp/bs36b-predictions-0210.md before `cargo
# xtask samples` ran at this pin, before the archive was unpacked, and
# before any file in this repository was edited.
#   * THE PAIR POINTS A THIRD WAY, and both halves are computed from the
#     two stamps rather than typed (ww16).
#       THE COMPILER'S HALF NAMES ONE RELEASE AHEAD. It reads `lupin
#         0.1.31` and the interpreter beside it is 0.1.30, because the
#         interpreter published again between this compiler being tested
#         and this printing being pinned. That half read one BEHIND at
#         bs35 and EXACT at bs36; this is the first printing it overshoots.
#       THE INTERPRETER'S HALF IS SIXTY-FIVE COMMITS. It names
#         `2c03ed9`; `git rev-list --count 2c03ed9..662b14c` is 65 on one
#         line of history. That half read 14, then 43, and opens further.
#     No superlative is claimed and none was measured. §1.2 and the
#     colophon say what each half reads and stop.
#   * THE CATALOG MOVES 168 -> 169 -> 170 across two bumps; at this one
#     it is 169 -> 170, added `{E0416}`, removed `{}`, derived from
#     `docs/diagnostics.md` at both revisions and diffed BOTH WAYS.
#     Appendix C's first count is arithmetic on it and moved with it.
#   * THE ANCHORS GROW 453 -> 458, exactly five, both key sets diffed
#     both ways per the is30 lesson: `mem.str.imm` into
#     `02-memory-model.md`, and `type.fn`, `type.fn.ret`, `type.row`,
#     `type.row.operand` into `10-types.md`. Namespaces hold at 12,
#     documents at 11, the namespace-to-document mapping is unchanged in
#     both directions, so Appendix D regenerates to itself.
#   * `grammar.ebnf` IS BYTE-IDENTICAL across the two revisions, checked
#     rather than assumed (it does not appear in the diffstat), so
#     Appendix A regenerates to itself and the vendored copy was not
#     touched.
#   * 4 CONSOLE BLOCKS RE-RECORDED AND 4 WERE PREDICTED, by name, for
#     the first time in three bumps. §1.2's and the colophon's
#     `--version`; §22.2's `wolf interface`; §25.2's `wolf publish`.
#   * THE INTERFACE HASHES MOVE ONCE, AND THIS IS THE BUMP THAT ENDS IT.
#     bs35 found the compiler's version was an input to the digest and
#     filed it; bs36 measured that a `+dev` bump inside a release line
#     moved neither hash, because the digest read the release-line
#     number. #292 removes the stamp from the digest altogether, so both
#     numbers move here for a change of SHAPE and never again for a
#     release. Measured, and §25.2 is the clean measurement for the
#     third printing running: `tree=b3:f8ce5f5b…` and
#     `manifest=b3:d932b3c6…` hold to the byte, `interface=` alone moves
#     `a80a1e34…` -> `97e6cc17…`, on a package nobody edited. §22.2's
#     two go `acf26d25…` -> `b7a67a58…` and its stamp line goes
#     `toolchain 0.2.9` -> `0.2.10`. ch22's ledger row CLOSES.
#   * A FIFTH TRANSCRIPT WAS WRONG AND THE BUILD WAS GREEN. Exercise
#     22-13's `wolf interface` solution carried `toolchain 0.2.9` and
#     `58f563f6…` where the compiler now answers `05a012a2…`. Found by
#     grepping for the old stamp after the two chapter blocks failed —
#     not by a gate, because the console lane replays what is under
#     `book/` and the exercise corpus's 237 console blocks are not
#     replayed at all. Re-recorded by hand; filed as wolf-book#24, which
#     is wolf-book#7's mirror: that one was literals with no run behind
#     them, this one is whole transcripts.
#   * APPENDIX E'S HELP TRANSCRIPT DOES NOT RE-RECORD, predicted with a
#     reason and measured. bs37 put the driver's whole `wolf --help` on
#     the page as a replayed fence — 69 lines, the largest byte-compared
#     block in the book — and `git diff --stat e0ce018 662b14c --
#     crates/wolf_driver/` is two files, `PAIRING` (data) and
#     `pkg_cmd.rs` (#292's publish change). No help text, no verb, no
#     flag. The third re-record site stayed quiet.
#   * 1 FLIP PREDICTED BY NAME AND ZERO MEASURED, and the reason is
#     measured too. ch07 §7.6's `lupin-run(exit=0)` block was declined
#     at the last pin by `unsupported — assignment through nested places
#     (c06)`, and #293's commit says the conservatism arm "narrowed to
#     what sema does not type as a place yet" — so the block was
#     predicted to graduate. It did not: at this pin `conform-run`
#     answers the SAME construct at the SAME span, `[210,225]`. The
#     narrowing carved out `str` index and slice places into E0416 and
#     left nested places where they were. A narrowing is not a general
#     one because a commit message says "narrowed".
#   * #293 PRODUCES NO FLIP ANYWHERE, predicted: a `str`-slice
#     assignment that used to be conservatism is `fail(E0416)` now,
#     which still counts as declining, so nothing graduates off it.
#     ch03's row closes on the message rather than on a fence.
#   * #284 COSTS NOTHING, predicted as the shakiest line in the file and
#     measured right. It makes a `()` tail under a declared result E0401
#     and a `!T` operand E0409 — it can only turn accepted programs into
#     rejected ones — and zero samples moved.
#   * 0 PENDING MOVEMENT, predicted and measured. Five rows; four
#     blockers are the interpreter's std subset and one is borrow-escape
#     reaching no static verdict, and the interpreter does not move.
#   * THE AUDITED-AT GATE DID ITS JOB, AND THIS IS THE POINT OF IT.
#     wolf-book#7 named three sites that five bumps running healed by
#     hand. This bump did not heal them by hand: moving `impl_version`
#     failed the build at each one by name, and it caught two things a
#     person would not have. The pair paragraph swapped `lupin 0.1.30`
#     for `0.1.31` on two pages, which left the allowlist stale in BOTH
#     directions and was reported both ways. And §22.2's new paragraph
#     introduced a literal — `toolchain 0.2.10` in prose — that had to
#     be classified before the build would pass. Neither is a pin move;
#     both are edits this lane made in this bump.
#   * THE RUNNER AT THIS PIN: 479 samples, 474 passed, 5 pending
#     (report-only), 0 failed, 0 flips; 207 of 209 console blocks
#     replayed.
#
# ===================================================================
# bs40 TAKES s152's DEV SHA, `3d0a6e1`, AND THE STAMP READS `+dev` AGAIN.
# PACING rule 5: a consumption lane launches on a dev sha when the
# release is not cut, the way bs33 did. Sixty-two commits past the tag
# (`git rev-list --count 662b14c..3d0a6e1`, ancestry checked), five
# sprints: s148's tail, s149 (`[os.fs.open]`), s150 (`[type.fn.value]`,
# `[conc.chan.payload]` — a struct, enum or tuple of payloads is
# sendable), s151 (`[gram.expr.if]`, the bare `if … then …`), s152
# (`[type.map]`, `[type.map.key]`, `[mem.map.absent]` — the key
# protocol this lane's chapter 5 is about). Built from a FRESH CLONE at
# the sha with `cargo xtask dist` (the archive pair staged whole; the
# stamp is seven characters by #301) and the whole gauntlet ran against
# THAT binary and lupin 0.1.32 from the release archive, never
# ~/.local/bin (bs38's rule).
#
#   wolf 0.2.10+dev.3d0a6e1 (wolfgang, pin 3d0a6e1)
#   paired with lupin 0.1.31 (reference interpreter), pin 4c60946
#   lupin 0.1.32 (wolf-interp, reference interpreter at pin e0ce018)
#
# THE PAIR POINTS A FIFTH WAY, both halves computed from the stamps:
#   THE COMPILER'S HALF IS ONE RELEASE BEHIND. `crates/wolf_driver/
#     PAIRING` at 3d0a6e1 still reads `lupin-version = 0.1.31`, and
#     this file now pins 0.1.32. That half read one BEHIND at bs35,
#     EXACT at bs36 and bs38, one AHEAD at bs36's second bump; behind
#     again here, and for the same reason as bs35 — the interpreter
#     published between this compiler being tested and this printing.
#   THE INTERPRETER'S HALF IS EIGHTY-FOUR COMMITS, and it OPENS. 0.1.32
#     declares pin `e0ce018` (s147's dev sha); `git rev-list --count
#     e0ce018..3d0a6e1` is 84 on one line of history, `merge-base
#     --is-ancestor` checked. That half read 14, 43, 65, 51; 84 is the
#     widest yet and it is stated as the fifth data point, no more.
#
# WHAT THE BOOK MEASURED, PREDICTION FIRST. Every number was written
# into the lane's scratchpad (bs40-predictions.md) before `cargo xtask
# samples` ran at the new pins and before any file here was edited; the
# control ran on the UNEDITED tree (trunk d001309) with the new pair:
#
#   479 samples, 471 passed, 5 pending, 6 failed, 16 flips
#
#   * 8 FLIPS PREDICTED BY NAME AND ALL 8 MEASURED; 8 MORE MEASURED
#     THAT WERE NOT PREDICTED. Predicted: `book/ch03/s6` (lupin 0.1.32
#     takes the range arm — the one lupin-side flip, bs38 saw it by
#     accident), `book/ch05/s8` (the absent-key read, s152),
#     `book/ch12/s1`, `s2`, `s5`, `s6`, `book/ch14/s8`, `ch14/ex14-10`
#     (a struct of payloads is sendable, s150 — the ledgers named these
#     as `fail(E1102)` and «channel payloads beyond one word»). Not
#     predicted, same two sprints, chapters whose ledgers this lane had
#     not read: `book/ch04/s6`, `s7`, `ch04/ex4-9` (closures returned
#     and passed as values — s150's `[type.fn.value]`), `ch11/ex11-5`,
#     `book/ch10/part-served`, `book/ch10/s7`, `book/ch15/part-sup`,
#     `part-roster` (channel payloads: `Doc`, `str`). The prediction
#     read the ledgers of the chapters the contract named and stopped;
#     the runner read all of them. Recorded as the miss it is.
#   * 13 OF THE 16 GRADUATE TO `run(…)`, measured byte-identical on
#     both machines (stdout and exit) on this host before any fence
#     moved. THREE DO NOT, each for a reason on the page:
#       `book/ch05/s8` prints `[none]` on the compiler and `[()]` on
#         lupin 0.1.32 — `[mem.map.absent]` versus the interpreter's
#         old read. The fence is `wolf-run(exit=0, stdout="[none]")`,
#         the transcript is the compiler's, and the row names
#         wolf-interp#91.
#       `book/ch12/s6` (two ready `select` arms) prints `served moves`
#         on the compiler and `served regions` on lupin, and the page
#         already says both are conforming outcomes. `run(exit=0)`, no
#         stdout claim — the claim the section makes is the exit.
#       `ch11/ex11-5` assigns jobs to workers in schedule order, which
#         the exercise says is the seed's business. `run(exit=0)`.
#   * 6 FAILURES, 5 PREDICTED BY NAME: §1.2's and the colophon's
#     `--version` transcripts (both stamps move), and the THREE E1102
#     snapshots whose note s150 rewrote (`book__ch13__s4`,
#     `book__ch16__s3`, `ch16__ex16-9` — the payload note now names
#     `[conc.chan.payload]` and the struct-of-payloads shape). The
#     sixth was the consequence of the third: §13.2 prints that
#     diagnostic as a `diagnostic,from(book/ch13/s4)` block, and a
#     snapshot that moves moves the page under it. Predicted the
#     snapshot, missed the block.
#   * 0 PENDING MOVEMENT, predicted and measured: `sorted_by` is still
#     outside lupin 0.1.32's std subset (bs39 measured it on this
#     binary), `par` and the two borrow rows stand.
#   * §22.2's `wolf interface` DOES NOT RE-RECORD, predicted from
#     source and measured: `build_interfaces` stamps
#     `env!("CARGO_PKG_VERSION")`, bare `0.2.10`, and #292 hashes the
#     interface rather than the stamp. §25.2's `wolf publish` has no
#     toolchain line. Appendix E's `wolf --help` is byte-identical
#     (diffed against the new binary before the run).
#   * E0201's renderings do not move: s151's catalog diff touched the
#     fixture lists only. Zero E0201 snapshots re-record.
#   * THE CATALOG MOVES 170 -> 172, `{E0417, E0418}` added, `{}`
#     removed, derived from `docs/diagnostics.md` at both revisions and
#     diffed both ways. Appendix C's first count moved with it; its
#     second count moves 56 -> 57 because §5.4 now prints E0417.
#   * THE ANCHORS GROW 458 -> 466, exactly eight, both key sets diffed
#     both ways: `conc.chan.payload`, `gram.expr.if`, `gram.fmt.if`,
#     `mem.map.absent`, `os.fs.open`, `type.fn.value`, `type.map`,
#     `type.map.key`. Namespaces hold at 12 and documents at 11, so
#     Appendix D regenerates to itself.
#   * `grammar.ebnf` MOVES for the first time in three bumps: `if_expr`
#     gains its second alternative (the bare branch closed by `then`).
#     Appendix A regenerates with that one hunk.
#   * THE CHAPTER THIS LANE IS FOR. s152 types `Map[str, int]` and the
#     native tier serves every admitted key, so the count exercise runs
#     on a map alone — but `m[k] += v` is E0417 now and every tally the
#     book printed spelled it, and lupin 0.1.32 still reads an absent
#     key as `()` and refuses `+` on it. So the respelled tallies are
#     `wolf-run(…)` with their transcripts under `$ wolf run`, each row
#     naming wolf-interp#91, and they graduate as FLIPs when the mirror
#     lands. Chapter 5's own record is in its ledger.
repo = "wolffe-lang/wolf-lang"
rev = "3d0a6e1b84ab7c5168747d83570ebd533a88cbfe"
impl_version = "0.2.10+dev.3d0a6e1"

[lupin]
# wolf-interp at the v0.1.30 release tag (is42): the stamped line names
# lupin 0.1.30 at pin 2c03ed9. A lupin release's pin is the wolf-lang
# revision it was built and differentially tested against, and is42
# released against `2c03ed9` — THE EXACT REVISION [wolf] PINS ABOVE.
#
#   wolf 0.2.8+dev.2c03ed9 (wolfgang, pin 2c03ed9)
#   paired with lupin 0.1.28 (reference interpreter), pin 5c729e8
#   lupin 0.1.30 (wolf-interp, reference interpreter at pin 2c03ed9)
#
# AT bs35 THE LUPIN PIN DOES NOT MOVE, and the pairing paragraph below
# is rewritten because the OTHER half did. The stamped lines this
# printing is graded on are:
#
#   wolf 0.2.9 (wolfgang, pin 4c60946)
#   paired with lupin 0.1.29 (reference interpreter), pin e9a17cb
#   lupin 0.1.30 (wolf-interp, reference interpreter at pin 2c03ed9)
#
# NEITHER HALF IS EXACT. Both computed from the two stamps, never
# hand-written (ww16):
#
#   THE COMPILER'S HALF IS ONE RELEASE. It names lupin 0.1.29 and the
#     interpreter beside it is 0.1.30, because the interpreter
#     published once after this compiler was cut. This half read TWO at
#     the last bump and closes by one.
#   THE INTERPRETER'S HALF IS FOURTEEN COMMITS. It names 2c03ed9, which
#     is not the revision this file now pins: `git rev-list --count
#     2c03ed9..4c60946` is 14 on the same line of history, ancestry
#     checked rather than assumed. This half read ZERO at the last bump
#     and opens.
#
# STILL NO SUPERLATIVE, and the temptation here is a real one: it is
# easy to write that this is the first printing where neither name is
# exact. Nothing in this file's history supports that and nothing was
# measured to support it, so §1.2 and the colophon say what each half
# reads and stop. This file's own history is where a claimed first was
# caught (the bs32 contract's "a gap of zero for the first time", which
# the log disproved), and the halves have swapped direction on three
# consecutive bumps before this one — this bump does not swap them, it
# moves both.
#
# WHAT THE BOOK MEASURED AT THIS PIN, prediction first (bs34). All of
# it was written down BEFORE the binary was installed, in
# /private/tmp/bs34-predictions-0130.md, and every line held:
#   * PREDICTED 1 FLIP AND 1 FAILURE, and the difference between them
#     is the whole point. MEASURED exactly that.
#   * THE FLIP: ch03 §3.2's aligned `if` / `else if` / `else` block.
#     0.1.30 mirrors #276, so the interpreter starts serving a
#     `wolf-run(…)` and the runner reported it in its own words —
#     `FLIP book/ch03/s7: directive `wolf-run(exit=0,
#     stdout="a<tab>b<nl>c")` holds, but `lupin` now meets
#     `run(exit=0…)` too`. Fence graduated, the per-machine note
#     retired, ch03's ledger row closed. The `wolf fmt` clause STAYS:
#     it is about the formatter, not about which machine runs the code.
#   * THE FAILURE, PREDICTED AS A FAILURE AT THE LAST BUMP AND FILED
#     THERE: ch05 §5.4's empty `pop`. It was
#     `lupin-run(exit=trap(bounds))`, a TRAP claim, and TWO-MACHINES.md
#     §6 says the graduation probe cannot see a trap claim retire — it
#     asks `conform-run`, which reaches a verdict without executing. So
#     when #274's mirror landed the sample went RED rather than
#     flipping: `FAIL book/ch05/s7: lupin: expected the bounds trap
#     named; exit Some(0)`. Measured directly on the section's own
#     program: 0.1.29 traps `bounds` and exits 3; 0.1.30 prints `none`
#     and exits 0. Fence is `run(exit=0, stdout="none")` by hand, the
#     transcript re-recorded, and the sentence "`pop` is bounds-checked
#     for the same reason" is GONE — `[mem.list.pop]` contradicts it.
#     §5.4 now teaches the pair the clause actually draws: the four
#     recoverable reads answer `none` and never fault, and the
#     subscript is the faulting twin.
#   * ch12 §12.1 DOES NOT FLIP, predicted and measured, and this is
#     where a plausible reading of the release notes goes wrong. The
#     block is `lupin-run(…)`, which retires when the COMPILER starts
#     serving it, and the compiler still refuses: `wolf conform-run`
#     answers `fail(E1102)` on the `channel[Doc]`, with E0608 underneath
#     it because `chan.send` is typed `()` where the clause says a send
#     after close returns an error value. That second one is
#     wolf-lang#275, still OPEN. What DID move is the printed tag:
#     0.1.30 mirrors #273, so the transcript's `Closed` is `closed` on
#     both lines and was re-recorded. Fence and per-machine note stand.
#   * THE BLAST RADIUS WAS FOUR READS, NOT ONE, and it was checked
#     before it was run. #274 changes `pop`, `get`, `first` AND `last`.
#     Every site was grepped first: ch05 §5.1's TWO-MACHINE
#     `run(exit=0)` block was the risky one — it prints `popped 100`
#     and is served by both machines — and it was PREDICTED unchanged
#     on the reasoning that the compiler has typed `pop` as `T ! {none}`
#     since s37 and renders `{last}` as `100`. MEASURED unchanged on
#     both binaries. ch12's §12.4 pop, ch27 §27.5's four (all already
#     `… else { return … }`) and the corpus files in ch04, ch05, ch12,
#     ch16, ch18 and ch27 were all predicted unchanged and all measured
#     unchanged. Zero other sample motion.
#   * 4 CONSOLE BLOCKS RE-RECORDED, all predicted: §5.4's transcript,
#     §12.1's two tag lines, and the two version transcripts (ch01 §1.2
#     and the colophon), which move because the interpreter's stamp
#     moves.
#   * 0 PENDING MOVEMENT. is42 mirrors three rulings and grows no std
#     subset, so `sorted_by`/`take` (ch05 ex5-8) and `par` (ch13 ex13-1,
#     ex13-6) stay pending, as do ch07 ex7-5 and ch08 ex8-7.
#   * WHAT DID NOT LAND: wolf-interp#73. Nothing in the book is written
#     as if it had.
#   * NOTHING OF wolf-lang's MOVES: the catalog stays 168, the anchors
#     stay 446, `grammar.ebnf` is untouched and zero diagnostic
#     snapshots re-record, because [wolf] did not move at this bump.
#
# THAT NEWER COMPILER IS PINNED NOW. The paragraph that stood here said
# `wolf 0.2.9 (wolfgang, pin 4c60946)` was installed on the box and not
# pinned, and that taking it would be a deliberate bump of its own. It
# is, and bs35 is it: [wolf] above names the tag, and the numbers in
# the [wolf] note were measured with the release archive's own binary
# rather than a dist build, because at a release tag those are the same
# build.
#
# AT bs36 THIS PIN DOES NOT MOVE EITHER, and this time that is a
# decision with a measurement behind it rather than a bump that had no
# reason to happen. lupin 0.1.31 (is43, `772099c`) is released and is
# what the box carries. It was run against the WHOLE BOOK at the
# unchanged [wolf] pin, as a control, before this bump touched
# anything, and it came back:
#
#   468 passed, 5 pending, 12 FAILED, 0 flips
#
# Six samples and six console blocks, and TEN of the twelve are one
# finding — the other two are §1.2's and the colophon's `--version`
# transcripts, which move whenever the interpreter's stamp does.
# wolf-interp#79 — "a type annotation's name is resolved at last" —
# makes 0.1.31 refuse a type annotation naming something it does not
# have, and the book has two such names: chapter 11's `Scope` (ex11-1,
# ex11-2, §11.1's and §11.2's part blocks, and the two transcripts
# under them) and chapter 14's `Proc` (§14.1's and §14.2's blocks and
# their transcripts). Every one is
# `E0301: nothing named `Scope` is in scope — a type annotation names a
# type, and resolution is the rung that answers whether a name exists
# ([mod])`, exit 2.
#
# THE COMPILER SAYS THE SAME THING, which is what makes this a book
# defect rather than an interpreter regression: `wolf conform-run` at
# BOTH revisions answers `error[E0301]: nothing named `Scope` is in
# scope` on the same programs, and that E0301 is precisely why those
# blocks have been `lupin-run(…)` all along. 0.1.30 served them only
# because it never resolved an annotation's name. So the two machines
# agree now and the book is the one that is wrong, and taking 0.1.31
# is six samples to re-home plus a decision about what `Scope` and
# `Proc` are supposed to name. That is a bump of its own, with its own
# prediction; it is not a line in a wolf-side bump, and it is reported
# up rather than absorbed here. The lupin binary this pin was measured
# with was built from `08d787a`, the rev below, so what CI builds and
# what graded these pages are the same interpreter.
#
# ONE THING bs35 SAID TO REVISIT AT THIS TAG, AND THE ANSWER IS NOT
# YET FOR THE SAME REASON. 0.1.31 mirrors wolf-interp#78, so `str +
# char` is served by both machines there — which is what bs35 said
# would let chapter 2's four `"{c}"` solutions get shorter. They stay,
# because this pin is not that tag.
#
# AND IT DOES NOT MOVE AT bs36's SECOND BUMP EITHER, for the third
# printing running and still on the measurement above. The compiler
# this file now pins NAMES 0.1.31 in its second line — it was
# differentially tested against it — and this file pins 0.1.30 beside
# it. That is not a contradiction and §1.2 teaches the reader to read
# it: the compiler reports what it was tested against, the pin file
# reports what THIS BOOK was graded against, and the book cannot grade
# itself against an interpreter that refuses six of its samples. The
# gap is visible on the page rather than hidden: the pair's compiler
# half names one release ahead for the first time in this file's
# history.
# WHAT WOULD CLOSE IT is the `Scope` / `Proc` work, not a version bump.
# Both names resolve to nothing in EITHER implementation, the compiler
# has said so all along, and 0.1.31 is the release that stopped
# papering over it. That is a book defect with a book fix, and it is
# a lane of its own. is44's 0.1.32, which mirrors the range arms and
# would flip §3.2's fence, is a second reason to take the interpreter
# soon and not a reason to take it here.
#
# ===================================================================
# bs38 TAKES 0.1.31, AND THE LANE THE PARAGRAPH ABOVE ASKED FOR IS
# THIS ONE. Four printings sat at 0.1.30 for one reason and the reason
# is now closed: `Scope` and `Proc` are re-homed, and the six samples
# that refused pass.
#
# THE REV IS A COMMIT, AND THE PARAGRAPH ABOVE NAMED A TAG OBJECT. The
# note above calls 0.1.31 "is43, `772099c`". `git cat-file -t
# 772099cf661e0d04b1997a013d5b2ee48f69d100` answers **tag** — it is the
# annotated tag object for `v0.1.31`, and the commit it points at is
# `e9c55b4a388f5ace9a3cc0a4f412ce7d0e474290`. The 0.1.30 rev this file
# carried (`08d787a`) is a commit, as is every other rev here, so the
# two would not have been the same kind of object. `rev` below is the
# COMMIT, checked with `cat-file` rather than copied out of prose. The
# stamped line names `e9c55b4` too: `lupin conform-run --json` reports
# `"commit":"e9c55b4"`, which is how this was caught.
#
#   wolf 0.2.10 (wolfgang, pin 662b14c)
#   paired with lupin 0.1.31 (reference interpreter), pin 4c60946
#   lupin 0.1.31 (wolf-interp, reference interpreter at pin 4c60946)
#
# THE PAIR POINTS A FOURTH WAY, both halves computed from the two
# stamps rather than typed (ww16):
#   THE COMPILER'S HALF IS EXACT. It names `lupin 0.1.31` and this file
#     now pins 0.1.31. That half read one BEHIND at bs35, EXACT at
#     bs36, one AHEAD at bs36's second bump, and closes back to exact
#     here. No superlative and none was measured: exact has happened
#     before and the line above this one says so.
#   THE INTERPRETER'S HALF IS FIFTY-ONE COMMITS, and it CLOSES. It
#     names `4c60946`; `git rev-list --count 4c60946..662b14c` is 51 on
#     one line of history, `git merge-base --is-ancestor 4c60946
#     662b14c` checked rather than assumed. That half read 14, then 43,
#     then 65, and 51 is the first time it has narrowed. Four data
#     points, stated as four data points.
#
# WHAT THE BOOK MEASURED, PREDICTION FIRST. Every number below was
# written into /private/tmp/bs38-predictions-0131.md before a single
# file in this repository was edited, and the CONTROL was re-run at the
# unchanged pin first: 479 samples, 468 passed, 5 pending, 12 failed,
# 0 flips — bs36's control reproduced to the number, with the twelve
# named.
#   * SIX SAMPLES GO RED -> GREEN, by name, and no others move:
#     `ch11/ex11-1`, `ch11/ex11-2`, `book/ch11/part-capability`,
#     `book/ch11/part-refresher`, `book/ch14/s2`, `book/ch14/s5`.
#   * THE OTHER TWO OF THE TWELVE, NAMED: §1.2's and the colophon's
#     `--version` transcripts. NOT the same defect — those ten are
#     `Scope`/`Proc`, these two are the interpreter's own stamp moving
#     when the binary does. They re-record here because that is what a
#     pin bump is.
#   * 0 FLIPS PREDICTED AND 0 MEASURED, with the reason checked rather
#     than assumed. All six stay `lupin-run(…)`: the compiler declined
#     them BEFORE at `fail(E0301)` and declines them AFTER at
#     `fail(E0501)`, and a `fail(…)` verdict is declining. The
#     tempting reading is that re-homing might let the compiler in, so
#     it was probed directly: an INLINED §11.1 with no helper and no
#     handle crossing any signature still answers `unsupported` at
#     `wir`. Nothing in these two chapters is one name away from a
#     flip.
#   * WHAT `Scope` AND `Proc` WERE. Nothing. Not a regression and not
#     a rename: `crates/wolf_sema/src/prelude.rs`'s `BUILTIN_TYPES` is
#     sixteen scalars plus `wrapping`, `PRELUDE` carries `List`,
#     `Pool`, `channel` and `Mutex`, and there is no entry for a scope
#     handle or a proc handle anywhere. The compiler's display names
#     for the two `TyKind`s are the lowercase KEYWORDS `scope` and
#     `proc`, which `[gram.type]` does not admit in type position the
#     way it admits `region`. Measured one probe per spelling, each in
#     its own directory, on BOTH machines: `Scope`/`Proc` are E0301;
#     `scope`/`proc` are E0206 on the compiler and E0201 on the
#     interpreter; `TaskScope`, `Nursery`, `ScopeHandle`, `Task` are
#     E0301. Filed as wolf-lang#316.
#   * THE RE-HOMING IS A GENERIC PARAMETER, and lupin's own diagnostic
#     is what names it: "neither a built-in scalar, an item this module
#     declares, nor a generic parameter of `launch`". `fn
#     tally_into[S](s: S, …)` and `fn reason_of[P](p: P, …)` run under
#     the interpreter and are `E0501` on the compiler ("the bounds on
#     `S` do not provide a method `spawn`"), correct by D28. The prose
#     is re-homed on spec/03's own words rather than on the old
#     invention: `[conc.task.scope]` says a scope handle is an ordinary
#     value and that "lifetime extension is visible at every call
#     site" — the CALL site, which is what the chapter can still point
#     at. §11.1's audit paragraph changes with it: the search is
#     `.spawn(` plus `scope` blocks, not a type name in a parameter
#     list, and the chapter says why in one clause.
#   * THREE LEDGER ROWS WERE WRONG AND THEY WERE THE BOOK'S. ch11's
#     Mutex row claimed "`Scope` and `channel[T]` both cross a
#     signature cleanly"; ch14's papercut claimed "`proc` is not a type
#     name; `Proc` is … and it does, cleanly"; ch15's roster row
#     claimed "`Proc` crosses a signature … `List[Proc]()` is
#     accepted". All three rewritten with the measurement rather than
#     struck. ch14's survived three re-verifies because each one
#     re-measured the KEYWORD and never measured `Proc` itself.
#   * A SECOND FINDING, FILED: `List[Proc]()` is "accepted" by lupin
#     because lupin never resolves a type ARGUMENT in expression
#     position. `List[Nonesuch]()` constructs and prints `0`; `fn
#     f(xs: List[Nonesuch])` one line away is E0301. wolf-interp#89,
#     which also carries the E0201/E0206 divergence above.
#   * 4 CONSOLE BLOCKS GO GREEN WITH THEIR BYTES UNCHANGED, predicted
#     by name: §11.1's `$ lupin serve.lu`, §11.2's `$ lupin
#     refresher.lu`, §14.1's `$ lupin reasons.lu`, §14.2's `$ lupin
#     verbs.lu`. The re-homing changes how a parameter is spelled and
#     nothing any of those four programs prints.
#   * 2 CONSOLE BLOCKS RE-RECORD AND BOTH ARE THE PIN'S: §1.2's and
#     the colophon's. The interpreter's line alone moves; the
#     compiler's two do not, because `[wolf]` does not move here.
#   * A FIFTH TRANSCRIPT WAS WRONG AND NOTHING HAD EVER LOOKED AT IT.
#     Exercise 11-2's deadlock roster in the corpus prints `task@219`
#     — a BYTE OFFSET into the program — and `at 11:5`. At lupin
#     0.1.30 on the UNEDITED corpus file the interpreter answers
#     `task@677` and `at 18:5`: the page has been wrong since bs31
#     added the file's per-machine header comment, which moved every
#     offset in it. Predicted before it was run, from the shape of the
#     string. This is wolf-book#24's argument with a second witness,
#     and item 2 of this lane is what makes it impossible to have
#     again.
#   * NOTHING OF wolf-lang's MOVES: `[wolf]` is unchanged, so the
#     catalog stays 170, the anchors stay 458, `grammar.ebnf` is
#     untouched, Appendices A/C/D regenerate to themselves, and zero
#     diagnostic snapshots re-record.
#   * 0 PENDING MOVEMENT, predicted and measured. Five rows; is43 grows
#     no std subset, so `sorted_by`/`take` (ch05 ex5-8), `par` (ch13
#     ex13-1, ex13-6), ch07 ex7-5's borrow-escape and ch08 ex8-7's pool
#     place all stand.
#   * `str + char` STILL DOES NOT SHORTEN chapter 2's four `"{c}"`
#     solutions, and now for a different reason than bs36's. 0.1.31
#     mirrors wolf-interp#78 and this file pins 0.1.31, so the
#     interpreter serves it — and the fences there are `run(…)`, a
#     claim about BOTH machines, so they move when the COMPILER does.
#     Not this bump's business and not deferred prose: nothing on the
#     page says otherwise.
#   * is44's 0.1.32 IS NOT THIS PIN, AND IT TAGGED WHILE THIS LANE HELD
#     THE BOOK. Recorded here so the next bump does not rediscover it:
#     lupin 0.1.32 is wolf-interp `c3dd607` and declares the wolf pin
#     `e0ce018` — s147's DEV sha, one revision shy of the v0.2.10 tag
#     this file pins, which is a thing to read before taking it. Range
#     arms run under the interpreter there, so ch03 §3.2's
#     `wolf-run(…)` fence FLIPS at that bump and the six-pin papercut
#     row bs36 opened closes with it. That is the next book lane's
#     prediction, not this one's: this contract's pin is 0.1.31, and a
#     bump that flips a fence is a bump that has to predict the flip.
#     THE FLIP IS MEASURED ALREADY, by accident, and the next lane
#     inherits the measurement: `~/.local/bin/lupin` was replaced with
#     0.1.32 while this lane held the book, and the run that caught it
#     named the fence — `FLIP book/ch03/s6: directive `wolf-run(exit=0)`
#     holds, but `lupin` now meets `run(exit=0…)` too`. ONE flip, that
#     one, and nothing else moved: the only other failures in that run
#     were §1.2's and the colophon's stamp lines reading `0.1.32`.
#   * THE GAUNTLET RAN AGAINST A BINARY BUILT FROM THE REV BELOW, not
#     against whatever `~/.local/bin` held, and that is not pedantry.
#     Mid-lane the installed interpreter became 0.1.32 and three gates
#     silently started grading this printing against an interpreter it
#     does not pin. CI has never had the problem — the samples lane
#     clones wolf-interp at `[lupin].rev` and builds it — so a local run
#     that borrows `~/.local/bin` is measuring a different book from the
#     one CI measures. The final numbers here come from
#     `LUPIN=/private/tmp/bs38-bin/lupin-0.1.31`, built at `e9c55b4`,
#     and the binary is kept beside bs36's 0.1.30 control for the next
#     lane that needs to reproduce this printing.
#
# ===================================================================
# bs40 TAKES 0.1.32, THE BUMP THE PARAGRAPH ABOVE PREDICTED. is44's
# release, wolf-interp `c3dd607` — the COMMIT the annotated tag
# `v0.1.32` points at (`gh api …/git/ref/tags/v0.1.32` answers the tag
# object `19687ef`, whose target is `c3dd6077…`; checked, not copied).
# It declares pin `e0ce018`, s147's dev sha. Taken from the release
# archive (`lupin-0.1.32-aarch64-apple-darwin.tar.gz`, sha256
# `9ca0f9f3…1097`, matched against the release's own digest before
# unpacking) and staged under the FILENAME `lupin`, because a transcript
# that quotes a tool's usage line is bound to argv[0] (bs38-bin/README).
#
#   lupin 0.1.32 (wolf-interp, reference interpreter at pin e0ce018)
#
# WHAT MOVED, PREDICTED AND MEASURED ON THE UNEDITED TREE (the numbers
# are the [wolf] note's; this is the interpreter's share of them):
#   * ONE FLIP, `book/ch03/s6`, predicted by name: 0.1.32 mirrors
#     `[gram.pat.range]` (wolf-interp#83), so §3.2's switch-shaped
#     `match` with its `1..=2` arm runs on both machines — measured
#     byte-identical (`   275 a dollar or two`, exit 0) before the
#     fence moved. `wolf-run(exit=0)` -> `run(exit=0)`, the per-machine
#     sentence retired, ch03's row closed.
#   * TWO CONSOLE RE-RECORDS, the pin's own: §1.2's and the colophon's
#     `lupin --version` line, `0.1.31 … 4c60946` -> `0.1.32 … e0ce018`.
#     The compiler's two lines on the same pages move for the [wolf]
#     bump, not this one.
#   * WHAT 0.1.32 DOES NOT MIRROR, and chapter 5 is written around it:
#     `[mem.map.absent]`. An absent-key read still answers `()` (probed:
#     `[()]` where the compiler prints `[none]`), `m[k] += v` still
#     executes, and `(m[k] else 0) + v` refuses at run time (`+` is not
#     defined on () and i32`, exit 4). That is wolf-interp#91, filed
#     with the clause by s152; every tally block this bump respells is
#     `wolf-run(…)` until it lands, and each ledger row says so.
#   * 0 PENDING MOVEMENT: no std subset grew.
repo = "wolffe-lang/wolf-interp"
rev = "c3dd6077c9a9a41b72497c195a291605a3ec8d86"
version = "lupin 0.1.32 (wolf-interp, reference interpreter at pin e0ce018)"

[wolf-lsp]
# Source of the vendored tmLanguage grammars under highlight/.
# `cargo xtask grammar-sync --check` fails on drift against this pin.
# Bumped with the bs24 pin move (le05, wolf-lsp trunk). The bs13 pin
# predated s121: it knew no `char` type and no `'…'` literal, so every
# char literal in the book painted as body ink — the rendering gap
# bs13 recorded and bs23 re-stated at its closeout. This rev closes it.
# The vendored tmLanguage moves in exactly three places, all in
# wolf.tmLanguage.json (wolfi and wolf-pkg are byte-identical):
#   * a `chars` rule — `'a'`, `'\n'`, `'\x41'`, `'\u{1F41B}'` — scoped
#     `constant.character.wolf`, which is a NEW scope for the book's
#     map: highlight.rs gains `constant.character` → hl-string beneath
#     the `constant.character.escape` → hl-number entry that must keep
#     winning (first hit wins, so order is load-bearing).
#   * `char` joins the type keywords (`support.type.wolf`).
#   * attributes begin `#!?\[`, so an inner attribute `#![…]` paints
#     as an attribute instead of leaving `#!` as ink.
# What the tree-sitter grammar gained at le05 and the book does NOT
# see: `invalid_escape` (`@error`), `region_cap`, and the D67/D69
# separator work are tree-sitter nodes. The book's highlighter is the
# tmLanguage interpreter in xtask/src/tm.rs (highlight/README.md); the
# tree-sitter queries are the editors' path, not the book's. A
# `@error` entry in the book's palette waits on a tmLanguage rule that
# emits `invalid.illegal.*` — there is none at this rev, and the book
# holds no invalid escape in any rendered block to paint (measured:
# zero across 991 blocks), so the palette gains no error entry here.
# `cap`/`rc`/`pool` stay body ink: the spec calls them CONTEXTUAL
# keywords ([gram.inv.ctx]), which a regex grammar cannot tell from an
# identifier, so `region r(cap: n)` paints `region` and nothing else.
# That is correct, not a gap.
#
# THE TWO WRONG PAINTS THAT BLOCK ABOVE RECORDED ARE FIXED, AND THIS
# BUMP IS THE RE-PIN THAT TAKES THE FIX. They were filed rather than
# worked around (the bs13 precedent: a rendering gap is recorded here,
# in the pin, not patched in the prose), and the prose they distorted
# never moved, so nothing has to be un-patched now.
#   * wolf-lsp#4 — `raw-string` and `generalized-string` included
#     `#interpolation`, and the spec says both are raw-mode ([gram.lex.
#     str.raw], [gram.lex.str.gen]); tree-sitter agreed. So ch02 §2.2's
#     `r"C:\logs\{today}\wolf.log"` painted `{today}` as an
#     interpolation hole three lines above the sentence "the braces are
#     two more characters, not an interpolation", and the brace-balancer
#     solution painted its raw `r"([{}])"` data as syntax. Both rules
#     now carry `"patterns": []`, so a raw literal is one green run.
#   * wolf-lsp#5 — the interpolation hole's pattern list carried no
#     `#chars` and no nested string rule, so `"{(m + ('a' as int)) as
#     char}"` painted `as` and `char` but left `'a'` as ink, and
#     `"{"total"}"` left the nested literal as ink. The hole's list
#     gains `#chars` and a new `interpolated-string` rule after
#     `#format-spec`.
#
# THE MOVE IS c97b81c -> d194486 (wolf-lsp trunk, ANCESTOR of the old
# pin checked rather than assumed, 86 commits). Eighty-six commits and
# the vendored bytes move in ONE FILE and THREE PLACES, which is the
# whole reason this is a re-pin and not a rendering sprint:
# `wolf.tmLanguage.json` gains `interpolated-string`, empties the two
# raw patterns, and extends the hole's list; `wolfi.tmLanguage.json` and
# `wolf-pkg.tmLanguage.json` are byte-identical, as at every bump. The
# operator alternation is unchanged, so no literal-form fragment starts
# painting as a keyword.
#
# `theme/highlight.css` DOES NOT MOVE, predicted from the scope map
# before `grammar-sync` ran and measured after. Neither changed rule
# introduces a scope the book has no entry for: the new
# `interpolated-string` is `string.quoted.double.wolf`, which the
# `string.` prefix already claims for hl-string, and `#chars` is
# `constant.character.wolf`, which bs24's `constant.character` entry
# already claims — and that entry must stay BELOW
# `constant.character.escape`, which is the ordering note above and is
# now load-bearing for a second reason.
#
# NO SAMPLE AND NO CONSOLE BLOCK MOVES AT THIS BUMP. A grammar re-pin
# changes what the rendered page looks like and nothing about what the
# tools do, so the samples runner cannot see it; the render lane is the
# lane that grades it.
repo = "wolffe-lang/wolf-lsp"
rev = "d194486e78478b6a744402e541ba2d112a0dac05"

[wolf-repl]
# REPL transcript replay is pending(is08): wolf-repl blocks are counted
# and reported, never silently unchecked. Flip this to a rev when is08
# ships.
pending = "is08"
