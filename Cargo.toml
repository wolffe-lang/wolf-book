[workspace]
resolver = "2"
members = ["xtask"]

[workspace.package]
edition = "2021"
license = "GPL-3.0-or-later"

[profile.dev]
# The preprocessor runs on every build; keep it quick without a release wall.
opt-level = 1
