# The pending manifest — the runner's twin of EXERCISES-PENDING.md.
# Every row names a corpus sample whose directive is EXPECTED to fail
# against today's pinned tools: the runner executes it report-only and
# treats a pass as a FLIP (hard error) so a landing feature is noticed
# in the same commit that bumps the pin. Rows are removed, never
# silently absorbed. Workflow-only pending exercises (21-8, 22-7,
# 23-7, 24-6, B-10) have no .lu file and no row; 20-5's program (19-1
# until the bs18 re-draw renumbered the contracts corpus) runs green
# today (its pending-ness is the unverified #[noalloc], not the run)
# so it carries no row either. Old 20-6, the bench-baseline workflow
# exercise, retired with its chapter at bs18 (EXERCISES-PENDING.md's
# re-draw note).
#
# Removed at the bs10 pin bump, both as reported FLIPs: `ch13/ex13-3`
# (E1101, unsynchronized mutable capture across tasks) and `ch16/ex16-9`
# (E1102, unsendable channel payload). Both codes exist in the compiler
# at this pin and both exercises are ordinary green samples with
# reviewed snapshots.
#
# REMOVED AT THE bs50 PIN BUMP (wolf 0.2.15 / lupin 0.1.37), both as
# reported FLIPs: `ch13/ex13-1` and `ch13/ex13-6`, the two `par` rows.
# `par` is implemented on BOTH machines at this pair — it is the
# language's own builtin (`[type.comb.builtin]`), not a std function,
# so it needs no std root and both tools serve it. lupin runs it
# sequentially and prints the same bytes, which `[conc.task.par.order]`
# makes conforming. Their section, §13.1, is written in the same
# commit range, which is what these two rows were holding.
#
# At the rp01 bump (lupin v0.1.6) the *interpreter* grew the same three
# checks — E1101, E1102, E1103 — refusing at the resolve rung with
# byte-identical codes and spans. No row moved, because none was waiting
# on it: both exercises had already flipped on the compiler's half.
# Recorded because it is what makes chapter 13 writable, and because a
# differential those rows rested on is gone — lupin no longer runs a
# racy capture to a silently wrong exit 0. The two ch13 rows left below
# are `par`'s, and they are what holds §13.1. bs49 re-pointed their
# owner from two 0.1-line sprint ids at wolf-lang#390, the ruling
# wolf-book#52 asked for: the spec still defines `[conc.task.par]` at
# 0.2.14 and neither machine implements it.

[[pending]]
id = "ch05/ex5-8"
blocker = "TWO blockers, re-measured at bs50 against wolf 0.2.15 / lupin 0.1.37, and NEITHER is the one this row used to name. (1) `sorted_by` exists now — it is one of `[type.comb.set]`'s ten — but it is a `std.list` function that must reach its home module, and this repository configures no std root for either tool, so both machines decline at `[type.method.root]`: lupin `unsupported`, wolf E0301. That half is wolf-book#39, not a std-surface debt. (2) `.take(1)` can never arrive: `take` is a keyword, so `fn take` is E0008 on both machines and no std function may carry the name. This row is waiting on a spelling that cannot exist and the exercise wants rewriting (wolf-book#58)."
owner = "wolf-book#39 (the std root) + wolf-book#58 (the unspellable `take`)"

[[pending]]
id = "ch07/ex7-5"
blocker = "borrow-escape checking reaches no static verdict; wolfc leaves `channel` unresolved"
owner = "s33-channels-select, then s18-tier0-exclusivity"

[[pending]]
id = "ch08/ex8-7"
blocker = "TWO blockers, bisected at bs51 against wolf 0.2.16 / lupin 0.1.38 with two minimal programs rather than inferred from the exercise, and EACH MACHINE NOW SERVES THE HALF THE OTHER REFUSES. (1) The place write this row has always named — `pool[h].next = k` — LANDED ON THE COMPILER (s173: the pool is the base and the handle the index) and is still the interpreter's wall, in new words: lupin answers `unsupported: handle is not a `Map` key — a key is `str`, `int`, `char` or `bool` [type.map.key]` and exits 4, where wolf 0.2.16 runs the same program to exit 0. (2) A blocker that is NEW and is the COMPILER's: the LRU walk's `while cur != tail` compares two handles, and wolf answers `cannot compile this yet — `==` on a container, tuple or handle (no `Eq` for these this edition)` and exits 4, where lupin runs it. A conservatism line, not a rejection. bs51 predicted this row would flip on s173 and was wrong: s173 moved the half the row named and a second wall was behind it."
owner = "wolf-book#60 (both halves, with the measurement)"




# Removed at the bs09 pin bump, all three as reported FLIPs: `ch18/ex18-3`,
# `ch18/ex18-5` and `ch18/ex18-11`. The comptime fold now reaches a
# running program — the compiler publishes the fold table to both of its
# own lanes and `wolf run` prints the folded value — so the blocker
# ("no lane executes a comptime fold's result") is gone. lupin still
# declines `comptime fn` by design, which is why all three directives
# moved from `run(…)` to `wolf-run(…)`: the book's runner grew a lane
# that executes a sample under the compiler, for exactly the programs
# only one implementation runs.

