# The pending manifest — the runner's twin of EXERCISES-PENDING.md.
# Every row names a corpus sample whose directive is EXPECTED to fail
# against today's pinned tools: the runner executes it report-only and
# treats a pass as a FLIP (hard error) so a landing feature is noticed
# in the same commit that bumps the pin. Rows are removed, never
# silently absorbed. Workflow-only pending exercises (20-6, 21-8, 22-7,
# 23-7, 24-6, B-10) have no .lu file and no row; 19-1's program runs
# green today (its pending-ness is the unverified #[noalloc], not the
# run) so it carries no row either.
#
# Removed at the bs10 pin bump, both as reported FLIPs: `ch13/ex13-3`
# (E1101, unsynchronized mutable capture across tasks) and `ch16/ex16-9`
# (E1102, unsendable channel payload). Both codes exist in the compiler
# at this pin and both exercises are ordinary green samples with
# reviewed snapshots.

[[pending]]
id = "ch05/ex5-8"
blocker = "`sorted_by` / `take` absent from the interp std subset"
owner = "s37-core-types (std surface pinning)"

[[pending]]
id = "ch07/ex7-5"
blocker = "borrow-escape checking reaches no static verdict; wolfc leaves `channel` unresolved"
owner = "s33-channels-select, then s18-tier0-exclusivity"

[[pending]]
id = "ch08/ex8-7"
blocker = "field writes through a pool index do not denote a place in the interp std subset"
owner = "s37-core-types (std surface pinning)"

[[pending]]
id = "ch13/ex13-1"
blocker = "`par` absent from the interp std subset"
owner = "s32-tasks-scheduler / s37-core-types"

[[pending]]
id = "ch13/ex13-6"
blocker = "`par` absent from the interp std subset"
owner = "s32-tasks-scheduler / s37-core-types"


# The three ch18 rows are re-blockered at the bs08 pin. The comptime
# evaluator computes all three of these programs — that is what chapter
# 18's E0705/E0706/E0710 verdicts prove — so "positive comptime
# evaluation" is no longer the blocker. What is missing is a lane that
# executes the *result*: `wolf build`/`wolf run` decline any module
# holding a `comptime fn`, the folded value is not handed to the lane
# that runs the program, and lupin declines `comptime fn` outright. Each
# row's directive is a `run(…)`, which the runner checks under lupin.

[[pending]]
id = "ch18/ex18-3"
blocker = "no lane executes a comptime fold's result: comptime-fn lowering refused natively, `comptime fn` declined by lupin"
owner = "c05-codegen (comptime-fn lowering) / wolf-interp std subset"

[[pending]]
id = "ch18/ex18-5"
blocker = "the same lane gap, plus a `typeinfo` result reaching a runtime const is `calls outside the modelled surface` in the checked lane"
owner = "c05-codegen (comptime-fn lowering) / wolf-interp std subset"

[[pending]]
id = "ch18/ex18-11"
blocker = "no lane executes a comptime fold's result: comptime-fn lowering refused natively, `comptime fn` declined by lupin"
owner = "c05-codegen (comptime-fn lowering) / wolf-interp std subset"
