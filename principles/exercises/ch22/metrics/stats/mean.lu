//! member: true
// The `stats` module: one export; `total` stays private to the module.
pub fn mean(xs: List[int]) -> int {
    if xs.is_empty() { 0 } else { total(xs) / xs.len }
}

fn total(xs: List[int]) -> int {
    var t = 0
    for x in xs { t += x }
    t
}
