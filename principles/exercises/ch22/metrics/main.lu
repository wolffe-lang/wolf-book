//! check: run(exit=0, stdout="6")
//! phase: run
// Exercise 22-1: directory = module; `stats/` is the module, its files
// are invisible from here.
use stats

fn main() -> !int {
    var xs = List[int]()
    xs.push(4)
    xs.push(6)
    xs.push(8)
    print("{stats.mean(xs)}")
    0
}
