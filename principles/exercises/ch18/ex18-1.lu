//! check: fail(E0705)
//! phase: typecheck
// Exercise 18-1: a comptime call whose argument arrives at runtime.
comptime fn double(n: int) -> int {
    n + n
}
fn main() -> !int {
    let x = 21
    const Y = double(x)
    if Y == 42 { 0 } else { 1 }
}
