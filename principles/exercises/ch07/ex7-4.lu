//! check: run(exit=trap(use-after-move))
//! phase: run
// Exercise 7-4: structs move on assignment, whatever their fields hold.
struct P { x: int, y: int }
fn main() -> !int {
    let a = P { x: 1, y: 2 }
    let b = a
    print("{a.x} {b.y}")
    0
}
