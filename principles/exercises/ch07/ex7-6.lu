//! check: run(exit=0, stdout="3 1")
//! phase: run
// Exercise 7-6 solution: swap, and the audit that finds it.
fn swap(mut a: int, mut b: int) {
    let t = a
    a = b
    b = t
}
fn main() -> !int {
    var x = 1
    var y = 3
    swap(mut x, mut y)
    print("{x} {y}")
    0
}
