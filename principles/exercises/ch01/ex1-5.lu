//! check: run(exit=0)
//! phase: run
// Exercise 1-5 solution: the temperature table, wolf's first table.
fn main() -> !int {
    var f = 0
    while f <= 120 {
        let c = (f - 32) * 5 / 9
        print("{f}	{c}")
        f += 20
    }
    0
}
