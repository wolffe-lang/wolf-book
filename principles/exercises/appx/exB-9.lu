//! check: run(exit=trap(deadlock))
//! phase: run
// Appendix B, exercise B-9: deadlock — one task, one unbuffered channel,
// nobody coming.
fn main() -> !int {
    let ch = channel[int](0)
    let v = ch.recv() else |_| { return 1 }
    v
}
